// DO NOT EDIT — generated by generate-java-sdk-public.ts
//
// Zero-dependency library build. Maven Central and JitPack are both deferred
// (JitPack needs a root build config to resolve this java/ subdirectory module);
// consumers install from source — see https://github.com/stat-api/stat-api/tree/main/java.
plugins {
    `java-library`
    `maven-publish`
}

group = "com.stat-api"
version = "0.1.0"

java {
    toolchain {
        languageVersion.set(JavaLanguageVersion.of(17))
    }
    withSourcesJar()
    withJavadocJar()
}

repositories {
    mavenCentral()
}

publishing {
    publications {
        create<MavenPublication>("maven") {
            groupId = "com.stat-api"
            artifactId = "statapi"
            version = "0.1.0"
            from(components["java"])
        }
    }
}
