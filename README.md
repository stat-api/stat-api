<!-- DO NOT EDIT — generated by packages/codegen/src/generators/generate-repo-home.ts
     (sports-db monorepo). Version 0.1.0. Regenerate with: codegen public -->

# stat-api

One API key for NBA, NFL, MLB, NHL, and PGA Tour data — plus DFS salaries/projections and prediction-market tape from Kalshi and Polymarket. 177 public tables over REST, GraphQL, and MCP, with official client libraries in TypeScript, Python, Go, Java, and C#.

**[Get a free API key →](https://stat-api.com)** · API base `https://api.stat-api.com` · [Interactive docs](https://stat-api.com/docs)

## 60-second quickstart

Create a key at [stat-api.com](https://stat-api.com), `export STAT_API_KEY=...`, pick a language:

<details open><summary><strong>TypeScript</strong></summary>

```sh
npm install @stat-api/client
```

```typescript
// Hello, NBA
// Generated from schema/api/examples/hello-stat-api.yml — do not edit.

import { StatApi } from '@stat-api/client'

const api = new StatApi() // reads STAT_API_KEY from the environment

// List a few teams
const teams = (await api.nba.teams.list({ limit: 3 })).teams

// Print what came back
console.log(JSON.stringify(teams, null, 2))
```

</details>

<details><summary><strong>Python</strong></summary>

```sh
pip install statapi
```

```python
# Hello, NBA
# Generated from schema/api/examples/hello-stat-api.yml — do not edit.
from statapi import StatApi
import json

api = StatApi()  # reads STAT_API_KEY from the environment

# List a few teams
teams = api.nba.teams.list(limit=3).rows

# Print what came back
print(json.dumps(teams, indent=2, default=str))
```

</details>

<details><summary><strong>Go</strong></summary>

```sh
go get github.com/stat-api/stat-api/go@latest
```

```go
// Hello, NBA
// Generated from schema/api/examples/hello-stat-api.yml — do not edit.
package main

import (
	"context"
	"encoding/json"
	"fmt"
	statapi "github.com/stat-api/stat-api/go"
	"log"
)

func main() {
	client, err := statapi.New()
	if err != nil {
		log.Fatal(err)
	}
	ctx := context.Background()

	// List a few teams
	teamsPage, err := client.NBA.Teams.List(ctx, &statapi.NBATeamsListParams{Limit: statapi.Int(3)})
	if err != nil {
		log.Fatal(err)
	}
	teams := teamsPage.Rows

	// Print what came back
	teamsJSON, err := json.MarshalIndent(teams, "", "  ")
	if err != nil {
		log.Fatal(err)
	}
	fmt.Println(string(teamsJSON))

}
```

</details>

<details><summary><strong>Java</strong></summary>

```sh
git clone https://github.com/stat-api/stat-api
```

```java
// Hello, NBA
// Generated from schema/api/examples/hello-stat-api.yml — do not edit.
package com.statapi.cookbook;

import com.statapi.StatApi;
import com.statapi.nba.*;
import java.util.*;

public final class NbaHelloStatApi {
    public static void main(String[] args) {
        StatApi api = new StatApi(); // reads STAT_API_KEY from the environment

        // List a few teams
        List<NBATeam> teams = api.nba().teams().list(new NBATeamsListParams().limit(3L)).rows();

        // Print what came back
        System.out.println(teams);

    }
}
```

</details>

<details><summary><strong>C#</strong></summary>

```sh
git clone https://github.com/stat-api/stat-api
```

```csharp
// Hello, NBA
// Generated from schema/api/examples/hello-stat-api.yml — do not edit.
using StatApi;
using System.Text.Json;

var client = new StatApiClient(); // reads STAT_API_KEY from the environment

// List a few teams
var teams = (await client.Nba.Teams.ListAsync(new NbaTeamsListParams { Limit = 3 })).Rows;

// Print what came back
Console.WriteLine(JsonSerializer.Serialize(teams, new JsonSerializerOptions { WriteIndented = true }));
```

</details>

No SDK? Plain HTTP works everywhere:

```sh
curl -sS --compressed \
  -H "Authorization: Bearer $STAT_API_KEY" \
  "https://api.stat-api.com/api/v1/nba/teams?limit=3"
```

Every list endpoint returns the same envelope — rows keyed by table name plus keyset-pagination fields:

```json
{ "teams": [ { "id": 1, "...": "..." } ], "limit": 3, "next_from_id": 4 }
```

## Which are you?

| You are… | Start here | You’ll build/learn |
| --- | --- | --- |
| Web & product developers | [`demos/dashboard/`](demos/dashboard) | a live-games React dashboard — SSE fan-out over a poll-only API, quota self-throttle |
| Daily-fantasy players | [`demos/notebooks/daily-fantasy/`](demos/notebooks/daily-fantasy) | DFS slates → salaries → a value screen, incl. required filter sets |
| Bettors | [`demos/notebooks/betting/`](demos/notebooks/betting) | sportsbook line movement → implied-probability curves |
| Prediction-market traders | [`demos/notebooks/prediction-markets/`](demos/notebooks/prediction-markets) | Kalshi market microstructure, joined to the underlying game |
| Sports quants | [`demos/notebooks/sports-quants/`](demos/notebooks/sports-quants) | auto-paged player logs and rolling-average features |
| Backend & terminal developers | [`demos/cli/`](demos/cli) | a stdlib-only Go CLI — scores, box scores, standings, quota |
| League & community operators | [`demos/discord-bot/`](demos/discord-bot) | a Discord bot with TTL caching + single-flight quota discipline |
| Just exploring | [`cookbook/`](cookbook) | runnable examples in all 5 languages, tiered basic → advanced |

## Data nobody else has

- **Prediction markets ⊗ games** — Kalshi tape joined to the box score through `competition_id` — market microstructure and the sporting event in one query. [runnable →](cookbook/typescript/src/nba-kalshi-markets-for-game.ts)
- **Every MLB pitch** — full pitch-by-pitch — velocity, movement, location — via `mlb.pitches`. [table docs →](https://stat-api.com/docs)
- **Shot-level golf** — TOURCAST shot traces and strokes-gained via `pga.player_shots`. [table docs →](https://stat-api.com/docs)
- **NBA possessions & lineup stints** — possession-grain results and who-was-on-floor lineup stints, not just box scores. [table docs →](https://stat-api.com/docs)
- **NFL Next Gen + PFR advanced stats** — per-game NGS passing/receiving/rushing and PFR advanced splits. [table docs →](https://stat-api.com/docs)
- **DFS salaries, projections & ownership** — operator slates with salaries, projections, and contest ownership. [runnable →](demos/notebooks/daily-fantasy)
- **Sportsbook line movement** — timestamped game lines across books — openers to close. [runnable →](demos/notebooks/betting)

## The SDKs

Five clients generated from one schema with identical semantics: zero runtime dependencies, env-var zero config (`STAT_API_KEY`), typed errors, auto-paginating iterators, and quota surfacing. Versions move in lockstep.

| Language | Package | Install | Source |
| --- | --- | --- | --- |
| TypeScript | [`@stat-api/client`](https://www.npmjs.com/package/@stat-api/client) | `npm install @stat-api/client` | [`typescript/`](typescript) |
| Python | [`statapi`](https://pypi.org/project/statapi/) | `pip install statapi` | [`python/`](python) |
| Go | [`github.com/stat-api/stat-api/go`](https://pkg.go.dev/github.com/stat-api/stat-api/go) | `go get github.com/stat-api/stat-api/go@latest` | [`go/`](go) |
| Java | Maven Central (coming soon) | `git clone https://github.com/stat-api/stat-api` | [`java/`](java) |
| C# | NuGet (coming soon) | `git clone https://github.com/stat-api/stat-api` | [`dotnet/`](dotnet) |

> **v0.1.0 is rolling out.** Registry links go live as each artifact publishes; anything that 404s today lands with the current release.

## Data coverage

| Group | Tables | Highlights |
| --- | --- | --- |
| `nfl` | 42 | games, play-by-play, drives, NGS/PFR advanced stats, depth charts, contracts, combine, weather |
| `mlb` | 38 | games, pitch-by-pitch, at-bats, batter/pitcher splits, park factors, umpires, starting lineups |
| `nba` | 29 | games, play-by-play, possessions, lineup stints, standings, referees, props |
| `nhl` | 22 | games, skater/goalie stats, line combinations, scoring plays *(coming soon)* |
| `pga` | 15 | tournaments, leaderboards, scorecards, shot-level TOURCAST data, strokes gained |
| `dfs` | 13 | slates, salaries, projections, contests, ownership |
| `polymarket` | 10 | events, markets, order-book snapshots, price histories, trades |
| `kalshi` | 7 | series, events, markets, candles, public trades |
| `reference` | 1 | cross-domain operators |

## Core concepts

| Guide | Covers |
| --- | --- |
| [Authentication](docs/authentication.md) | API keys, the `Authorization` header, environment variables |
| [Pagination](docs/pagination.md) | keyset paging with `limit`/`from_id`, ordering, required filter sets |
| [Errors](docs/errors.md) | status codes, error body shape, SDK retry policy |
| [Quota](docs/quota.md) | record metering, `X-Quota-*` headers, 429 behavior |

## License

Everything in this repository is [MIT licensed](LICENSE). API usage is governed by the [stat-api terms](https://stat-api.com).
