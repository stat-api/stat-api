# Go cookbook

Examples for [`github.com/stat-api/stat-api/go`](https://pkg.go.dev/github.com/stat-api/stat-api/go) (v0.1.0).

## Run

```sh
go mod download
export STAT_API_KEY=...
go run ./nba-hello-stat-api
```

Each example is its own `main` package in a subdirectory.
