# C# cookbook

NuGet publishing is deferred; reference the `StatApi` project
from source (v0.1.0). Requires .NET 8.

## Run

```sh
export STAT_API_KEY=...
dotnet run --project NbaHelloStatApi
```

Each example is its own console project; `Cookbook.sln` references them all.
