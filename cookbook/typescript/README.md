# TypeScript cookbook

Examples for [`@stat-api/client`](https://www.npmjs.com/package/@stat-api/client) (v0.1.0).

## Run

```sh
npm install
export STAT_API_KEY=...
npx tsx src/nba-hello-stat-api.ts
```

Each file in `src/` is a standalone program that reads the API key from
`STAT_API_KEY`.
