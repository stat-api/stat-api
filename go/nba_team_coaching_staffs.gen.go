// Code generated by generate-go-sdk-public.ts. DO NOT EDIT.
//
// SDK resource for nba.team_coaching_staffs (public API).
// Source: schema/api/groups/public.yml + schema/model/nba/*.yml

package statapi

import (
	"context"
	"encoding/json"
	"iter"
	"net/url"
	"strconv"
)

// NBATeamCoachingStaff is a row from nba.team_coaching_staffs.
type NBATeamCoachingStaff struct {
	ID              int64   `json:"id"`
	CoachingStaffID int64   `json:"coaching_staff_id"`
	SeasonID        int64   `json:"season_id"`
	TeamID          int64   `json:"team_id"`
	CoachType       string  `json:"coach_type"`
	EndDate         *string `json:"end_date"`
	IsAssistant     bool    `json:"is_assistant"`
	SortSequence    *int    `json:"sort_sequence"`
	StartDate       string  `json:"start_date"`
}

// NBATeamCoachingStaffsListParams are the query parameters for listing nba.team_coaching_staffs.
type NBATeamCoachingStaffsListParams struct {
	TeamID    *int64
	SeasonID  *int64
	CoachType *string
	Limit     *int
	FromID    *int64
}

func (p *NBATeamCoachingStaffsListParams) values() url.Values {
	v := url.Values{}
	if p == nil {
		return v
	}
	if p.TeamID != nil {
		v.Set("team_id", strconv.FormatInt(*p.TeamID, 10))
	}
	if p.SeasonID != nil {
		v.Set("season_id", strconv.FormatInt(*p.SeasonID, 10))
	}
	if p.CoachType != nil {
		v.Set("coach_type", *p.CoachType)
	}
	if p.Limit != nil {
		v.Set("limit", strconv.Itoa(*p.Limit))
	}
	if p.FromID != nil {
		v.Set("from_id", strconv.FormatInt(*p.FromID, 10))
	}
	return v
}

// NBATeamCoachingStaffsPage is one page of nba.team_coaching_staffs results plus the keyset
// cursor to the next page (NextFromID is nil on the final page).
type NBATeamCoachingStaffsPage struct {
	Rows       []NBATeamCoachingStaff `json:"team_coaching_staffs"`
	Limit      int                    `json:"limit"`
	NextFromID *int64                 `json:"next_from_id"`
	Quota      Quota                  `json:"-"`
}

// NBATeamCoachingStaffsService accesses the nba.team_coaching_staffs endpoints.
type NBATeamCoachingStaffsService struct {
	client *Client
}

// List returns one page of nba.team_coaching_staffs matching params
// (pass nil for no filters).
func (s *NBATeamCoachingStaffsService) List(ctx context.Context, params *NBATeamCoachingStaffsListParams) (*NBATeamCoachingStaffsPage, error) {
	body, quota, err := s.client.get(ctx, "/api/v1/nba/team_coaching_staffs", params.values())
	if err != nil {
		return nil, err
	}
	var page NBATeamCoachingStaffsPage
	if err := json.Unmarshal(body, &page); err != nil {
		return nil, err
	}
	page.Quota = quota
	return &page, nil
}

// Get returns a single nba.team_coaching_staffs row by id.
func (s *NBATeamCoachingStaffsService) Get(ctx context.Context, id int64) (*NBATeamCoachingStaff, error) {
	body, _, err := s.client.get(ctx, "/api/v1/nba/team_coaching_staffs/"+strconv.FormatInt(id, 10), nil)
	if err != nil {
		return nil, err
	}
	var row NBATeamCoachingStaff
	if err := json.Unmarshal(body, &row); err != nil {
		return nil, err
	}
	return &row, nil
}

// All auto-paginates nba.team_coaching_staffs, walking next_from_id.
// Iteration stops on the first error, yielded with a zero-value row.
// Always walks with FromID set (starting at 0): from_id forces the server's
// ascending keyset order; without it, DESC-default tables re-yield page 1 and
// under-walk. Iteration is therefore always oldest-first.
func (s *NBATeamCoachingStaffsService) All(ctx context.Context, params *NBATeamCoachingStaffsListParams) iter.Seq2[NBATeamCoachingStaff, error] {
	return func(yield func(NBATeamCoachingStaff, error) bool) {
		page := NBATeamCoachingStaffsListParams{}
		if params != nil {
			page = *params
		}
		if page.FromID == nil {
			page.FromID = new(int64)
		}
		for {
			resp, err := s.List(ctx, &page)
			if err != nil {
				var zero NBATeamCoachingStaff
				yield(zero, err)
				return
			}
			for _, row := range resp.Rows {
				if !yield(row, nil) {
					return
				}
			}
			if resp.NextFromID == nil {
				return
			}
			page.FromID = resp.NextFromID
		}
	}
}
