// Code generated by generate-go-sdk-public.ts. DO NOT EDIT.
//
// SDK resource for nba.team_game_contexts (public API).
// Source: schema/api/groups/public.yml + schema/model/nba/*.yml

package statapi

import (
	"context"
	"encoding/json"
	"iter"
	"net/url"
	"strconv"
)

// NBATeamGameContext is a row from nba.team_game_contexts.
type NBATeamGameContext struct {
	ID             int64   `json:"id"`
	GameID         int64   `json:"game_id"`
	OpponentTeamID int64   `json:"opponent_team_id"`
	SeasonID       int64   `json:"season_id"`
	TeamID         int64   `json:"team_id"`
	ConferenceRank int     `json:"conference_rank"`
	GameType       string  `json:"game_type"`
	GamesRemaining int     `json:"games_remaining"`
	Intensity      int     `json:"intensity"`
	IsBackToBack   bool    `json:"is_back_to_back"`
	IsClinch       bool    `json:"is_clinch"`
	IsElimination  bool    `json:"is_elimination"`
	IstGroupRank   *int    `json:"ist_group_rank"`
	IstGroupRecord *string `json:"ist_group_record"`
	Motivation     string  `json:"motivation"`
	RestDays       int     `json:"rest_days"`
	SeasonPct      float64 `json:"season_pct"`
	SeasonPhase    string  `json:"season_phase"`
	TeamSeriesWins int     `json:"team_series_wins"`
	WinPct         float64 `json:"win_pct"`
}

// NBATeamGameContextsListParams are the query parameters for listing nba.team_game_contexts.
type NBATeamGameContextsListParams struct {
	GameID   *int64
	TeamID   *int64
	SeasonID *int64
	Limit    *int
	FromID   *int64
}

func (p *NBATeamGameContextsListParams) values() url.Values {
	v := url.Values{}
	if p == nil {
		return v
	}
	if p.GameID != nil {
		v.Set("game_id", strconv.FormatInt(*p.GameID, 10))
	}
	if p.TeamID != nil {
		v.Set("team_id", strconv.FormatInt(*p.TeamID, 10))
	}
	if p.SeasonID != nil {
		v.Set("season_id", strconv.FormatInt(*p.SeasonID, 10))
	}
	if p.Limit != nil {
		v.Set("limit", strconv.Itoa(*p.Limit))
	}
	if p.FromID != nil {
		v.Set("from_id", strconv.FormatInt(*p.FromID, 10))
	}
	return v
}

// NBATeamGameContextsPage is one page of nba.team_game_contexts results plus the keyset
// cursor to the next page (NextFromID is nil on the final page).
type NBATeamGameContextsPage struct {
	Rows       []NBATeamGameContext `json:"team_game_contexts"`
	Limit      int                  `json:"limit"`
	NextFromID *int64               `json:"next_from_id"`
	Quota      Quota                `json:"-"`
}

// NBATeamGameContextsService accesses the nba.team_game_contexts endpoints.
type NBATeamGameContextsService struct {
	client *Client
}

// List returns one page of nba.team_game_contexts matching params
// (pass nil for no filters).
func (s *NBATeamGameContextsService) List(ctx context.Context, params *NBATeamGameContextsListParams) (*NBATeamGameContextsPage, error) {
	body, quota, err := s.client.get(ctx, "/api/v1/nba/team_game_contexts", params.values())
	if err != nil {
		return nil, err
	}
	var page NBATeamGameContextsPage
	if err := json.Unmarshal(body, &page); err != nil {
		return nil, err
	}
	page.Quota = quota
	return &page, nil
}

// Get returns a single nba.team_game_contexts row by id.
func (s *NBATeamGameContextsService) Get(ctx context.Context, id int64) (*NBATeamGameContext, error) {
	body, _, err := s.client.get(ctx, "/api/v1/nba/team_game_contexts/"+strconv.FormatInt(id, 10), nil)
	if err != nil {
		return nil, err
	}
	var row NBATeamGameContext
	if err := json.Unmarshal(body, &row); err != nil {
		return nil, err
	}
	return &row, nil
}

// All auto-paginates nba.team_game_contexts, walking next_from_id.
// Iteration stops on the first error, yielded with a zero-value row.
// Always walks with FromID set (starting at 0): from_id forces the server's
// ascending keyset order; without it, DESC-default tables re-yield page 1 and
// under-walk. Iteration is therefore always oldest-first.
func (s *NBATeamGameContextsService) All(ctx context.Context, params *NBATeamGameContextsListParams) iter.Seq2[NBATeamGameContext, error] {
	return func(yield func(NBATeamGameContext, error) bool) {
		page := NBATeamGameContextsListParams{}
		if params != nil {
			page = *params
		}
		if page.FromID == nil {
			page.FromID = new(int64)
		}
		for {
			resp, err := s.List(ctx, &page)
			if err != nil {
				var zero NBATeamGameContext
				yield(zero, err)
				return
			}
			for _, row := range resp.Rows {
				if !yield(row, nil) {
					return
				}
			}
			if resp.NextFromID == nil {
				return
			}
			page.FromID = resp.NextFromID
		}
	}
}
