// Code generated by generate-go-sdk-public.ts. DO NOT EDIT.
//
// SDK resource for nba.game_team_lineup_stints (public API).
// Source: schema/api/groups/public.yml + schema/model/nba/*.yml

package statapi

import (
	"context"
	"encoding/json"
	"iter"
	"net/url"
	"strconv"
)

// NBAGameTeamLineupStint is a row from nba.game_team_lineup_stints.
type NBAGameTeamLineupStint struct {
	ID                  int64           `json:"id"`
	GameID              int64           `json:"game_id"`
	Player1ID           int64           `json:"player1_id"`
	Player2ID           int64           `json:"player2_id"`
	Player3ID           int64           `json:"player3_id"`
	Player4ID           int64           `json:"player4_id"`
	Player5ID           int64           `json:"player5_id"`
	TeamID              int64           `json:"team_id"`
	AssistPct           float64         `json:"assist_pct"`
	DefensiveRating     float64         `json:"defensive_rating"`
	DefensiveReboundPct float64         `json:"defensive_rebound_pct"`
	EndClock            int             `json:"end_clock"`
	EndEventSeq         int             `json:"end_event_seq"`
	NetRating           float64         `json:"net_rating"`
	OffensiveRating     float64         `json:"offensive_rating"`
	OffensiveReboundPct float64         `json:"offensive_rebound_pct"`
	Pace                float64         `json:"pace"`
	Period              int             `json:"period"`
	PlayerStats         json.RawMessage `json:"player_stats"`
	Possessions         int             `json:"possessions"`
	PtsAllowed          int             `json:"pts_allowed"`
	PtsScored           int             `json:"pts_scored"`
	StartClock          int             `json:"start_clock"`
	StartEventSeq       int             `json:"start_event_seq"`
	TurnoverPct         float64         `json:"turnover_pct"`
}

// NBAGameTeamLineupStintsListParams are the query parameters for listing nba.game_team_lineup_stints.
type NBAGameTeamLineupStintsListParams struct {
	GameID *int64
	TeamID *int64
	Limit  *int
	FromID *int64
}

func (p *NBAGameTeamLineupStintsListParams) values() url.Values {
	v := url.Values{}
	if p == nil {
		return v
	}
	if p.GameID != nil {
		v.Set("game_id", strconv.FormatInt(*p.GameID, 10))
	}
	if p.TeamID != nil {
		v.Set("team_id", strconv.FormatInt(*p.TeamID, 10))
	}
	if p.Limit != nil {
		v.Set("limit", strconv.Itoa(*p.Limit))
	}
	if p.FromID != nil {
		v.Set("from_id", strconv.FormatInt(*p.FromID, 10))
	}
	return v
}

// NBAGameTeamLineupStintsPage is one page of nba.game_team_lineup_stints results plus the keyset
// cursor to the next page (NextFromID is nil on the final page).
type NBAGameTeamLineupStintsPage struct {
	Rows       []NBAGameTeamLineupStint `json:"game_team_lineup_stints"`
	Limit      int                      `json:"limit"`
	NextFromID *int64                   `json:"next_from_id"`
	Quota      Quota                    `json:"-"`
}

// NBAGameTeamLineupStintsService accesses the nba.game_team_lineup_stints endpoints.
type NBAGameTeamLineupStintsService struct {
	client *Client
}

// List returns one page of nba.game_team_lineup_stints matching params
// (pass nil for no filters).
func (s *NBAGameTeamLineupStintsService) List(ctx context.Context, params *NBAGameTeamLineupStintsListParams) (*NBAGameTeamLineupStintsPage, error) {
	body, quota, err := s.client.get(ctx, "/api/v1/nba/game_team_lineup_stints", params.values())
	if err != nil {
		return nil, err
	}
	var page NBAGameTeamLineupStintsPage
	if err := json.Unmarshal(body, &page); err != nil {
		return nil, err
	}
	page.Quota = quota
	return &page, nil
}

// Get returns a single nba.game_team_lineup_stints row by id.
func (s *NBAGameTeamLineupStintsService) Get(ctx context.Context, id int64) (*NBAGameTeamLineupStint, error) {
	body, _, err := s.client.get(ctx, "/api/v1/nba/game_team_lineup_stints/"+strconv.FormatInt(id, 10), nil)
	if err != nil {
		return nil, err
	}
	var row NBAGameTeamLineupStint
	if err := json.Unmarshal(body, &row); err != nil {
		return nil, err
	}
	return &row, nil
}

// All auto-paginates nba.game_team_lineup_stints, walking next_from_id.
// Iteration stops on the first error, yielded with a zero-value row.
// Always walks with FromID set (starting at 0): from_id forces the server's
// ascending keyset order; without it, DESC-default tables re-yield page 1 and
// under-walk. Iteration is therefore always oldest-first.
func (s *NBAGameTeamLineupStintsService) All(ctx context.Context, params *NBAGameTeamLineupStintsListParams) iter.Seq2[NBAGameTeamLineupStint, error] {
	return func(yield func(NBAGameTeamLineupStint, error) bool) {
		page := NBAGameTeamLineupStintsListParams{}
		if params != nil {
			page = *params
		}
		if page.FromID == nil {
			page.FromID = new(int64)
		}
		for {
			resp, err := s.List(ctx, &page)
			if err != nil {
				var zero NBAGameTeamLineupStint
				yield(zero, err)
				return
			}
			for _, row := range resp.Rows {
				if !yield(row, nil) {
					return
				}
			}
			if resp.NextFromID == nil {
				return
			}
			page.FromID = resp.NextFromID
		}
	}
}
