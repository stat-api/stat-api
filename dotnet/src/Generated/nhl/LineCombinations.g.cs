// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for nhl.line_combinations (public API).
// Source: schema/api/groups/public.yml + schema/model/nhl/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record NhlLineCombination
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("player1_id")]
    public long Player1Id { get; init; }

    [JsonPropertyName("player2_id")]
    public long? Player2Id { get; init; }

    [JsonPropertyName("player3_id")]
    public long? Player3Id { get; init; }

    [JsonPropertyName("player4_id")]
    public long? Player4Id { get; init; }

    [JsonPropertyName("player5_id")]
    public long? Player5Id { get; init; }

    [JsonPropertyName("season_id")]
    public long SeasonId { get; init; }

    [JsonPropertyName("team_id")]
    public long TeamId { get; init; }

    [JsonPropertyName("day")]
    public required string Day { get; init; }

    [JsonPropertyName("line_number")]
    public int LineNumber { get; init; }

    [JsonPropertyName("line_type")]
    public required string LineType { get; init; }
}

public sealed class NhlLineCombinationsListParams
{
    public long? TeamId { get; set; }
    public long? SeasonId { get; set; }
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (TeamId is not null)
        {
            query.Add(new KeyValuePair<string, string>("team_id", TeamId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (SeasonId is not null)
        {
            query.Add(new KeyValuePair<string, string>("season_id", SeasonId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class NhlLineCombinationsResource
{
    private const string Path = "/api/v1/nhl/line_combinations";
    private readonly HttpTransport _http;

    public NhlLineCombinationsResource(HttpTransport http) => _http = http;

    public async Task<Page<NhlLineCombination>> ListAsync(NhlLineCombinationsListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<NhlLineCombination>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("line_combinations", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<NhlLineCombination>()!);
            }
        }
        return new Page<NhlLineCombination>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<NhlLineCombination> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<NhlLineCombination>()!;
    }

    public async IAsyncEnumerable<NhlLineCombination> IterateAsync(NhlLineCombinationsListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new NhlLineCombinationsListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
