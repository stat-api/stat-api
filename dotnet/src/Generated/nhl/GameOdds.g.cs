// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for nhl.game_odds (public API).
// Source: schema/api/groups/public.yml + schema/model/nhl/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record NhlGameOdd
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("game_id")]
    public long GameId { get; init; }

    [JsonPropertyName("away_money_line")]
    public int? AwayMoneyLine { get; init; }

    [JsonPropertyName("away_point_spread")]
    public double? AwayPointSpread { get; init; }

    [JsonPropertyName("away_point_spread_payout")]
    public int? AwayPointSpreadPayout { get; init; }

    [JsonPropertyName("draw_money_line")]
    public int? DrawMoneyLine { get; init; }

    [JsonPropertyName("home_money_line")]
    public int? HomeMoneyLine { get; init; }

    [JsonPropertyName("home_point_spread")]
    public double? HomePointSpread { get; init; }

    [JsonPropertyName("home_point_spread_payout")]
    public int? HomePointSpreadPayout { get; init; }

    [JsonPropertyName("over_payout")]
    public int? OverPayout { get; init; }

    [JsonPropertyName("over_under")]
    public double? OverUnder { get; init; }

    [JsonPropertyName("sportsbook")]
    public string? Sportsbook { get; init; }

    [JsonPropertyName("under_payout")]
    public int? UnderPayout { get; init; }
}

public sealed class NhlGameOddsListParams
{
    public long? GameId { get; set; }
    public string? Sportsbook { get; set; }
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (GameId is not null)
        {
            query.Add(new KeyValuePair<string, string>("game_id", GameId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (Sportsbook is not null)
        {
            query.Add(new KeyValuePair<string, string>("sportsbook", Sportsbook));
        }
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class NhlGameOddsResource
{
    private const string Path = "/api/v1/nhl/game_odds";
    private readonly HttpTransport _http;

    public NhlGameOddsResource(HttpTransport http) => _http = http;

    public async Task<Page<NhlGameOdd>> ListAsync(NhlGameOddsListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<NhlGameOdd>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("game_odds", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<NhlGameOdd>()!);
            }
        }
        return new Page<NhlGameOdd>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<NhlGameOdd> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<NhlGameOdd>()!;
    }

    public async IAsyncEnumerable<NhlGameOdd> IterateAsync(NhlGameOddsListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new NhlGameOddsListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
