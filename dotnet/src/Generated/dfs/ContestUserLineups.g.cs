// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for dfs.contest_user_lineups (public API).
// Source: schema/api/groups/public.yml + schema/model/dfs/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record DfsContestUserLineup
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("contest_id")]
    public long ContestId { get; init; }

    [JsonPropertyName("contest_user_id")]
    public long ContestUserId { get; init; }

    [JsonPropertyName("external_id")]
    public long ExternalId { get; init; }

    [JsonPropertyName("points")]
    public double? Points { get; init; }

    [JsonPropertyName("rank")]
    public int? Rank { get; init; }

    [JsonPropertyName("winnings")]
    public int? Winnings { get; init; }
}

public sealed class DfsContestUserLineupsListParams
{
    public long? ContestId { get; set; }
    public long? ContestUserId { get; set; }
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (ContestId is not null)
        {
            query.Add(new KeyValuePair<string, string>("contest_id", ContestId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (ContestUserId is not null)
        {
            query.Add(new KeyValuePair<string, string>("contest_user_id", ContestUserId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class DfsContestUserLineupsResource
{
    private const string Path = "/api/v1/dfs/contest_user_lineups";
    private readonly HttpTransport _http;

    public DfsContestUserLineupsResource(HttpTransport http) => _http = http;

    public async Task<Page<DfsContestUserLineup>> ListAsync(DfsContestUserLineupsListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<DfsContestUserLineup>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("contest_user_lineups", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<DfsContestUserLineup>()!);
            }
        }
        return new Page<DfsContestUserLineup>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<DfsContestUserLineup> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<DfsContestUserLineup>()!;
    }

    public async IAsyncEnumerable<DfsContestUserLineup> IterateAsync(DfsContestUserLineupsListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new DfsContestUserLineupsListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
