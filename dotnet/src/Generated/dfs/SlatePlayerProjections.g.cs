// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for dfs.slate_player_projections (public API).
// Source: schema/api/groups/public.yml + schema/model/dfs/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record DfsSlatePlayerProjection
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("slate_player_id")]
    public long SlatePlayerId { get; init; }

    [JsonPropertyName("slate_projection_source_id")]
    public long SlateProjectionSourceId { get; init; }

    [JsonPropertyName("ceiling")]
    public double? Ceiling { get; init; }

    [JsonPropertyName("floor")]
    public double? Floor { get; init; }

    [JsonPropertyName("projection")]
    public double Projection { get; init; }

    [JsonPropertyName("stats")]
    public JsonElement? Stats { get; init; }

    [JsonPropertyName("std_dev")]
    public double? StdDev { get; init; }
}

public sealed class DfsSlatePlayerProjectionsListParams
{
    public long? SlatePlayerId { get; set; }
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (SlatePlayerId is not null)
        {
            query.Add(new KeyValuePair<string, string>("slate_player_id", SlatePlayerId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class DfsSlatePlayerProjectionsResource
{
    private const string Path = "/api/v1/dfs/slate_player_projections";
    private readonly HttpTransport _http;

    public DfsSlatePlayerProjectionsResource(HttpTransport http) => _http = http;

    public async Task<Page<DfsSlatePlayerProjection>> ListAsync(DfsSlatePlayerProjectionsListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<DfsSlatePlayerProjection>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("slate_player_projections", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<DfsSlatePlayerProjection>()!);
            }
        }
        return new Page<DfsSlatePlayerProjection>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<DfsSlatePlayerProjection> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<DfsSlatePlayerProjection>()!;
    }

    public async IAsyncEnumerable<DfsSlatePlayerProjection> IterateAsync(DfsSlatePlayerProjectionsListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new DfsSlatePlayerProjectionsListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
