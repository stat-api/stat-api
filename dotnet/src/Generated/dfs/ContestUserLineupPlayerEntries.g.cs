// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for dfs.contest_user_lineup_player_entries (public API).
// Source: schema/api/groups/public.yml + schema/model/dfs/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record DfsContestUserLineupPlayerEntry
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("contest_user_lineup_id")]
    public long ContestUserLineupId { get; init; }

    [JsonPropertyName("slate_player_id")]
    public long SlatePlayerId { get; init; }

    [JsonPropertyName("display_order")]
    public int DisplayOrder { get; init; }

    [JsonPropertyName("points")]
    public double? Points { get; init; }

    [JsonPropertyName("roster_position")]
    public required string RosterPosition { get; init; }
}

public sealed class DfsContestUserLineupPlayerEntriesListParams
{
    public long? ContestUserLineupId { get; set; }
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (ContestUserLineupId is not null)
        {
            query.Add(new KeyValuePair<string, string>("contest_user_lineup_id", ContestUserLineupId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class DfsContestUserLineupPlayerEntriesResource
{
    private const string Path = "/api/v1/dfs/contest_user_lineup_player_entries";
    private readonly HttpTransport _http;

    public DfsContestUserLineupPlayerEntriesResource(HttpTransport http) => _http = http;

    public async Task<Page<DfsContestUserLineupPlayerEntry>> ListAsync(DfsContestUserLineupPlayerEntriesListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<DfsContestUserLineupPlayerEntry>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("contest_user_lineup_player_entries", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<DfsContestUserLineupPlayerEntry>()!);
            }
        }
        return new Page<DfsContestUserLineupPlayerEntry>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<DfsContestUserLineupPlayerEntry> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<DfsContestUserLineupPlayerEntry>()!;
    }

    public async IAsyncEnumerable<DfsContestUserLineupPlayerEntry> IterateAsync(DfsContestUserLineupPlayerEntriesListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new DfsContestUserLineupPlayerEntriesListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
