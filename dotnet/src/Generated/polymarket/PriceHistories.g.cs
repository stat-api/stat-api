// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for polymarket.price_histories (public API).
// Source: schema/api/groups/public.yml + schema/model/polymarket/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record PolymarketPriceHistory
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("market_id")]
    public long MarketId { get; init; }

    [JsonPropertyName("token_id")]
    public required string TokenId { get; init; }

    [JsonPropertyName("close_bps")]
    public int CloseBps { get; init; }

    [JsonPropertyName("high_bps")]
    public int HighBps { get; init; }

    [JsonPropertyName("interval_seconds")]
    public int IntervalSeconds { get; init; }

    [JsonPropertyName("low_bps")]
    public int LowBps { get; init; }

    [JsonPropertyName("open_bps")]
    public int OpenBps { get; init; }

    [JsonPropertyName("period_start")]
    public required string PeriodStart { get; init; }

    [JsonPropertyName("volume")]
    public long? Volume { get; init; }
}

public sealed class PolymarketPriceHistoriesListParams
{
    public string? TokenId { get; set; }
    public long? MarketId { get; set; }
    public int? IntervalSeconds { get; set; }
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (TokenId is not null)
        {
            query.Add(new KeyValuePair<string, string>("token_id", TokenId));
        }
        if (MarketId is not null)
        {
            query.Add(new KeyValuePair<string, string>("market_id", MarketId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (IntervalSeconds is not null)
        {
            query.Add(new KeyValuePair<string, string>("interval_seconds", IntervalSeconds.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class PolymarketPriceHistoriesResource
{
    private const string Path = "/api/v1/polymarket/price_histories";
    private readonly HttpTransport _http;

    public PolymarketPriceHistoriesResource(HttpTransport http) => _http = http;

    public async Task<Page<PolymarketPriceHistory>> ListAsync(PolymarketPriceHistoriesListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<PolymarketPriceHistory>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("price_histories", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<PolymarketPriceHistory>()!);
            }
        }
        return new Page<PolymarketPriceHistory>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<PolymarketPriceHistory> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<PolymarketPriceHistory>()!;
    }

    public async IAsyncEnumerable<PolymarketPriceHistory> IterateAsync(PolymarketPriceHistoriesListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new PolymarketPriceHistoriesListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
