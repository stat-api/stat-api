// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for polymarket.market_snapshots (public API).
// Source: schema/api/groups/public.yml + schema/model/polymarket/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record PolymarketMarketSnapshot
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("condition_id")]
    public required string ConditionId { get; init; }

    [JsonPropertyName("market_id")]
    public long MarketId { get; init; }

    [JsonPropertyName("best_ask_bps")]
    public int? BestAskBps { get; init; }

    [JsonPropertyName("best_bid_bps")]
    public int? BestBidBps { get; init; }

    [JsonPropertyName("captured_at")]
    public required string CapturedAt { get; init; }

    [JsonPropertyName("last_trade_price_bps")]
    public int? LastTradePriceBps { get; init; }

    [JsonPropertyName("liquidity")]
    public long? Liquidity { get; init; }

    [JsonPropertyName("volume")]
    public long? Volume { get; init; }
}

public sealed class PolymarketMarketSnapshotsListParams
{
    public long? MarketId { get; set; }
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (MarketId is not null)
        {
            query.Add(new KeyValuePair<string, string>("market_id", MarketId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class PolymarketMarketSnapshotsResource
{
    private const string Path = "/api/v1/polymarket/market_snapshots";
    private readonly HttpTransport _http;

    public PolymarketMarketSnapshotsResource(HttpTransport http) => _http = http;

    public async Task<Page<PolymarketMarketSnapshot>> ListAsync(PolymarketMarketSnapshotsListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<PolymarketMarketSnapshot>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("market_snapshots", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<PolymarketMarketSnapshot>()!);
            }
        }
        return new Page<PolymarketMarketSnapshot>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<PolymarketMarketSnapshot> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<PolymarketMarketSnapshot>()!;
    }

    public async IAsyncEnumerable<PolymarketMarketSnapshot> IterateAsync(PolymarketMarketSnapshotsListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new PolymarketMarketSnapshotsListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
