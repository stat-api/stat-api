// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for mlb.woba_weights (public API).
// Source: schema/api/groups/public.yml + schema/model/mlb/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record MlbWobaWeight
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("season_id")]
    public long SeasonId { get; init; }

    [JsonPropertyName("double_weight")]
    public double DoubleWeight { get; init; }

    [JsonPropertyName("hit_by_pitch_weight")]
    public double HitByPitchWeight { get; init; }

    [JsonPropertyName("home_run_weight")]
    public double HomeRunWeight { get; init; }

    [JsonPropertyName("league_obp")]
    public double LeagueObp { get; init; }

    [JsonPropertyName("league_woba")]
    public double LeagueWoba { get; init; }

    [JsonPropertyName("single_weight")]
    public double SingleWeight { get; init; }

    [JsonPropertyName("triple_weight")]
    public double TripleWeight { get; init; }

    [JsonPropertyName("updated_date")]
    public required string UpdatedDate { get; init; }

    [JsonPropertyName("walk_weight")]
    public double WalkWeight { get; init; }

    [JsonPropertyName("woba_scale")]
    public double WobaScale { get; init; }
}

public sealed class MlbWobaWeightsListParams
{
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class MlbWobaWeightsResource
{
    private const string Path = "/api/v1/mlb/woba_weights";
    private readonly HttpTransport _http;

    public MlbWobaWeightsResource(HttpTransport http) => _http = http;

    public async Task<Page<MlbWobaWeight>> ListAsync(MlbWobaWeightsListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<MlbWobaWeight>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("woba_weights", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<MlbWobaWeight>()!);
            }
        }
        return new Page<MlbWobaWeight>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<MlbWobaWeight> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<MlbWobaWeight>()!;
    }

    public async IAsyncEnumerable<MlbWobaWeight> IterateAsync(MlbWobaWeightsListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new MlbWobaWeightsListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
