// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for mlb.season_run_environments (public API).
// Source: schema/api/groups/public.yml + schema/model/mlb/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record MlbSeasonRunEnvironment
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("season_id")]
    public long SeasonId { get; init; }

    [JsonPropertyName("batting_average_on_balls_in_play")]
    public double BattingAverageOnBallsInPlay { get; init; }

    [JsonPropertyName("fip_constant")]
    public double FipConstant { get; init; }

    [JsonPropertyName("home_run_per_fly_ball")]
    public double HomeRunPerFlyBall { get; init; }

    [JsonPropertyName("runs_per_out")]
    public double RunsPerOut { get; init; }

    [JsonPropertyName("runs_per_plate_appearance")]
    public double RunsPerPlateAppearance { get; init; }

    [JsonPropertyName("strikeout_rate")]
    public double StrikeoutRate { get; init; }

    [JsonPropertyName("updated_date")]
    public required string UpdatedDate { get; init; }

    [JsonPropertyName("walk_rate")]
    public double WalkRate { get; init; }
}

public sealed class MlbSeasonRunEnvironmentsListParams
{
    public long? SeasonId { get; set; }
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (SeasonId is not null)
        {
            query.Add(new KeyValuePair<string, string>("season_id", SeasonId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class MlbSeasonRunEnvironmentsResource
{
    private const string Path = "/api/v1/mlb/season_run_environments";
    private readonly HttpTransport _http;

    public MlbSeasonRunEnvironmentsResource(HttpTransport http) => _http = http;

    public async Task<Page<MlbSeasonRunEnvironment>> ListAsync(MlbSeasonRunEnvironmentsListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<MlbSeasonRunEnvironment>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("season_run_environments", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<MlbSeasonRunEnvironment>()!);
            }
        }
        return new Page<MlbSeasonRunEnvironment>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<MlbSeasonRunEnvironment> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<MlbSeasonRunEnvironment>()!;
    }

    public async IAsyncEnumerable<MlbSeasonRunEnvironment> IterateAsync(MlbSeasonRunEnvironmentsListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new MlbSeasonRunEnvironmentsListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
