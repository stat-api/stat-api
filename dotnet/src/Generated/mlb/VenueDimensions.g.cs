// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for mlb.venue_dimensions (public API).
// Source: schema/api/groups/public.yml + schema/model/mlb/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record MlbVenueDimension
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("mlb_venue_id")]
    public int MlbVenueId { get; init; }

    [JsonPropertyName("venue_id")]
    public long VenueId { get; init; }

    [JsonPropertyName("altitude_ft")]
    public double? AltitudeFt { get; init; }

    [JsonPropertyName("backstop_distance")]
    public double? BackstopDistance { get; init; }

    [JsonPropertyName("center_field")]
    public double CenterField { get; init; }

    [JsonPropertyName("center_field_fence_height")]
    public double CenterFieldFenceHeight { get; init; }

    [JsonPropertyName("description")]
    public string? Description { get; init; }

    [JsonPropertyName("fair_territory_sq_ft")]
    public double? FairTerritorySqFt { get; init; }

    [JsonPropertyName("foul_territory_rating")]
    public double? FoulTerritoryRating { get; init; }

    [JsonPropertyName("foul_territory_sq_ft")]
    public double? FoulTerritorySqFt { get; init; }

    [JsonPropertyName("left_center_field")]
    public double? LeftCenterField { get; init; }

    [JsonPropertyName("left_field")]
    public double LeftField { get; init; }

    [JsonPropertyName("left_field_fence_height")]
    public double LeftFieldFenceHeight { get; init; }

    [JsonPropertyName("orientation")]
    public double Orientation { get; init; }

    [JsonPropertyName("right_center_field")]
    public double? RightCenterField { get; init; }

    [JsonPropertyName("right_field")]
    public double RightField { get; init; }

    [JsonPropertyName("right_field_fence_height")]
    public double RightFieldFenceHeight { get; init; }

    [JsonPropertyName("total_territory_sq_ft")]
    public double? TotalTerritorySqFt { get; init; }

    [JsonPropertyName("venue_name")]
    public string? VenueName { get; init; }

    [JsonPropertyName("year")]
    public int Year { get; init; }
}

public sealed class MlbVenueDimensionsListParams
{
    public long? VenueId { get; set; }
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (VenueId is not null)
        {
            query.Add(new KeyValuePair<string, string>("venue_id", VenueId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class MlbVenueDimensionsResource
{
    private const string Path = "/api/v1/mlb/venue_dimensions";
    private readonly HttpTransport _http;

    public MlbVenueDimensionsResource(HttpTransport http) => _http = http;

    public async Task<Page<MlbVenueDimension>> ListAsync(MlbVenueDimensionsListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<MlbVenueDimension>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("venue_dimensions", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<MlbVenueDimension>()!);
            }
        }
        return new Page<MlbVenueDimension>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<MlbVenueDimension> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<MlbVenueDimension>()!;
    }

    public async IAsyncEnumerable<MlbVenueDimension> IterateAsync(MlbVenueDimensionsListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new MlbVenueDimensionsListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
