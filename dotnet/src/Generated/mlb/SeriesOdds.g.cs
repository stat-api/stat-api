// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for mlb.series_odds (public API).
// Source: schema/api/groups/public.yml + schema/model/mlb/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record MlbSeriesOdd
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("away_team_id")]
    public long AwayTeamId { get; init; }

    [JsonPropertyName("home_team_id")]
    public long HomeTeamId { get; init; }

    [JsonPropertyName("operator_id")]
    public long OperatorId { get; init; }

    [JsonPropertyName("season_id")]
    public long SeasonId { get; init; }

    [JsonPropertyName("away_odds")]
    public int AwayOdds { get; init; }

    [JsonPropertyName("captured_at")]
    public required string CapturedAt { get; init; }

    [JsonPropertyName("games_played")]
    public int? GamesPlayed { get; init; }

    [JsonPropertyName("home_odds")]
    public int HomeOdds { get; init; }

    [JsonPropertyName("series_name")]
    public required string SeriesName { get; init; }

    [JsonPropertyName("series_score")]
    public string? SeriesScore { get; init; }
}

public sealed class MlbSeriesOddsListParams
{
    public long? SeasonId { get; set; }
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (SeasonId is not null)
        {
            query.Add(new KeyValuePair<string, string>("season_id", SeasonId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class MlbSeriesOddsResource
{
    private const string Path = "/api/v1/mlb/series_odds";
    private readonly HttpTransport _http;

    public MlbSeriesOddsResource(HttpTransport http) => _http = http;

    public async Task<Page<MlbSeriesOdd>> ListAsync(MlbSeriesOddsListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<MlbSeriesOdd>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("series_odds", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<MlbSeriesOdd>()!);
            }
        }
        return new Page<MlbSeriesOdd>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<MlbSeriesOdd> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<MlbSeriesOdd>()!;
    }

    public async IAsyncEnumerable<MlbSeriesOdd> IterateAsync(MlbSeriesOddsListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new MlbSeriesOddsListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
