// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for mlb.game_period_lines (public API).
// Source: schema/api/groups/public.yml + schema/model/mlb/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record MlbGamePeriodLine
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("game_id")]
    public long GameId { get; init; }

    [JsonPropertyName("operator_id")]
    public long OperatorId { get; init; }

    [JsonPropertyName("season_id")]
    public long SeasonId { get; init; }

    [JsonPropertyName("captured_at")]
    public required string CapturedAt { get; init; }

    [JsonPropertyName("implied_away_win_pct")]
    public double? ImpliedAwayWinPct { get; init; }

    [JsonPropertyName("implied_home_win_pct")]
    public double? ImpliedHomeWinPct { get; init; }

    [JsonPropertyName("is_opening")]
    public bool IsOpening { get; init; }

    [JsonPropertyName("moneyline_away")]
    public int? MoneylineAway { get; init; }

    [JsonPropertyName("moneyline_home")]
    public int? MoneylineHome { get; init; }

    [JsonPropertyName("over_odds")]
    public int? OverOdds { get; init; }

    [JsonPropertyName("period_code")]
    public required string PeriodCode { get; init; }

    [JsonPropertyName("spread")]
    public double? Spread { get; init; }

    [JsonPropertyName("spread_away_odds")]
    public int? SpreadAwayOdds { get; init; }

    [JsonPropertyName("spread_home_odds")]
    public int? SpreadHomeOdds { get; init; }

    [JsonPropertyName("total")]
    public double? Total { get; init; }

    [JsonPropertyName("under_odds")]
    public int? UnderOdds { get; init; }
}

public sealed class MlbGamePeriodLinesListParams
{
    public long? GameId { get; set; }
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (GameId is not null)
        {
            query.Add(new KeyValuePair<string, string>("game_id", GameId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class MlbGamePeriodLinesResource
{
    private const string Path = "/api/v1/mlb/game_period_lines";
    private readonly HttpTransport _http;

    public MlbGamePeriodLinesResource(HttpTransport http) => _http = http;

    public async Task<Page<MlbGamePeriodLine>> ListAsync(MlbGamePeriodLinesListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<MlbGamePeriodLine>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("game_period_lines", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<MlbGamePeriodLine>()!);
            }
        }
        return new Page<MlbGamePeriodLine>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<MlbGamePeriodLine> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<MlbGamePeriodLine>()!;
    }

    public async IAsyncEnumerable<MlbGamePeriodLine> IterateAsync(MlbGamePeriodLinesListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new MlbGamePeriodLinesListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
