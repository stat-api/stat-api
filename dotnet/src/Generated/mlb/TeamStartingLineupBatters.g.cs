// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for mlb.team_starting_lineup_batters (public API).
// Source: schema/api/groups/public.yml + schema/model/mlb/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record MlbTeamStartingLineupBatter
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("player_id")]
    public long PlayerId { get; init; }

    [JsonPropertyName("team_starting_lineup_id")]
    public long TeamStartingLineupId { get; init; }

    [JsonPropertyName("batting_order")]
    public required string BattingOrder { get; init; }

    [JsonPropertyName("position")]
    public required string Position { get; init; }
}

public sealed class MlbTeamStartingLineupBattersListParams
{
    public long? TeamStartingLineupId { get; set; }
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (TeamStartingLineupId is not null)
        {
            query.Add(new KeyValuePair<string, string>("team_starting_lineup_id", TeamStartingLineupId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class MlbTeamStartingLineupBattersResource
{
    private const string Path = "/api/v1/mlb/team_starting_lineup_batters";
    private readonly HttpTransport _http;

    public MlbTeamStartingLineupBattersResource(HttpTransport http) => _http = http;

    public async Task<Page<MlbTeamStartingLineupBatter>> ListAsync(MlbTeamStartingLineupBattersListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<MlbTeamStartingLineupBatter>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("team_starting_lineup_batters", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<MlbTeamStartingLineupBatter>()!);
            }
        }
        return new Page<MlbTeamStartingLineupBatter>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<MlbTeamStartingLineupBatter> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<MlbTeamStartingLineupBatter>()!;
    }

    public async IAsyncEnumerable<MlbTeamStartingLineupBatter> IterateAsync(MlbTeamStartingLineupBattersListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new MlbTeamStartingLineupBattersListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
