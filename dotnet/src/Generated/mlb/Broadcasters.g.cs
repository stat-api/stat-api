// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for mlb.broadcasters (public API).
// Source: schema/api/groups/public.yml + schema/model/mlb/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record MlbBroadcaster
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("broadcaster_id")]
    public int BroadcasterId { get; init; }

    [JsonPropertyName("broadcaster_team_id")]
    public int BroadcasterTeamId { get; init; }

    [JsonPropertyName("region_id")]
    public int RegionId { get; init; }

    [JsonPropertyName("broadcaster_abbreviation")]
    public required string BroadcasterAbbreviation { get; init; }

    [JsonPropertyName("broadcaster_description")]
    public string? BroadcasterDescription { get; init; }

    [JsonPropertyName("broadcaster_display")]
    public required string BroadcasterDisplay { get; init; }

    [JsonPropertyName("broadcaster_media")]
    public required string BroadcasterMedia { get; init; }

    [JsonPropertyName("broadcaster_ranking")]
    public int BroadcasterRanking { get; init; }

    [JsonPropertyName("broadcaster_scope")]
    public required string BroadcasterScope { get; init; }

    [JsonPropertyName("broadcaster_video_link")]
    public string? BroadcasterVideoLink { get; init; }

    [JsonPropertyName("tape_delay_comments")]
    public string? TapeDelayComments { get; init; }
}

public sealed class MlbBroadcastersListParams
{
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class MlbBroadcastersResource
{
    private const string Path = "/api/v1/mlb/broadcasters";
    private readonly HttpTransport _http;

    public MlbBroadcastersResource(HttpTransport http) => _http = http;

    public async Task<Page<MlbBroadcaster>> ListAsync(MlbBroadcastersListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<MlbBroadcaster>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("broadcasters", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<MlbBroadcaster>()!);
            }
        }
        return new Page<MlbBroadcaster>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<MlbBroadcaster> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<MlbBroadcaster>()!;
    }

    public async IAsyncEnumerable<MlbBroadcaster> IterateAsync(MlbBroadcastersListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new MlbBroadcastersListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
