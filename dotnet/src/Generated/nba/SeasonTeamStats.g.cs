// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for nba.season_team_stats (public API).
// Source: schema/api/groups/public.yml + schema/model/nba/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record NbaSeasonTeamStat
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("season_id")]
    public long SeasonId { get; init; }

    [JsonPropertyName("team_id")]
    public long TeamId { get; init; }

    [JsonPropertyName("games_played")]
    public int GamesPlayed { get; init; }

    [JsonPropertyName("losses")]
    public int Losses { get; init; }

    [JsonPropertyName("pts_allowed")]
    public int PtsAllowed { get; init; }

    [JsonPropertyName("pts_scored")]
    public int PtsScored { get; init; }

    [JsonPropertyName("ties")]
    public int Ties { get; init; }

    [JsonPropertyName("wins")]
    public int Wins { get; init; }
}

public sealed class NbaSeasonTeamStatsListParams
{
    public long? SeasonId { get; set; }
    public long? TeamId { get; set; }
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (SeasonId is not null)
        {
            query.Add(new KeyValuePair<string, string>("season_id", SeasonId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (TeamId is not null)
        {
            query.Add(new KeyValuePair<string, string>("team_id", TeamId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class NbaSeasonTeamStatsResource
{
    private const string Path = "/api/v1/nba/season_team_stats";
    private readonly HttpTransport _http;

    public NbaSeasonTeamStatsResource(HttpTransport http) => _http = http;

    public async Task<Page<NbaSeasonTeamStat>> ListAsync(NbaSeasonTeamStatsListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<NbaSeasonTeamStat>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("season_team_stats", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<NbaSeasonTeamStat>()!);
            }
        }
        return new Page<NbaSeasonTeamStat>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<NbaSeasonTeamStat> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<NbaSeasonTeamStat>()!;
    }

    public async IAsyncEnumerable<NbaSeasonTeamStat> IterateAsync(NbaSeasonTeamStatsListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new NbaSeasonTeamStatsListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
