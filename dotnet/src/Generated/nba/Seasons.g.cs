// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for nba.seasons (public API).
// Source: schema/api/groups/public.yml + schema/model/nba/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record NbaSeason
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("end_date")]
    public string? EndDate { get; init; }

    [JsonPropertyName("post_season_start_date")]
    public string? PostSeasonStartDate { get; init; }

    [JsonPropertyName("regular_season_start_date")]
    public string? RegularSeasonStartDate { get; init; }

    [JsonPropertyName("start_date")]
    public required string StartDate { get; init; }

    [JsonPropertyName("start_year")]
    public int StartYear { get; init; }
}

public sealed class NbaSeasonsListParams
{
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class NbaSeasonsResource
{
    private const string Path = "/api/v1/nba/seasons";
    private readonly HttpTransport _http;

    public NbaSeasonsResource(HttpTransport http) => _http = http;

    public async Task<Page<NbaSeason>> ListAsync(NbaSeasonsListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<NbaSeason>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("seasons", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<NbaSeason>()!);
            }
        }
        return new Page<NbaSeason>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<NbaSeason> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<NbaSeason>()!;
    }

    public async IAsyncEnumerable<NbaSeason> IterateAsync(NbaSeasonsListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new NbaSeasonsListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
