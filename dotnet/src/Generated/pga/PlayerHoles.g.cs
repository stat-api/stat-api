// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for pga.player_holes (public API).
// Source: schema/api/groups/public.yml + schema/model/golf/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record PgaPlayerHole
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("player_id")]
    public long PlayerId { get; init; }

    [JsonPropertyName("player_round_id")]
    public long PlayerRoundId { get; init; }

    [JsonPropertyName("tournament_id")]
    public long TournamentId { get; init; }

    [JsonPropertyName("birdie")]
    public bool? Birdie { get; init; }

    [JsonPropertyName("bogey")]
    public bool? Bogey { get; init; }

    [JsonPropertyName("double_bogey")]
    public bool? DoubleBogey { get; init; }

    [JsonPropertyName("double_eagle")]
    public bool? DoubleEagle { get; init; }

    [JsonPropertyName("eagle")]
    public bool? Eagle { get; init; }

    [JsonPropertyName("hole_in_one")]
    public bool? HoleInOne { get; init; }

    [JsonPropertyName("hole_number")]
    public int HoleNumber { get; init; }

    [JsonPropertyName("is_par")]
    public bool? IsPar { get; init; }

    [JsonPropertyName("par")]
    public int? Par { get; init; }

    [JsonPropertyName("round_number")]
    public int RoundNumber { get; init; }

    [JsonPropertyName("score")]
    public int? Score { get; init; }

    [JsonPropertyName("to_par")]
    public int? ToPar { get; init; }

    [JsonPropertyName("worse_than_double_bogey")]
    public bool? WorseThanDoubleBogey { get; init; }
}

public sealed class PgaPlayerHolesListParams
{
    public long? PlayerId { get; set; }
    public long? TournamentId { get; set; }
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (PlayerId is not null)
        {
            query.Add(new KeyValuePair<string, string>("player_id", PlayerId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (TournamentId is not null)
        {
            query.Add(new KeyValuePair<string, string>("tournament_id", TournamentId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class PgaPlayerHolesResource
{
    private const string Path = "/api/v1/pga/player_holes";
    private readonly HttpTransport _http;

    public PgaPlayerHolesResource(HttpTransport http) => _http = http;

    public async Task<Page<PgaPlayerHole>> ListAsync(PgaPlayerHolesListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<PgaPlayerHole>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("player_holes", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<PgaPlayerHole>()!);
            }
        }
        return new Page<PgaPlayerHole>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<PgaPlayerHole> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<PgaPlayerHole>()!;
    }

    public async IAsyncEnumerable<PgaPlayerHole> IterateAsync(PgaPlayerHolesListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new PgaPlayerHolesListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
