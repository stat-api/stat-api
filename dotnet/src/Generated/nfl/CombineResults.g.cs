// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for nfl.combine_results (public API).
// Source: schema/api/groups/public.yml + schema/model/nfl/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record NflCombineResult
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("cfb_id")]
    public string? CfbId { get; init; }

    [JsonPropertyName("pfr_id")]
    public string? PfrId { get; init; }

    [JsonPropertyName("player_id")]
    public long PlayerId { get; init; }

    [JsonPropertyName("team_id")]
    public long? TeamId { get; init; }

    [JsonPropertyName("bench")]
    public int? Bench { get; init; }

    [JsonPropertyName("broad_jump")]
    public int? BroadJump { get; init; }

    [JsonPropertyName("cone")]
    public double? Cone { get; init; }

    [JsonPropertyName("forty")]
    public double? Forty { get; init; }

    [JsonPropertyName("ht")]
    public string? Ht { get; init; }

    [JsonPropertyName("position")]
    public required string Position { get; init; }

    [JsonPropertyName("school")]
    public string? School { get; init; }

    [JsonPropertyName("season")]
    public int Season { get; init; }

    [JsonPropertyName("shuttle")]
    public double? Shuttle { get; init; }

    [JsonPropertyName("vertical")]
    public double? Vertical { get; init; }

    [JsonPropertyName("wt")]
    public int? Wt { get; init; }
}

public sealed class NflCombineResultsListParams
{
    public long? PlayerId { get; set; }
    public int? Season { get; set; }
    public string? Position { get; set; }
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (PlayerId is not null)
        {
            query.Add(new KeyValuePair<string, string>("player_id", PlayerId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (Season is not null)
        {
            query.Add(new KeyValuePair<string, string>("season", Season.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (Position is not null)
        {
            query.Add(new KeyValuePair<string, string>("position", Position));
        }
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class NflCombineResultsResource
{
    private const string Path = "/api/v1/nfl/combine_results";
    private readonly HttpTransport _http;

    public NflCombineResultsResource(HttpTransport http) => _http = http;

    public async Task<Page<NflCombineResult>> ListAsync(NflCombineResultsListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<NflCombineResult>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("combine_results", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<NflCombineResult>()!);
            }
        }
        return new Page<NflCombineResult>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<NflCombineResult> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<NflCombineResult>()!;
    }

    public async IAsyncEnumerable<NflCombineResult> IterateAsync(NflCombineResultsListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new NflCombineResultsListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
