// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for nfl.coaching_staffs (public API).
// Source: schema/api/groups/public.yml + schema/model/nfl/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record NflCoachingStaff
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("season_id")]
    public long SeasonId { get; init; }

    [JsonPropertyName("team_id")]
    public long TeamId { get; init; }

    [JsonPropertyName("experience")]
    public int? Experience { get; init; }

    [JsonPropertyName("first_name")]
    public required string FirstName { get; init; }

    [JsonPropertyName("full_name")]
    public required string FullName { get; init; }

    [JsonPropertyName("last_name")]
    public required string LastName { get; init; }

    [JsonPropertyName("position")]
    public required string Position { get; init; }
}

public sealed class NflCoachingStaffsListParams
{
    public long? TeamId { get; set; }
    public long? SeasonId { get; set; }
    public string? Position { get; set; }
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (TeamId is not null)
        {
            query.Add(new KeyValuePair<string, string>("team_id", TeamId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (SeasonId is not null)
        {
            query.Add(new KeyValuePair<string, string>("season_id", SeasonId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (Position is not null)
        {
            query.Add(new KeyValuePair<string, string>("position", Position));
        }
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class NflCoachingStaffsResource
{
    private const string Path = "/api/v1/nfl/coaching_staffs";
    private readonly HttpTransport _http;

    public NflCoachingStaffsResource(HttpTransport http) => _http = http;

    public async Task<Page<NflCoachingStaff>> ListAsync(NflCoachingStaffsListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<NflCoachingStaff>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("coaching_staffs", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<NflCoachingStaff>()!);
            }
        }
        return new Page<NflCoachingStaff>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<NflCoachingStaff> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<NflCoachingStaff>()!;
    }

    public async IAsyncEnumerable<NflCoachingStaff> IterateAsync(NflCoachingStaffsListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new NflCoachingStaffsListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
