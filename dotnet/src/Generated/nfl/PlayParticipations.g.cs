// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for nfl.play_participations (public API).
// Source: schema/api/groups/public.yml + schema/model/nfl/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record NflPlayParticipation
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("game_id")]
    public long GameId { get; init; }

    [JsonPropertyName("play_id")]
    public required string PlayId { get; init; }

    [JsonPropertyName("possession_team_id")]
    public long PossessionTeamId { get; init; }

    [JsonPropertyName("defenders_in_box")]
    public int? DefendersInBox { get; init; }

    [JsonPropertyName("defense_coverage_type")]
    public string? DefenseCoverageType { get; init; }

    [JsonPropertyName("defense_man_zone_type")]
    public string? DefenseManZoneType { get; init; }

    [JsonPropertyName("defense_personnel")]
    public string? DefensePersonnel { get; init; }

    [JsonPropertyName("defense_players_gsis_ids")]
    public string? DefensePlayersGsisIds { get; init; }

    [JsonPropertyName("n_defense")]
    public int? NDefense { get; init; }

    [JsonPropertyName("n_offense")]
    public int? NOffense { get; init; }

    [JsonPropertyName("ngs_air_yds")]
    public double? NgsAirYds { get; init; }

    [JsonPropertyName("number_of_pass_rushers")]
    public int? NumberOfPassRushers { get; init; }

    [JsonPropertyName("offense_formation")]
    public string? OffenseFormation { get; init; }

    [JsonPropertyName("offense_personnel")]
    public string? OffensePersonnel { get; init; }

    [JsonPropertyName("offense_players_gsis_ids")]
    public string? OffensePlayersGsisIds { get; init; }

    [JsonPropertyName("players_on_play_gsis_ids")]
    public string? PlayersOnPlayGsisIds { get; init; }

    [JsonPropertyName("route")]
    public string? Route { get; init; }

    [JsonPropertyName("time_to_throw")]
    public double? TimeToThrow { get; init; }

    [JsonPropertyName("was_pressure")]
    public bool? WasPressure { get; init; }
}

public sealed class NflPlayParticipationsListParams
{
    public string? PlayId { get; set; }
    public long? GameId { get; set; }
    public long? PossessionTeamId { get; set; }
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (PlayId is not null)
        {
            query.Add(new KeyValuePair<string, string>("play_id", PlayId));
        }
        if (GameId is not null)
        {
            query.Add(new KeyValuePair<string, string>("game_id", GameId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (PossessionTeamId is not null)
        {
            query.Add(new KeyValuePair<string, string>("possession_team_id", PossessionTeamId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class NflPlayParticipationsResource
{
    private const string Path = "/api/v1/nfl/play_participations";
    private readonly HttpTransport _http;

    public NflPlayParticipationsResource(HttpTransport http) => _http = http;

    public async Task<Page<NflPlayParticipation>> ListAsync(NflPlayParticipationsListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<NflPlayParticipation>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("play_participations", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<NflPlayParticipation>()!);
            }
        }
        return new Page<NflPlayParticipation>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<NflPlayParticipation> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<NflPlayParticipation>()!;
    }

    public async IAsyncEnumerable<NflPlayParticipation> IterateAsync(NflPlayParticipationsListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new NflPlayParticipationsListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
