// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for nfl.playoffs (public API).
// Source: schema/api/groups/public.yml + schema/model/nfl/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record NflPlayoff
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("away_team_id")]
    public long AwayTeamId { get; init; }

    [JsonPropertyName("home_team_id")]
    public long HomeTeamId { get; init; }

    [JsonPropertyName("season_id")]
    public long SeasonId { get; init; }

    [JsonPropertyName("winner_team_id")]
    public long? WinnerTeamId { get; init; }

    [JsonPropertyName("away_team_seed")]
    public int AwayTeamSeed { get; init; }

    [JsonPropertyName("conference")]
    public string? Conference { get; init; }

    [JsonPropertyName("game_number")]
    public int GameNumber { get; init; }

    [JsonPropertyName("home_team_seed")]
    public int HomeTeamSeed { get; init; }

    [JsonPropertyName("playoff_round")]
    public required string PlayoffRound { get; init; }

    [JsonPropertyName("status")]
    public required string Status { get; init; }
}

public sealed class NflPlayoffsListParams
{
    public long? SeasonId { get; set; }
    public long? HomeTeamId { get; set; }
    public long? AwayTeamId { get; set; }
    public string? Status { get; set; }
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (SeasonId is not null)
        {
            query.Add(new KeyValuePair<string, string>("season_id", SeasonId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (HomeTeamId is not null)
        {
            query.Add(new KeyValuePair<string, string>("home_team_id", HomeTeamId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (AwayTeamId is not null)
        {
            query.Add(new KeyValuePair<string, string>("away_team_id", AwayTeamId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (Status is not null)
        {
            query.Add(new KeyValuePair<string, string>("status", Status));
        }
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class NflPlayoffsResource
{
    private const string Path = "/api/v1/nfl/playoffs";
    private readonly HttpTransport _http;

    public NflPlayoffsResource(HttpTransport http) => _http = http;

    public async Task<Page<NflPlayoff>> ListAsync(NflPlayoffsListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<NflPlayoff>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("playoffs", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<NflPlayoff>()!);
            }
        }
        return new Page<NflPlayoff>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<NflPlayoff> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<NflPlayoff>()!;
    }

    public async IAsyncEnumerable<NflPlayoff> IterateAsync(NflPlayoffsListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new NflPlayoffsListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
