// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for nfl.depth_chart_entries (public API).
// Source: schema/api/groups/public.yml + schema/model/nfl/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record NflDepthChartEntry
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("player_id")]
    public long PlayerId { get; init; }

    [JsonPropertyName("snapshot_id")]
    public long SnapshotId { get; init; }

    [JsonPropertyName("depth_order")]
    public int DepthOrder { get; init; }

    [JsonPropertyName("depth_position")]
    public required string DepthPosition { get; init; }

    [JsonPropertyName("formation")]
    public string? Formation { get; init; }

    [JsonPropertyName("jersey_number")]
    public int? JerseyNumber { get; init; }

    [JsonPropertyName("position")]
    public required string Position { get; init; }
}

public sealed class NflDepthChartEntriesListParams
{
    public long? SnapshotId { get; set; }
    public long? PlayerId { get; set; }
    public string? Position { get; set; }
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (SnapshotId is not null)
        {
            query.Add(new KeyValuePair<string, string>("snapshot_id", SnapshotId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (PlayerId is not null)
        {
            query.Add(new KeyValuePair<string, string>("player_id", PlayerId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (Position is not null)
        {
            query.Add(new KeyValuePair<string, string>("position", Position));
        }
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class NflDepthChartEntriesResource
{
    private const string Path = "/api/v1/nfl/depth_chart_entries";
    private readonly HttpTransport _http;

    public NflDepthChartEntriesResource(HttpTransport http) => _http = http;

    public async Task<Page<NflDepthChartEntry>> ListAsync(NflDepthChartEntriesListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<NflDepthChartEntry>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("depth_chart_entries", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<NflDepthChartEntry>()!);
            }
        }
        return new Page<NflDepthChartEntry>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<NflDepthChartEntry> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<NflDepthChartEntry>()!;
    }

    public async IAsyncEnumerable<NflDepthChartEntry> IterateAsync(NflDepthChartEntriesListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new NflDepthChartEntriesListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
