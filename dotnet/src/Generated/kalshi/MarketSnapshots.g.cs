// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for kalshi.market_snapshots (public API).
// Source: schema/api/groups/public.yml + schema/model/kalshi/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record KalshiMarketSnapshot
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("market_id")]
    public long MarketId { get; init; }

    [JsonPropertyName("captured_at")]
    public required string CapturedAt { get; init; }

    [JsonPropertyName("is_opening")]
    public bool IsOpening { get; init; }

    [JsonPropertyName("last_price")]
    public int? LastPrice { get; init; }

    [JsonPropertyName("liquidity")]
    public int Liquidity { get; init; }

    [JsonPropertyName("open_interest")]
    public int OpenInterest { get; init; }

    [JsonPropertyName("ticker")]
    public required string Ticker { get; init; }

    [JsonPropertyName("volume")]
    public int Volume { get; init; }

    [JsonPropertyName("yes_ask")]
    public int? YesAsk { get; init; }

    [JsonPropertyName("yes_bid")]
    public int? YesBid { get; init; }
}

public sealed class KalshiMarketSnapshotsListParams
{
    public long? MarketId { get; set; }
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (MarketId is not null)
        {
            query.Add(new KeyValuePair<string, string>("market_id", MarketId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class KalshiMarketSnapshotsResource
{
    private const string Path = "/api/v1/kalshi/market_snapshots";
    private readonly HttpTransport _http;

    public KalshiMarketSnapshotsResource(HttpTransport http) => _http = http;

    public async Task<Page<KalshiMarketSnapshot>> ListAsync(KalshiMarketSnapshotsListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<KalshiMarketSnapshot>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("market_snapshots", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<KalshiMarketSnapshot>()!);
            }
        }
        return new Page<KalshiMarketSnapshot>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<KalshiMarketSnapshot> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<KalshiMarketSnapshot>()!;
    }

    public async IAsyncEnumerable<KalshiMarketSnapshot> IterateAsync(KalshiMarketSnapshotsListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new KalshiMarketSnapshotsListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
