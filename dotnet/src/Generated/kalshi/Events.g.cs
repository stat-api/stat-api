// <auto-generated/>
// DO NOT EDIT — generated by generate-csharp-sdk-public.ts
//
// SDK resource for kalshi.events (public API).
// Source: schema/api/groups/public.yml + schema/model/kalshi/*.yml
#nullable enable
using System.Globalization;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace StatApi;

public sealed record KalshiEvent
{
    [JsonPropertyName("id")]
    public long Id { get; init; }

    [JsonPropertyName("competition_id")]
    public long? CompetitionId { get; init; }

    [JsonPropertyName("league_game_id")]
    public string? LeagueGameId { get; init; }

    [JsonPropertyName("series_id")]
    public long SeriesId { get; init; }

    [JsonPropertyName("away_team")]
    public string? AwayTeam { get; init; }

    [JsonPropertyName("category")]
    public required string Category { get; init; }

    [JsonPropertyName("event_ticker")]
    public required string EventTicker { get; init; }

    [JsonPropertyName("game_date")]
    public string? GameDate { get; init; }

    [JsonPropertyName("home_team")]
    public string? HomeTeam { get; init; }

    [JsonPropertyName("league_code")]
    public string? LeagueCode { get; init; }

    [JsonPropertyName("market_count")]
    public int? MarketCount { get; init; }

    [JsonPropertyName("settled_at")]
    public string? SettledAt { get; init; }

    [JsonPropertyName("status")]
    public required string Status { get; init; }

    [JsonPropertyName("title")]
    public required string Title { get; init; }
}

public sealed class KalshiEventsListParams
{
    public long? SeriesId { get; set; }
    public long? CompetitionId { get; set; }
    public string? LeagueCode { get; set; }
    public long? Limit { get; set; }
    public long? FromId { get; set; }

    internal IEnumerable<KeyValuePair<string, string>> ToQuery()
    {
        var query = new List<KeyValuePair<string, string>>();
        if (SeriesId is not null)
        {
            query.Add(new KeyValuePair<string, string>("series_id", SeriesId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (CompetitionId is not null)
        {
            query.Add(new KeyValuePair<string, string>("competition_id", CompetitionId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (LeagueCode is not null)
        {
            query.Add(new KeyValuePair<string, string>("league_code", LeagueCode));
        }
        if (Limit is not null)
        {
            query.Add(new KeyValuePair<string, string>("limit", Limit.Value.ToString(CultureInfo.InvariantCulture)));
        }
        if (FromId is not null)
        {
            query.Add(new KeyValuePair<string, string>("from_id", FromId.Value.ToString(CultureInfo.InvariantCulture)));
        }
        return query;
    }
}

public sealed class KalshiEventsResource
{
    private const string Path = "/api/v1/kalshi/events";
    private readonly HttpTransport _http;

    public KalshiEventsResource(HttpTransport http) => _http = http;

    public async Task<Page<KalshiEvent>> ListAsync(KalshiEventsListParams? parameters = null, CancellationToken cancellationToken = default)
    {
        var (root, quota) = await _http.GetAsync(Path, parameters?.ToQuery(), cancellationToken).ConfigureAwait(false);
        var rows = new List<KalshiEvent>();
        if (root.ValueKind == JsonValueKind.Object
            && root.TryGetProperty("events", out var array)
            && array.ValueKind == JsonValueKind.Array)
        {
            foreach (var element in array.EnumerateArray())
            {
                rows.Add(element.Deserialize<KalshiEvent>()!);
            }
        }
        return new Page<KalshiEvent>(rows, HttpTransport.ReadLong(root, "limit"), HttpTransport.ReadLong(root, "next_from_id"), quota);
    }

    public async Task<KalshiEvent> GetAsync(long id, CancellationToken cancellationToken = default)
    {
        var (root, _) = await _http.GetAsync($"{Path}/{id}", null, cancellationToken).ConfigureAwait(false);
        return root.Deserialize<KalshiEvent>()!;
    }

    public async IAsyncEnumerable<KalshiEvent> IterateAsync(KalshiEventsListParams? parameters = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var query = parameters ?? new KalshiEventsListParams();
        // Always walk with FromId (starting at 0): from_id forces the server's
        // ascending keyset order. Without it, DESC-default tables re-yield
        // page 1 and under-walk. Iteration is therefore always oldest-first.
        long? fromId = query.FromId ?? 0;
        while (true)
        {
            query.FromId = fromId;
            var page = await ListAsync(query, cancellationToken).ConfigureAwait(false);
            foreach (var row in page.Rows)
            {
                yield return row;
            }
            if (page.NextFromId is null)
            {
                yield break;
            }
            fromId = page.NextFromId;
        }
    }
}
