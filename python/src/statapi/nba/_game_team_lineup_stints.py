# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nba.game_team_lineup_stints (public API).
# Source: schema/api/groups/public.yml + schema/model/nba/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import Any, TypedDict

from ..page import Page
from ..transport import HttpTransport

NBAGameTeamLineupStint = TypedDict(
    "NBAGameTeamLineupStint",
    {
        "id": int,
        "game_id": int,
        "player1_id": int,
        "player2_id": int,
        "player3_id": int,
        "player4_id": int,
        "player5_id": int,
        "team_id": int,
        "assist_pct": float,
        "defensive_rating": float,
        "defensive_rebound_pct": float,
        "end_clock": int,
        "end_event_seq": int,
        "net_rating": float,
        "offensive_rating": float,
        "offensive_rebound_pct": float,
        "pace": float,
        "period": int,
        "player_stats": dict[str, Any],
        "possessions": int,
        "pts_allowed": int,
        "pts_scored": int,
        "start_clock": int,
        "start_event_seq": int,
        "turnover_pct": float,
    },
)


class NBAGameTeamLineupStintsResource:
    _PATH = "/api/v1/nba/game_team_lineup_stints"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, game_id: int | None = None, team_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NBAGameTeamLineupStint]:
        body, quota = self._transport.request(self._PATH, {
            "game_id": game_id,
            "team_id": team_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NBAGameTeamLineupStint] = body["game_team_lineup_stints"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NBAGameTeamLineupStint:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NBAGameTeamLineupStint = body
        return row

    def iter(self, *, game_id: int | None = None, team_id: int | None = None, limit: int | None = None) -> Iterator[NBAGameTeamLineupStint]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(game_id=game_id, team_id=team_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
