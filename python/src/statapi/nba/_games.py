# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nba.games (public API).
# Source: schema/api/groups/public.yml + schema/model/nba/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NBAGame = TypedDict(
    "NBAGame",
    {
        "id": int,
        "away_team_id": int,
        "home_team_id": int,
        "league_game_id": str | None,
        "season_id": int,
        "stadium_id": int | None,
        "attendance": int | None,
        "away_rotation_number": int | None,
        "away_team": str,
        "away_team_score": int,
        "channel": str | None,
        "day": int,
        "end_time": str | None,
        "game_label": str | None,
        "game_sub_label": str | None,
        "game_time": str,
        "home_rotation_number": int | None,
        "home_team": str,
        "home_team_score": int,
        "if_necessary": bool | None,
        "neutral_venue": bool,
        "postponed_status": str | None,
        "season_type": str,
        "series_game_number": str | None,
        "series_name": str | None,
        "series_text": str | None,
        "start_time": str,
        "status": str,
    },
)


class NBAGamesResource:
    _PATH = "/api/v1/nba/games"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, season_id: int | None = None, home_team_id: int | None = None, away_team_id: int | None = None, status: str | None = None, day: int | None = None, season_type: str | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NBAGame]:
        body, quota = self._transport.request(self._PATH, {
            "season_id": season_id,
            "home_team_id": home_team_id,
            "away_team_id": away_team_id,
            "status": status,
            "day": day,
            "season_type": season_type,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NBAGame] = body["games"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NBAGame:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NBAGame = body
        return row

    def iter(self, *, season_id: int | None = None, home_team_id: int | None = None, away_team_id: int | None = None, status: str | None = None, day: int | None = None, season_type: str | None = None, limit: int | None = None) -> Iterator[NBAGame]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(season_id=season_id, home_team_id=home_team_id, away_team_id=away_team_id, status=status, day=day, season_type=season_type, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
