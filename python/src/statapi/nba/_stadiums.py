# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nba.stadiums (public API).
# Source: schema/api/groups/public.yml + schema/model/nba/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NBAStadium = TypedDict(
    "NBAStadium",
    {
        "id": int,
        "league_stadium_id": int | None,
        "address": str | None,
        "capacity": int | None,
        "city": str,
        "closed_date": str | None,
        "country": str | None,
        "description": str | None,
        "elevation": float | None,
        "image_url": str | None,
        "latitude": float | None,
        "longitude": float | None,
        "name": str,
        "opened_date": str | None,
        "roof_type": str | None,
        "state": str | None,
        "surface": str | None,
        "team_name": str | None,
        "timezone": str | None,
    },
)


class NBAStadiumsResource:
    _PATH = "/api/v1/nba/stadiums"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, limit: int | None = None, from_id: int | None = None) -> Page[NBAStadium]:
        body, quota = self._transport.request(self._PATH, {
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NBAStadium] = body["stadiums"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NBAStadium:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NBAStadium = body
        return row

    def iter(self, *, limit: int | None = None) -> Iterator[NBAStadium]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
