# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nba.team_standings (public API).
# Source: schema/api/groups/public.yml + schema/model/nba/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NBATeamStanding = TypedDict(
    "NBATeamStanding",
    {
        "id": int,
        "season_id": int,
        "team_id": int,
        "away_losses": int,
        "away_wins": int,
        "conference_gb": float,
        "conference_rank": int,
        "day": int,
        "division_rank": int,
        "games_played": int,
        "games_remaining": int,
        "home_losses": int,
        "home_wins": int,
        "l10_losses": int,
        "l10_wins": int,
        "losses": int,
        "playin_gb": float,
        "playoff_gb": float,
        "streak": int,
        "win_pct": float,
        "wins": int,
    },
)


class NBATeamStandingsResource:
    _PATH = "/api/v1/nba/team_standings"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, season_id: int | None = None, team_id: int | None = None, day: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NBATeamStanding]:
        body, quota = self._transport.request(self._PATH, {
            "season_id": season_id,
            "team_id": team_id,
            "day": day,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NBATeamStanding] = body["team_standings"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NBATeamStanding:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NBATeamStanding = body
        return row

    def iter(self, *, season_id: int | None = None, team_id: int | None = None, day: int | None = None, limit: int | None = None) -> Iterator[NBATeamStanding]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(season_id=season_id, team_id=team_id, day=day, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
