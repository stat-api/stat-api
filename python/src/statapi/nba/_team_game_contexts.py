# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nba.team_game_contexts (public API).
# Source: schema/api/groups/public.yml + schema/model/nba/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NBATeamGameContext = TypedDict(
    "NBATeamGameContext",
    {
        "id": int,
        "game_id": int,
        "opponent_team_id": int,
        "season_id": int,
        "team_id": int,
        "conference_rank": int,
        "game_type": str,
        "games_remaining": int,
        "intensity": int,
        "is_back_to_back": bool,
        "is_clinch": bool,
        "is_elimination": bool,
        "ist_group_rank": int | None,
        "ist_group_record": str | None,
        "motivation": str,
        "rest_days": int,
        "season_pct": float,
        "season_phase": str,
        "team_series_wins": int,
        "win_pct": float,
    },
)


class NBATeamGameContextsResource:
    _PATH = "/api/v1/nba/team_game_contexts"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, game_id: int | None = None, team_id: int | None = None, season_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NBATeamGameContext]:
        body, quota = self._transport.request(self._PATH, {
            "game_id": game_id,
            "team_id": team_id,
            "season_id": season_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NBATeamGameContext] = body["team_game_contexts"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NBATeamGameContext:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NBATeamGameContext = body
        return row

    def iter(self, *, game_id: int | None = None, team_id: int | None = None, season_id: int | None = None, limit: int | None = None) -> Iterator[NBATeamGameContext]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(game_id=game_id, team_id=team_id, season_id=season_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
