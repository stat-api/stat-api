# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nba.game_team_rosters (public API).
# Source: schema/api/groups/public.yml + schema/model/nba/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NBAGameTeamRoster = TypedDict(
    "NBAGameTeamRoster",
    {
        "id": int,
        "game_id": int,
        "player_id": int,
        "team_id": int,
        "comment": str | None,
        "confirmed": bool | None,
        "day": int,
        "injury_status": str | None,
        "position": str | None,
        "pre_game_confirmed": bool | None,
        "pre_game_injury_status": str | None,
        "pre_game_starter": bool | None,
        "pre_game_status": int | None,
        "starter": bool | None,
        "status": int | None,
    },
)


class NBAGameTeamRostersResource:
    _PATH = "/api/v1/nba/game_team_rosters"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, game_id: int | None = None, team_id: int | None = None, player_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NBAGameTeamRoster]:
        body, quota = self._transport.request(self._PATH, {
            "game_id": game_id,
            "team_id": team_id,
            "player_id": player_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NBAGameTeamRoster] = body["game_team_rosters"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NBAGameTeamRoster:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NBAGameTeamRoster = body
        return row

    def iter(self, *, game_id: int | None = None, team_id: int | None = None, player_id: int | None = None, limit: int | None = None) -> Iterator[NBAGameTeamRoster]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(game_id=game_id, team_id=team_id, player_id=player_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
