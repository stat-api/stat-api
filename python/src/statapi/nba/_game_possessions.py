# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nba.game_possessions (public API).
# Source: schema/api/groups/public.yml + schema/model/nba/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NBAGamePossession = TypedDict(
    "NBAGamePossession",
    {
        "id": int,
        "game_id": int,
        "team_id": int | None,
        "ast": int,
        "away_score": int,
        "blk": int,
        "cum_away_poss": int,
        "cum_home_poss": int,
        "dreb": int,
        "end_clock": int | None,
        "end_nba_event_num": int,
        "fg3a": int,
        "fg3m": int,
        "fga": int,
        "fgm": int,
        "fta": int,
        "ftm": int,
        "game_seconds_elapsed": int,
        "home_score": int,
        "oreb": int,
        "period": int,
        "pf": int,
        "pts": int,
        "start_clock": int,
        "start_nba_event_num": int,
        "stl": int,
        "time_actual_end": str | None,
        "time_actual_start": str | None,
        "tov": int,
    },
)


class NBAGamePossessionsResource:
    _PATH = "/api/v1/nba/game_possessions"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, game_id: int | None = None, team_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NBAGamePossession]:
        body, quota = self._transport.request(self._PATH, {
            "game_id": game_id,
            "team_id": team_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NBAGamePossession] = body["game_possessions"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NBAGamePossession:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NBAGamePossession = body
        return row

    def iter(self, *, game_id: int | None = None, team_id: int | None = None, limit: int | None = None) -> Iterator[NBAGamePossession]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(game_id=game_id, team_id=team_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
