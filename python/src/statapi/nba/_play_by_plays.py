# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nba.play_by_plays (public API).
# Source: schema/api/groups/public.yml + schema/model/nba/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NBAPlayByPlay = TypedDict(
    "NBAPlayByPlay",
    {
        "id": int,
        "game_id": int,
        "game_possession_id": int,
        "play_id": str,
        "player_id": int | None,
        "player2_id": int | None,
        "player2_team_id": int | None,
        "player3_id": int | None,
        "player3_team_id": int | None,
        "team_id": int | None,
        "end_clock": int | None,
        "event_seq": int,
        "event_sub_type": int,
        "event_type": int,
        "home_description": str,
        "nba_event_num": int,
        "neutral_description": str,
        "possession_seq": int,
        "start_clock": int,
        "time_actual": str | None,
        "visitor_description": str,
    },
)


class NBAPlayByPlaysResource:
    _PATH = "/api/v1/nba/play_by_plays"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, game_id: int | None = None, player_id: int | None = None, team_id: int | None = None, event_type: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NBAPlayByPlay]:
        body, quota = self._transport.request(self._PATH, {
            "game_id": game_id,
            "player_id": player_id,
            "team_id": team_id,
            "event_type": event_type,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NBAPlayByPlay] = body["play_by_plays"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NBAPlayByPlay:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NBAPlayByPlay = body
        return row

    def iter(self, *, game_id: int | None = None, player_id: int | None = None, team_id: int | None = None, event_type: int | None = None, limit: int | None = None) -> Iterator[NBAPlayByPlay]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(game_id=game_id, player_id=player_id, team_id=team_id, event_type=event_type, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
