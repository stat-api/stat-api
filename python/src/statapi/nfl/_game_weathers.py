# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nfl.game_weathers (public API).
# Source: schema/api/groups/public.yml + schema/model/nfl/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NFLGameWeather = TypedDict(
    "NFLGameWeather",
    {
        "id": int,
        "game_id": int,
        "stadium_id": int,
        "dew_point": float | None,
        "feels_like_temperature": int | None,
        "field_condition": str | None,
        "humidity": float | None,
        "is_dome": bool,
        "minutes_from_start": int,
        "precipitation": float | None,
        "precipitation_probability": float | None,
        "rain_accum": float | None,
        "recorded_at": str,
        "roof_status": str | None,
        "snow_accum": float | None,
        "snow_depth": float | None,
        "snowfall": float | None,
        "temperature": int | None,
        "weather_condition": str | None,
        "weather_description": str | None,
        "wind_direction": float | None,
        "wind_gust": float | None,
        "wind_speed": float | None,
    },
)


class NFLGameWeathersResource:
    _PATH = "/api/v1/nfl/game_weathers"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, game_id: int | None = None, stadium_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NFLGameWeather]:
        body, quota = self._transport.request(self._PATH, {
            "game_id": game_id,
            "stadium_id": stadium_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NFLGameWeather] = body["game_weathers"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NFLGameWeather:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NFLGameWeather = body
        return row

    def iter(self, *, game_id: int | None = None, stadium_id: int | None = None, limit: int | None = None) -> Iterator[NFLGameWeather]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(game_id=game_id, stadium_id=stadium_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
