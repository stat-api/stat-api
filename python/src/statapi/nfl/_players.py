# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nfl.players (public API).
# Source: schema/api/groups/public.yml + schema/model/nfl/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NFLPlayer = TypedDict(
    "NFLPlayer",
    {
        "id": int,
        "gsis_id": str,
        "league_player_id": str,
        "team_id": int | None,
        "birth_city": str | None,
        "birth_country": str | None,
        "birth_date": str | None,
        "birth_state": str | None,
        "college": str | None,
        "draft_number": str | None,
        "draft_round": str | None,
        "draft_year": str | None,
        "experience": int | None,
        "first_name": str,
        "full_name": str,
        "full_position_list": str | None,
        "height": int | None,
        "is_team": bool,
        "jersey": str | None,
        "last_name": str,
        "nfl_slug": str | None,
        "overall_pick": int | None,
        "position_group": str | None,
        "primary_position": str,
        "profile_pic_url": str | None,
        "roster_status": str | None,
        "weight": int | None,
    },
)


class NFLPlayersResource:
    _PATH = "/api/v1/nfl/players"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, team_id: int | None = None, roster_status: str | None = None, gsis_id: str | None = None, primary_position: str | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NFLPlayer]:
        body, quota = self._transport.request(self._PATH, {
            "team_id": team_id,
            "roster_status": roster_status,
            "gsis_id": gsis_id,
            "primary_position": primary_position,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NFLPlayer] = body["players"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NFLPlayer:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NFLPlayer = body
        return row

    def iter(self, *, team_id: int | None = None, roster_status: str | None = None, gsis_id: str | None = None, primary_position: str | None = None, limit: int | None = None) -> Iterator[NFLPlayer]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(team_id=team_id, roster_status=roster_status, gsis_id=gsis_id, primary_position=primary_position, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
