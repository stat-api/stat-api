# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nfl.game_drives (public API).
# Source: schema/api/groups/public.yml + schema/model/nfl/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NFLGameDrive = TypedDict(
    "NFLGameDrive",
    {
        "id": int,
        "first_play_id": int,
        "game_id": int,
        "last_play_id": int,
        "possession_team_id": int,
        "drive_number": int,
        "end_transition": str | None,
        "end_yard_line": str | None,
        "ended_with_score": bool,
        "first_downs": int | None,
        "fixed_drive": int,
        "game_clock_end": str | None,
        "game_clock_start": str | None,
        "inside_20": bool,
        "play_count": int | None,
        "quarter_end": int | None,
        "quarter_start": int | None,
        "result": str | None,
        "start_transition": str | None,
        "start_yard_line": str | None,
        "time_of_possession": str | None,
        "yds_penalized": int | None,
    },
)


class NFLGameDrivesResource:
    _PATH = "/api/v1/nfl/game_drives"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, game_id: int | None = None, possession_team_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NFLGameDrive]:
        body, quota = self._transport.request(self._PATH, {
            "game_id": game_id,
            "possession_team_id": possession_team_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NFLGameDrive] = body["game_drives"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NFLGameDrive:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NFLGameDrive = body
        return row

    def iter(self, *, game_id: int | None = None, possession_team_id: int | None = None, limit: int | None = None) -> Iterator[NFLGameDrive]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(game_id=game_id, possession_team_id=possession_team_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
