# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nfl.depth_charts (public API).
# Source: schema/api/groups/public.yml + schema/model/nfl/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NFLDepthChart = TypedDict(
    "NFLDepthChart",
    {
        "id": int,
        "elias_id": str | None,
        "game_id": int,
        "player_id": int,
        "season_id": int,
        "team_id": int,
        "depth_order": int | None,
        "depth_position": str,
        "depth_team": str | None,
        "formation": str | None,
        "game_type": str,
        "jersey_number": int | None,
        "position": str,
        "status": str | None,
        "week": int,
    },
)


class NFLDepthChartsResource:
    _PATH = "/api/v1/nfl/depth_charts"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, game_id: int | None = None, team_id: int | None = None, season_id: int | None = None, week: int | None = None, player_id: int | None = None, position: str | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NFLDepthChart]:
        body, quota = self._transport.request(self._PATH, {
            "game_id": game_id,
            "team_id": team_id,
            "season_id": season_id,
            "week": week,
            "player_id": player_id,
            "position": position,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NFLDepthChart] = body["depth_charts"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NFLDepthChart:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NFLDepthChart = body
        return row

    def iter(self, *, game_id: int | None = None, team_id: int | None = None, season_id: int | None = None, week: int | None = None, player_id: int | None = None, position: str | None = None, limit: int | None = None) -> Iterator[NFLDepthChart]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(game_id=game_id, team_id=team_id, season_id=season_id, week=week, player_id=player_id, position=position, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
