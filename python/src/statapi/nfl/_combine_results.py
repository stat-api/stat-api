# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nfl.combine_results (public API).
# Source: schema/api/groups/public.yml + schema/model/nfl/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NFLCombineResult = TypedDict(
    "NFLCombineResult",
    {
        "id": int,
        "cfb_id": str | None,
        "pfr_id": str | None,
        "player_id": int,
        "team_id": int | None,
        "bench": int | None,
        "broad_jump": int | None,
        "cone": float | None,
        "forty": float | None,
        "ht": str | None,
        "position": str,
        "school": str | None,
        "season": int,
        "shuttle": float | None,
        "vertical": float | None,
        "wt": int | None,
    },
)


class NFLCombineResultsResource:
    _PATH = "/api/v1/nfl/combine_results"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, player_id: int | None = None, season: int | None = None, position: str | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NFLCombineResult]:
        body, quota = self._transport.request(self._PATH, {
            "player_id": player_id,
            "season": season,
            "position": position,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NFLCombineResult] = body["combine_results"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NFLCombineResult:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NFLCombineResult = body
        return row

    def iter(self, *, player_id: int | None = None, season: int | None = None, position: str | None = None, limit: int | None = None) -> Iterator[NFLCombineResult]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(player_id=player_id, season=season, position=position, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
