# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nfl.games (public API).
# Source: schema/api/groups/public.yml + schema/model/nfl/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NFLGame = TypedDict(
    "NFLGame",
    {
        "id": int,
        "away_team_id": int,
        "home_team_id": int,
        "league_game_id": str,
        "season_id": int,
        "stadium_id": int,
        "attendance": int | None,
        "away_rotation_number": int | None,
        "away_team": str,
        "away_team_score": int,
        "channel": str | None,
        "day": int,
        "end_time": str | None,
        "game_label": str | None,
        "game_sub_label": str | None,
        "game_time": str,
        "home_rotation_number": int | None,
        "home_team": str,
        "home_team_score": int,
        "neutral_venue": bool,
        "overtime": bool,
        "overtime_periods": int | None,
        "playoff_round": str | None,
        "playoff_seed_away": int | None,
        "playoff_seed_home": int | None,
        "postponed_status": str | None,
        "season_type": str,
        "start_time": str,
        "status": str,
        "type": str,
        "week": int,
    },
)


class NFLGamesResource:
    _PATH = "/api/v1/nfl/games"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, season_id: int | None = None, week: int | None = None, season_type: str | None = None, home_team_id: int | None = None, away_team_id: int | None = None, status: str | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NFLGame]:
        body, quota = self._transport.request(self._PATH, {
            "season_id": season_id,
            "week": week,
            "season_type": season_type,
            "home_team_id": home_team_id,
            "away_team_id": away_team_id,
            "status": status,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NFLGame] = body["games"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NFLGame:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NFLGame = body
        return row

    def iter(self, *, season_id: int | None = None, week: int | None = None, season_type: str | None = None, home_team_id: int | None = None, away_team_id: int | None = None, status: str | None = None, limit: int | None = None) -> Iterator[NFLGame]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(season_id=season_id, week=week, season_type=season_type, home_team_id=home_team_id, away_team_id=away_team_id, status=status, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
