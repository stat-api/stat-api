# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nfl.player_contracts (public API).
# Source: schema/api/groups/public.yml + schema/model/nfl/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NFLPlayerContract = TypedDict(
    "NFLPlayerContract",
    {
        "id": int,
        "otc_id": str,
        "player_id": int,
        "team_id": int,
        "apy": int,
        "apy_cap_pct": float,
        "college": str | None,
        "date_of_birth": str | None,
        "guaranteed": int,
        "height": str | None,
        "inflated_apy": int,
        "inflated_guaranteed": int,
        "inflated_value": int,
        "is_active": bool,
        "player_page_url": str | None,
        "position": str,
        "season_history_json": str | None,
        "value": int,
        "weight": int | None,
        "year_signed": int,
        "years": int,
    },
)


class NFLPlayerContractsResource:
    _PATH = "/api/v1/nfl/player_contracts"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, player_id: int | None = None, team_id: int | None = None, position: str | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NFLPlayerContract]:
        body, quota = self._transport.request(self._PATH, {
            "player_id": player_id,
            "team_id": team_id,
            "position": position,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NFLPlayerContract] = body["player_contracts"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NFLPlayerContract:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NFLPlayerContract = body
        return row

    def iter(self, *, player_id: int | None = None, team_id: int | None = None, position: str | None = None, limit: int | None = None) -> Iterator[NFLPlayerContract]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(player_id=player_id, team_id=team_id, position=position, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
