# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nfl.season_player_thirddown_stats (public API).
# Source: schema/api/groups/public.yml + schema/model/nfl/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NFLSeasonPlayerThirddownStat = TypedDict(
    "NFLSeasonPlayerThirddownStat",
    {
        "id": int,
        "player_id": int,
        "season_id": int,
        "assisted_tackles": int,
        "defensive_tds": int,
        "forced_fumbles": int,
        "fumble_recovery_tds": int,
        "fumble_recovery_yds": int,
        "fumbles": int,
        "fumbles_lost": int,
        "fumbles_recovered": int,
        "games_played": int,
        "games_started": int,
        "interception_tds": int,
        "interception_yds": int,
        "interceptions": int,
        "offensive_snaps": int,
        "offensive_tds": int,
        "passes_defended": int,
        "passing_attempts": int,
        "passing_completion_percentage": float,
        "passing_completions": int,
        "passing_interceptions": int,
        "passing_long": int,
        "passing_rating": float,
        "passing_sack_yds": int,
        "passing_sacks": int,
        "passing_tds": int,
        "passing_yds": int,
        "passing_yds_per_attempt": float,
        "passing_yds_per_completion": float,
        "quarterback_hits": int,
        "receiving_long": int,
        "receiving_tds": int,
        "receiving_yds": int,
        "receiving_yds_per_reception": float,
        "receiving_yds_per_target": float,
        "reception_percentage": float,
        "receptions": int,
        "rushing_attempts": int,
        "rushing_long": int,
        "rushing_tds": int,
        "rushing_yds": int,
        "rushing_yds_per_attempt": float,
        "sacks": float,
        "solo_tackles": int,
        "tackles_for_loss": int,
        "targets": int,
        "total_tds": int,
        "two_pt_pass_conversions": int,
        "two_pt_reception_conversions": int,
        "two_pt_rush_conversions": int,
    },
)


class NFLSeasonPlayerThirddownStatsResource:
    _PATH = "/api/v1/nfl/season_player_thirddown_stats"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, player_id: int | None = None, season_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NFLSeasonPlayerThirddownStat]:
        body, quota = self._transport.request(self._PATH, {
            "player_id": player_id,
            "season_id": season_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NFLSeasonPlayerThirddownStat] = body["season_player_thirddown_stats"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NFLSeasonPlayerThirddownStat:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NFLSeasonPlayerThirddownStat = body
        return row

    def iter(self, *, player_id: int | None = None, season_id: int | None = None, limit: int | None = None) -> Iterator[NFLSeasonPlayerThirddownStat]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(player_id=player_id, season_id=season_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
