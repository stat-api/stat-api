# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nfl.play_participations (public API).
# Source: schema/api/groups/public.yml + schema/model/nfl/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NFLPlayParticipation = TypedDict(
    "NFLPlayParticipation",
    {
        "id": int,
        "game_id": int,
        "play_id": str,
        "possession_team_id": int,
        "defenders_in_box": int | None,
        "defense_coverage_type": str | None,
        "defense_man_zone_type": str | None,
        "defense_personnel": str | None,
        "defense_players_gsis_ids": str | None,
        "n_defense": int | None,
        "n_offense": int | None,
        "ngs_air_yds": float | None,
        "number_of_pass_rushers": int | None,
        "offense_formation": str | None,
        "offense_personnel": str | None,
        "offense_players_gsis_ids": str | None,
        "players_on_play_gsis_ids": str | None,
        "route": str | None,
        "time_to_throw": float | None,
        "was_pressure": bool | None,
    },
)


class NFLPlayParticipationsResource:
    _PATH = "/api/v1/nfl/play_participations"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, play_id: str | None = None, game_id: int | None = None, possession_team_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NFLPlayParticipation]:
        body, quota = self._transport.request(self._PATH, {
            "play_id": play_id,
            "game_id": game_id,
            "possession_team_id": possession_team_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NFLPlayParticipation] = body["play_participations"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NFLPlayParticipation:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NFLPlayParticipation = body
        return row

    def iter(self, *, play_id: str | None = None, game_id: int | None = None, possession_team_id: int | None = None, limit: int | None = None) -> Iterator[NFLPlayParticipation]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(play_id=play_id, game_id=game_id, possession_team_id=possession_team_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
