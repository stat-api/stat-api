# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nfl.season_team_stats (public API).
# Source: schema/api/groups/public.yml + schema/model/nfl/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NFLSeasonTeamStat = TypedDict(
    "NFLSeasonTeamStat",
    {
        "id": int,
        "season_id": int,
        "team_id": int,
        "completions": int,
        "defensive_interceptions": int,
        "defensive_sacks": float,
        "defensive_tds": int,
        "extra_pts_attempted": int,
        "extra_pts_made": int,
        "field_goals_attempted": int,
        "field_goals_made": int,
        "first_downs": int,
        "fourth_down_attempts": int,
        "fourth_down_conversions": int,
        "fumbles_recovered": int,
        "games_played": int,
        "losses": int,
        "pass_attempts": int,
        "passing_tds": int,
        "passing_yds": int,
        "pts_allowed": int,
        "pts_scored": int,
        "red_zone_attempts": int,
        "red_zone_conversions": int,
        "rushing_tds": int,
        "rushing_yds": int,
        "third_down_attempts": int,
        "third_down_conversions": int,
        "ties": int,
        "total_yds": int,
        "turnovers": int,
        "wins": int,
    },
)


class NFLSeasonTeamStatsResource:
    _PATH = "/api/v1/nfl/season_team_stats"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, team_id: int | None = None, season_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NFLSeasonTeamStat]:
        body, quota = self._transport.request(self._PATH, {
            "team_id": team_id,
            "season_id": season_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NFLSeasonTeamStat] = body["season_team_stats"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NFLSeasonTeamStat:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NFLSeasonTeamStat = body
        return row

    def iter(self, *, team_id: int | None = None, season_id: int | None = None, limit: int | None = None) -> Iterator[NFLSeasonTeamStat]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(team_id=team_id, season_id=season_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
