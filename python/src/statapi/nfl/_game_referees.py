# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nfl.game_referees (public API).
# Source: schema/api/groups/public.yml + schema/model/nfl/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NFLGameReferee = TypedDict(
    "NFLGameReferee",
    {
        "id": int,
        "game_id": int,
        "referee_id": int,
        "position": str,
    },
)


class NFLGameRefereesResource:
    _PATH = "/api/v1/nfl/game_referees"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, game_id: int | None = None, referee_id: int | None = None, position: str | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NFLGameReferee]:
        body, quota = self._transport.request(self._PATH, {
            "game_id": game_id,
            "referee_id": referee_id,
            "position": position,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NFLGameReferee] = body["game_referees"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NFLGameReferee:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NFLGameReferee = body
        return row

    def iter(self, *, game_id: int | None = None, referee_id: int | None = None, position: str | None = None, limit: int | None = None) -> Iterator[NFLGameReferee]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(game_id=game_id, referee_id=referee_id, position=position, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
