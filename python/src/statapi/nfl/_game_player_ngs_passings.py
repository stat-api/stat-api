# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nfl.game_player_ngs_passings (public API).
# Source: schema/api/groups/public.yml + schema/model/nfl/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NFLGamePlayerNgsPassing = TypedDict(
    "NFLGamePlayerNgsPassing",
    {
        "id": int,
        "game_id": int,
        "player_gsis_id": str,
        "player_id": int,
        "team_id": int,
        "aggressiveness": float | None,
        "attempts": int | None,
        "avg_air_distance": float | None,
        "avg_air_yds_differential": float | None,
        "avg_air_yds_to_sticks": float | None,
        "avg_completed_air_yds": float | None,
        "avg_intended_air_yds": float | None,
        "avg_time_to_throw": float | None,
        "completion_percentage": float | None,
        "completion_percentage_above_expectation": float | None,
        "completions": int | None,
        "expected_completion_percentage": float | None,
        "interceptions": int | None,
        "max_air_distance": float | None,
        "max_completed_air_distance": float | None,
        "pass_tds": int | None,
        "pass_yds": int | None,
        "passer_rating": float | None,
        "season": int,
        "season_type": str,
        "week": int,
    },
)


class NFLGamePlayerNgsPassingsResource:
    _PATH = "/api/v1/nfl/game_player_ngs_passings"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, game_id: int | None = None, player_id: int | None = None, team_id: int | None = None, season: int | None = None, week: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NFLGamePlayerNgsPassing]:
        body, quota = self._transport.request(self._PATH, {
            "game_id": game_id,
            "player_id": player_id,
            "team_id": team_id,
            "season": season,
            "week": week,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NFLGamePlayerNgsPassing] = body["game_player_ngs_passings"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NFLGamePlayerNgsPassing:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NFLGamePlayerNgsPassing = body
        return row

    def iter(self, *, game_id: int | None = None, player_id: int | None = None, team_id: int | None = None, season: int | None = None, week: int | None = None, limit: int | None = None) -> Iterator[NFLGamePlayerNgsPassing]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(game_id=game_id, player_id=player_id, team_id=team_id, season=season, week=week, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
