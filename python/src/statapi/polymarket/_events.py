# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for polymarket.events (public API).
# Source: schema/api/groups/public.yml + schema/model/polymarket/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

PolymarketEvent = TypedDict(
    "PolymarketEvent",
    {
        "id": int,
        "competition_id": int | None,
        "condition_id": str,
        "away_team": str | None,
        "description": str | None,
        "end_date": str | None,
        "game_date": str | None,
        "home_team": str | None,
        "league_code": str | None,
        "liquidity": int | None,
        "market_count": int | None,
        "neg_risk": bool,
        "slug": str,
        "start_date": str | None,
        "status": str,
        "title": str,
        "volume": int | None,
    },
)


class PolymarketEventsResource:
    _PATH = "/api/v1/polymarket/events"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, league_code: str | None = None, competition_id: int | None = None, status: str | None = None, game_date: str | None = None, limit: int | None = None, from_id: int | None = None) -> Page[PolymarketEvent]:
        body, quota = self._transport.request(self._PATH, {
            "league_code": league_code,
            "competition_id": competition_id,
            "status": status,
            "game_date": game_date,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[PolymarketEvent] = body["events"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> PolymarketEvent:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: PolymarketEvent = body
        return row

    def iter(self, *, league_code: str | None = None, competition_id: int | None = None, status: str | None = None, game_date: str | None = None, limit: int | None = None) -> Iterator[PolymarketEvent]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(league_code=league_code, competition_id=competition_id, status=status, game_date=game_date, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
