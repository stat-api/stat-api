# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for kalshi.markets (public API).
# Source: schema/api/groups/public.yml + schema/model/kalshi/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

KalshiMarket = TypedDict(
    "KalshiMarket",
    {
        "id": int,
        "competition_id": int | None,
        "event_id": int,
        "series_id": int,
        "close_time": str | None,
        "contract_type": str,
        "expiration_time": str | None,
        "last_price": int | None,
        "line": float | None,
        "liquidity": int,
        "open_interest": int,
        "open_time": str | None,
        "result": str | None,
        "settled_at": str | None,
        "settlement_value": int | None,
        "status": str,
        "subtitle": str | None,
        "ticker": str,
        "title": str,
        "volume": int,
        "yes_ask": int | None,
        "yes_bid": int | None,
    },
)


class KalshiMarketsResource:
    _PATH = "/api/v1/kalshi/markets"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, event_id: int | None = None, series_id: int | None = None, competition_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[KalshiMarket]:
        body, quota = self._transport.request(self._PATH, {
            "event_id": event_id,
            "series_id": series_id,
            "competition_id": competition_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[KalshiMarket] = body["markets"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> KalshiMarket:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: KalshiMarket = body
        return row

    def iter(self, *, event_id: int | None = None, series_id: int | None = None, competition_id: int | None = None, limit: int | None = None) -> Iterator[KalshiMarket]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(event_id=event_id, series_id=series_id, competition_id=competition_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
