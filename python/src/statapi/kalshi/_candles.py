# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for kalshi.candles (public API).
# Source: schema/api/groups/public.yml + schema/model/kalshi/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

KalshiCandle = TypedDict(
    "KalshiCandle",
    {
        "id": int,
        "market_id": int,
        "end_period_ts": int,
        "open_interest": int,
        "period_interval": int,
        "ticker": str,
        "volume": int,
        "yes_ask_close": int | None,
        "yes_ask_high": int | None,
        "yes_ask_low": int | None,
        "yes_ask_open": int | None,
        "yes_bid_close": int | None,
        "yes_bid_high": int | None,
        "yes_bid_low": int | None,
        "yes_bid_open": int | None,
    },
)


class KalshiCandlesResource:
    _PATH = "/api/v1/kalshi/candles"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, market_id: int | None = None, period_interval: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[KalshiCandle]:
        body, quota = self._transport.request(self._PATH, {
            "market_id": market_id,
            "period_interval": period_interval,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[KalshiCandle] = body["candles"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> KalshiCandle:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: KalshiCandle = body
        return row

    def iter(self, *, market_id: int | None = None, period_interval: int | None = None, limit: int | None = None) -> Iterator[KalshiCandle]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(market_id=market_id, period_interval=period_interval, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
