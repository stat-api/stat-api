# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for mlb.games (public API).
# Source: schema/api/groups/public.yml + schema/model/mlb/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

MLBGame = TypedDict(
    "MLBGame",
    {
        "id": int,
        "away_team_id": int,
        "home_team_id": int,
        "league_game_id": int | None,
        "league_id": str | None,
        "losing_pitcher_id": int | None,
        "save_pitcher_id": int | None,
        "season_id": int,
        "venue_id": int | None,
        "winning_pitcher_id": int | None,
        "attendance": int | None,
        "away_team_score": int,
        "channel": str | None,
        "day": int,
        "day_night": str | None,
        "doubleheader": bool,
        "duration_minutes": int | None,
        "end_time": str | None,
        "game_label": str | None,
        "game_number": int,
        "game_time": str,
        "game_type": str,
        "home_team_score": int,
        "if_necessary": bool | None,
        "neutral_venue": bool,
        "postponed_status": str | None,
        "season_type": str,
        "series_game_number": str | None,
        "series_name": str | None,
        "series_text": str | None,
        "status": str,
    },
)


class MLBGamesResource:
    _PATH = "/api/v1/mlb/games"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, season_id: int | None = None, home_team_id: int | None = None, away_team_id: int | None = None, status: str | None = None, day: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[MLBGame]:
        body, quota = self._transport.request(self._PATH, {
            "season_id": season_id,
            "home_team_id": home_team_id,
            "away_team_id": away_team_id,
            "status": status,
            "day": day,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[MLBGame] = body["games"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> MLBGame:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: MLBGame = body
        return row

    def iter(self, *, season_id: int | None = None, home_team_id: int | None = None, away_team_id: int | None = None, status: str | None = None, day: int | None = None, limit: int | None = None) -> Iterator[MLBGame]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(season_id=season_id, home_team_id=home_team_id, away_team_id=away_team_id, status=status, day=day, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
