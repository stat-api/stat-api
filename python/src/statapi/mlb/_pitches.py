# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for mlb.pitches (public API).
# Source: schema/api/groups/public.yml + schema/model/mlb/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

MLBPitch = TypedDict(
    "MLBPitch",
    {
        "id": int,
        "at_bat_id": int,
        "batter_id": int,
        "game_id": int,
        "pitcher_id": int,
        "play_id": int,
        "air_out": bool,
        "ball": bool,
        "barrel": bool | None,
        "break_angle": float | None,
        "break_length": float | None,
        "break_y": float | None,
        "breaking_ball": bool | None,
        "call": str,
        "catchers_interference": bool,
        "chase": bool | None,
        "double": bool,
        "effective_speed": float | None,
        "expected_batting_avg": float | None,
        "expected_woba": float | None,
        "extension": float | None,
        "fastball": bool | None,
        "flare_burner": bool | None,
        "fly_out": bool,
        "ground_into_double_play": bool,
        "ground_into_triple_play": bool,
        "ground_out": bool,
        "hard_hit": bool | None,
        "hit_by_pitch": bool,
        "hit_coord_x": float | None,
        "hit_coord_y": float | None,
        "hit_distance": int | None,
        "hit_exit_velocity": float | None,
        "hit_launch_angle": float | None,
        "home_run": bool,
        "in_play": bool,
        "in_strike_zone": bool | None,
        "intentional_walk": bool,
        "launch_speed_angle_zone": int | None,
        "line_out": bool,
        "offspeed": bool | None,
        "pitch_number": int,
        "pitch_speed": float | None,
        "pitch_type": str | None,
        "pitch_x": float | None,
        "pitch_y": float | None,
        "pitch_z": float | None,
        "pitch_zone": int | None,
        "plate_x": float | None,
        "plate_z": float | None,
        "pop_out": bool,
        "release_speed": float | None,
        "result": str,
        "sacrifice_fly": bool,
        "sacrifice_hit": bool,
        "single": bool,
        "solid_contact": bool | None,
        "spin_axis": float | None,
        "spin_direction": float | None,
        "spin_rate": float | None,
        "spray_angle": float | None,
        "statcast_quality_score": float | None,
        "strike": bool,
        "strike_zone_bottom": float | None,
        "strike_zone_top": float | None,
        "strikeout": bool,
        "sweet_spot": bool | None,
        "topped": bool | None,
        "tracking_confidence": float | None,
        "triple": bool,
        "under": bool | None,
        "walk": bool,
    },
)


class MLBPitchesResource:
    _PATH = "/api/v1/mlb/pitches"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, game_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[MLBPitch]:
        body, quota = self._transport.request(self._PATH, {
            "game_id": game_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[MLBPitch] = body["pitches"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> MLBPitch:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: MLBPitch = body
        return row

    def iter(self, *, game_id: int | None = None, limit: int | None = None) -> Iterator[MLBPitch]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(game_id=game_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
