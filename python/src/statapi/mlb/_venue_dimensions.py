# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for mlb.venue_dimensions (public API).
# Source: schema/api/groups/public.yml + schema/model/mlb/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

MLBVenueDimension = TypedDict(
    "MLBVenueDimension",
    {
        "id": int,
        "mlb_venue_id": int,
        "venue_id": int,
        "altitude_ft": float | None,
        "backstop_distance": float | None,
        "center_field": float,
        "center_field_fence_height": float,
        "description": str | None,
        "fair_territory_sq_ft": float | None,
        "foul_territory_rating": float | None,
        "foul_territory_sq_ft": float | None,
        "left_center_field": float | None,
        "left_field": float,
        "left_field_fence_height": float,
        "orientation": float,
        "right_center_field": float | None,
        "right_field": float,
        "right_field_fence_height": float,
        "total_territory_sq_ft": float | None,
        "venue_name": str | None,
        "year": int,
    },
)


class MLBVenueDimensionsResource:
    _PATH = "/api/v1/mlb/venue_dimensions"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, venue_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[MLBVenueDimension]:
        body, quota = self._transport.request(self._PATH, {
            "venue_id": venue_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[MLBVenueDimension] = body["venue_dimensions"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> MLBVenueDimension:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: MLBVenueDimension = body
        return row

    def iter(self, *, venue_id: int | None = None, limit: int | None = None) -> Iterator[MLBVenueDimension]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(venue_id=venue_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
