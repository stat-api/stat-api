# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for mlb.game_player_batter_stats (public API).
# Source: schema/api/groups/public.yml + schema/model/mlb/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

MLBGamePlayerBatterStat = TypedDict(
    "MLBGamePlayerBatterStat",
    {
        "id": int,
        "game_id": int,
        "player_id": int,
        "team_id": int,
        "air_outs": int,
        "at_bats": int,
        "at_bats_per_home_run": float,
        "babip": float,
        "balls_in_play": int,
        "batting_average": float,
        "catchers_interference": int,
        "caught_stealing": int,
        "double_plays": int,
        "doubles": int,
        "fly_outs": int,
        "ground_into_double_play": int,
        "ground_into_triple_play": int,
        "ground_outs": int,
        "hit_by_pitch": int,
        "hits": int,
        "home_runs": int,
        "intentional_walks": int,
        "isolated_power": float,
        "left_on_base": int,
        "line_outs": int,
        "on_base_percentage": float,
        "on_base_plus_slugging": float,
        "pickoffs": int,
        "plate_appearances": int,
        "pop_outs": int,
        "runs": int,
        "runs_batted_in": int,
        "sacrifice_flies": int,
        "sacrifices": int,
        "singles": int,
        "slugging_percentage": float,
        "stolen_bases": int,
        "strikeouts": int,
        "total_bases": int,
        "triples": int,
        "walks": int,
        "woba": float,
    },
)


class MLBGamePlayerBatterStatsResource:
    _PATH = "/api/v1/mlb/game_player_batter_stats"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, game_id: int | None = None, player_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[MLBGamePlayerBatterStat]:
        body, quota = self._transport.request(self._PATH, {
            "game_id": game_id,
            "player_id": player_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[MLBGamePlayerBatterStat] = body["game_player_batter_stats"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> MLBGamePlayerBatterStat:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: MLBGamePlayerBatterStat = body
        return row

    def iter(self, *, game_id: int | None = None, player_id: int | None = None, limit: int | None = None) -> Iterator[MLBGamePlayerBatterStat]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(game_id=game_id, player_id=player_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
