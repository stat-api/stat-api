# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for mlb.game_team_stats (public API).
# Source: schema/api/groups/public.yml + schema/model/mlb/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

MLBGameTeamStat = TypedDict(
    "MLBGameTeamStat",
    {
        "id": int,
        "game_id": int,
        "team_id": int,
        "assists": int,
        "balks": int,
        "batters_faced": int,
        "batting_average": float,
        "caught_stealing": int,
        "double_plays_turned": int,
        "doubles": int,
        "earned_run_average": float,
        "earned_runs": int,
        "errors": int,
        "grounded_into_double_play": int,
        "hit_by_pitch": int,
        "hits": int,
        "home_runs": int,
        "innings_pitched": float,
        "left_on_base": int,
        "loss": int,
        "on_base_percentage": float,
        "on_base_plus_slugging": float,
        "passed_balls": int,
        "pitches_thrown": int,
        "putouts": int,
        "runs": int,
        "runs_batted_in": int,
        "sacrifice_flies": int,
        "sacrifice_hits": int,
        "slugging_percentage": float,
        "stolen_bases": int,
        "strikeouts": int,
        "strikes": int,
        "triples": int,
        "walks": int,
        "wild_pitches": int,
        "win": int,
    },
)


class MLBGameTeamStatsResource:
    _PATH = "/api/v1/mlb/game_team_stats"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, game_id: int | None = None, team_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[MLBGameTeamStat]:
        body, quota = self._transport.request(self._PATH, {
            "game_id": game_id,
            "team_id": team_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[MLBGameTeamStat] = body["game_team_stats"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> MLBGameTeamStat:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: MLBGameTeamStat = body
        return row

    def iter(self, *, game_id: int | None = None, team_id: int | None = None, limit: int | None = None) -> Iterator[MLBGameTeamStat]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(game_id=game_id, team_id=team_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
