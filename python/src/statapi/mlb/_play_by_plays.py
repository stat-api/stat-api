# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for mlb.play_by_plays (public API).
# Source: schema/api/groups/public.yml + schema/model/mlb/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

MLBPlayByPlay = TypedDict(
    "MLBPlayByPlay",
    {
        "id": int,
        "at_bat_id": int | None,
        "batter_id": int | None,
        "catcher_id": int | None,
        "first_base_runner_id": int | None,
        "game_id": int,
        "inning_id": int,
        "pitcher_id": int | None,
        "play_id": int,
        "player_id": int | None,
        "second_base_runner_id": int | None,
        "team_id": int,
        "third_base_runner_id": int | None,
        "count_balls": int,
        "count_strikes": int,
        "event_sub_type": int,
        "event_type": int,
        "hit_location": str | None,
        "hit_type": str | None,
        "mlb_event_num": int,
        "outs": int,
        "runners_on_base": int,
    },
)


class MLBPlayByPlaysResource:
    _PATH = "/api/v1/mlb/play_by_plays"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, game_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[MLBPlayByPlay]:
        body, quota = self._transport.request(self._PATH, {
            "game_id": game_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[MLBPlayByPlay] = body["play_by_plays"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> MLBPlayByPlay:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: MLBPlayByPlay = body
        return row

    def iter(self, *, game_id: int | None = None, limit: int | None = None) -> Iterator[MLBPlayByPlay]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(game_id=game_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
