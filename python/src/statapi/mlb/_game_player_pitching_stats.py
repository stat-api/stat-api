# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for mlb.game_player_pitching_stats (public API).
# Source: schema/api/groups/public.yml + schema/model/mlb/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

MLBGamePlayerPitchingStat = TypedDict(
    "MLBGamePlayerPitchingStat",
    {
        "id": int,
        "game_id": int,
        "player_id": int,
        "team_id": int,
        "air_outs": int,
        "babip": float,
        "balks": int,
        "balls": int,
        "balls_in_play": int,
        "batters_faced": int,
        "blown_save": int,
        "catchers_interference": int,
        "complete_games": int,
        "double_plays": int,
        "doubles_allowed": int,
        "earned_run_average": float,
        "earned_runs": int,
        "fip": float,
        "fly_outs": int,
        "games_finished": int,
        "games_pitched": int,
        "games_started": int,
        "grand_slams_allowed": int,
        "ground_into_double_play": int,
        "ground_outs": int,
        "hit_by_pitch": int,
        "hits_allowed": int,
        "hold": int,
        "home_runs_allowed": int,
        "home_runs_per_nine": float,
        "inherited_runners": int,
        "inherited_runners_scored": int,
        "inning_started": int,
        "innings_pitched": float,
        "intentional_walks": int,
        "left_on_base": float,
        "line_outs": int,
        "loss": int,
        "no_hitters": int,
        "on_base_percentage": float,
        "on_base_plus_slugging": float,
        "outs": int,
        "perfect_games": int,
        "pickoffs": int,
        "pitches_thrown": int,
        "plate_appearances": int,
        "pop_outs": int,
        "quality_starts": int,
        "runs_allowed": int,
        "runs_scored_per_nine": float,
        "sacrifice_flies_allowed": int,
        "sacrifices_allowed": int,
        "save": int,
        "shutouts": int,
        "singles_allowed": int,
        "slugging_percentage": float,
        "strike_percentage": float,
        "strikeouts_per_nine": float,
        "strikeouts_pitched": int,
        "strikes": int,
        "total_bases_allowed": int,
        "triples_allowed": int,
        "walks_allowed": int,
        "walks_per_nine": float,
        "whip": float,
        "wild_pitches": int,
        "win": int,
        "woba": float,
    },
)


class MLBGamePlayerPitchingStatsResource:
    _PATH = "/api/v1/mlb/game_player_pitching_stats"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, game_id: int | None = None, player_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[MLBGamePlayerPitchingStat]:
        body, quota = self._transport.request(self._PATH, {
            "game_id": game_id,
            "player_id": player_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[MLBGamePlayerPitchingStat] = body["game_player_pitching_stats"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> MLBGamePlayerPitchingStat:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: MLBGamePlayerPitchingStat = body
        return row

    def iter(self, *, game_id: int | None = None, player_id: int | None = None, limit: int | None = None) -> Iterator[MLBGamePlayerPitchingStat]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(game_id=game_id, player_id=player_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
