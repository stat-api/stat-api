# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for mlb.team_starting_lineup_batters (public API).
# Source: schema/api/groups/public.yml + schema/model/mlb/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

MLBTeamStartingLineupBatter = TypedDict(
    "MLBTeamStartingLineupBatter",
    {
        "id": int,
        "player_id": int,
        "team_starting_lineup_id": int,
        "batting_order": str,
        "position": str,
    },
)


class MLBTeamStartingLineupBattersResource:
    _PATH = "/api/v1/mlb/team_starting_lineup_batters"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, team_starting_lineup_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[MLBTeamStartingLineupBatter]:
        body, quota = self._transport.request(self._PATH, {
            "team_starting_lineup_id": team_starting_lineup_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[MLBTeamStartingLineupBatter] = body["team_starting_lineup_batters"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> MLBTeamStartingLineupBatter:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: MLBTeamStartingLineupBatter = body
        return row

    def iter(self, *, team_starting_lineup_id: int | None = None, limit: int | None = None) -> Iterator[MLBTeamStartingLineupBatter]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(team_starting_lineup_id=team_starting_lineup_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
