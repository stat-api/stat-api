# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for mlb.innings (public API).
# Source: schema/api/groups/public.yml + schema/model/mlb/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

MLBInning = TypedDict(
    "MLBInning",
    {
        "id": int,
        "game_id": int,
        "away_team_errors": int,
        "away_team_hits": int,
        "away_team_runs": int,
        "home_team_errors": int,
        "home_team_hits": int,
        "home_team_runs": int,
        "inning_half": str,
        "inning_number": int,
    },
)


class MLBInningsResource:
    _PATH = "/api/v1/mlb/innings"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, game_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[MLBInning]:
        body, quota = self._transport.request(self._PATH, {
            "game_id": game_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[MLBInning] = body["innings"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> MLBInning:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: MLBInning = body
        return row

    def iter(self, *, game_id: int | None = None, limit: int | None = None) -> Iterator[MLBInning]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(game_id=game_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
