# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for dfs.contest_users (public API).
# Source: schema/api/groups/public.yml + schema/model/dfs/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

DFSContestUser = TypedDict(
    "DFSContestUser",
    {
        "id": int,
        "external_id": str,
        "operator_id": int,
        "name": str | None,
    },
)


class DFSContestUsersResource:
    _PATH = "/api/v1/dfs/contest_users"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, operator_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[DFSContestUser]:
        body, quota = self._transport.request(self._PATH, {
            "operator_id": operator_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[DFSContestUser] = body["contest_users"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> DFSContestUser:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: DFSContestUser = body
        return row

    def iter(self, *, operator_id: int | None = None, limit: int | None = None) -> Iterator[DFSContestUser]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(operator_id=operator_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
