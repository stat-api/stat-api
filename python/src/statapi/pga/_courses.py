# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for pga.courses (public API).
# Source: schema/api/groups/public.yml + schema/model/golf/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

PGACourse = TypedDict(
    "PGACourse",
    {
        "id": int,
        "league_course_id": int | None,
        "address": str | None,
        "city": str | None,
        "country": str | None,
        "elevation": float | None,
        "image_url": str | None,
        "latitude": float | None,
        "longitude": float | None,
        "name": str,
        "par": int,
        "state": str | None,
        "timezone": str | None,
        "yards": int,
        "zip": str | None,
    },
)


class PGACoursesResource:
    _PATH = "/api/v1/pga/courses"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, limit: int | None = None, from_id: int | None = None) -> Page[PGACourse]:
        body, quota = self._transport.request(self._PATH, {
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[PGACourse] = body["courses"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> PGACourse:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: PGACourse = body
        return row

    def iter(self, *, limit: int | None = None) -> Iterator[PGACourse]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
