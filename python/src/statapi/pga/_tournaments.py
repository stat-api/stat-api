# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for pga.tournaments (public API).
# Source: schema/api/groups/public.yml + schema/model/golf/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

PGATournament = TypedDict(
    "PGATournament",
    {
        "id": int,
        "league_id": str | None,
        "season_id": int | None,
        "tour_id": int | None,
        "tournament_id": int,
        "canceled": bool,
        "city": str | None,
        "country": str | None,
        "end_date": str | None,
        "is_in_progress": bool,
        "is_over": bool,
        "name": str,
        "par": int | None,
        "purse": int | None,
        "rounds": int | None,
        "start_date": str,
        "start_date_time": str | None,
        "state": str | None,
        "time_zone": str | None,
        "venue": str | None,
        "yards": int | None,
        "zip": str | None,
    },
)


class PGATournamentsResource:
    _PATH = "/api/v1/pga/tournaments"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, season_id: int | None = None, tour_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[PGATournament]:
        body, quota = self._transport.request(self._PATH, {
            "season_id": season_id,
            "tour_id": tour_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[PGATournament] = body["tournaments"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> PGATournament:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: PGATournament = body
        return row

    def iter(self, *, season_id: int | None = None, tour_id: int | None = None, limit: int | None = None) -> Iterator[PGATournament]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(season_id=season_id, tour_id=tour_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
