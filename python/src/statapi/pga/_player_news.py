# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for pga.player_news (public API).
# Source: schema/api/groups/public.yml + schema/model/golf/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import Any, TypedDict

from ..page import Page
from ..transport import HttpTransport

PGAPlayerNew = TypedDict(
    "PGAPlayerNew",
    {
        "id": int,
        "player_id": int | None,
        "tournament_id": int | None,
        "ai_processed": bool,
        "author": str | None,
        "categories": str | None,
        "content": str | None,
        "news_time": str,
        "original_source": str | None,
        "original_source_url": str | None,
        "priority": int,
        "situational_impact": dict[str, Any] | None,
        "source": str | None,
        "terms_of_use": str | None,
        "title": str,
        "updated_time": str | None,
        "url": str | None,
    },
)


class PGAPlayerNewsResource:
    _PATH = "/api/v1/pga/player_news"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, player_id: int | None = None, tournament_id: int | None = None, news_time: str | None = None, limit: int | None = None, from_id: int | None = None) -> Page[PGAPlayerNew]:
        body, quota = self._transport.request(self._PATH, {
            "player_id": player_id,
            "tournament_id": tournament_id,
            "news_time": news_time,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[PGAPlayerNew] = body["player_news"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> PGAPlayerNew:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: PGAPlayerNew = body
        return row

    def iter(self, *, player_id: int | None = None, tournament_id: int | None = None, news_time: str | None = None, limit: int | None = None) -> Iterator[PGAPlayerNew]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(player_id=player_id, tournament_id=tournament_id, news_time=news_time, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
