# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for pga.leaderboards (public API).
# Source: schema/api/groups/public.yml + schema/model/golf/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

PGALeaderboard = TypedDict(
    "PGALeaderboard",
    {
        "id": int,
        "player_id": int,
        "player_tournament_id": int,
        "tournament_id": int,
        "earnings": int | None,
        "fedex_pts": int | None,
        "is_alternate": bool | None,
        "is_withdrawn": bool | None,
        "made_cut": int | None,
        "made_cut_did_not_finish": bool | None,
        "rank": int | None,
        "sg_approach": float | None,
        "sg_around_green": float | None,
        "sg_off_tee": float | None,
        "sg_putting": float | None,
        "sg_tee_to_green": float | None,
        "sg_total": float | None,
        "tee_time": str | None,
        "total_score": int | None,
        "total_strokes": int | None,
        "total_through": int | None,
        "tournament_status": str | None,
        "win": int | None,
    },
)


class PGALeaderboardsResource:
    _PATH = "/api/v1/pga/leaderboards"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, tournament_id: int | None = None, player_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[PGALeaderboard]:
        body, quota = self._transport.request(self._PATH, {
            "tournament_id": tournament_id,
            "player_id": player_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[PGALeaderboard] = body["leaderboards"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> PGALeaderboard:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: PGALeaderboard = body
        return row

    def iter(self, *, tournament_id: int | None = None, player_id: int | None = None, limit: int | None = None) -> Iterator[PGALeaderboard]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(tournament_id=tournament_id, player_id=player_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
