# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for pga.player_shots (public API).
# Source: schema/api/groups/public.yml + schema/model/golf/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

PGAPlayerShot = TypedDict(
    "PGAPlayerShot",
    {
        "id": int,
        "player_id": int,
        "tournament_id": int,
        "apex_height": float | None,
        "ball_speed": float | None,
        "carry_distance": float | None,
        "club_speed": float | None,
        "distance_remaining_yards": float | None,
        "distance_yards": float | None,
        "final_stroke": bool | None,
        "from_location": str | None,
        "from_location_code": str | None,
        "from_x": float | None,
        "from_y": float | None,
        "from_z": float | None,
        "hole_number": int,
        "launch_angle": float | None,
        "launch_spin": float | None,
        "par": int | None,
        "play_by_play": str | None,
        "round_number": int,
        "shot_number": int,
        "smash_factor": float | None,
        "spin_axis": float | None,
        "stroke_type": str | None,
        "to_location": str | None,
        "to_location_code": str | None,
        "to_x": float | None,
        "to_y": float | None,
        "to_z": float | None,
    },
)


class PGAPlayerShotsResource:
    _PATH = "/api/v1/pga/player_shots"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, player_id: int | None = None, tournament_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[PGAPlayerShot]:
        body, quota = self._transport.request(self._PATH, {
            "player_id": player_id,
            "tournament_id": tournament_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[PGAPlayerShot] = body["player_shots"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> PGAPlayerShot:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: PGAPlayerShot = body
        return row

    def iter(self, *, player_id: int | None = None, tournament_id: int | None = None, limit: int | None = None) -> Iterator[PGAPlayerShot]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(player_id=player_id, tournament_id=tournament_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
