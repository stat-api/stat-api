# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for pga.player_season_stats (public API).
# Source: schema/api/groups/public.yml + schema/model/golf/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

PGAPlayerSeasonStat = TypedDict(
    "PGAPlayerSeasonStat",
    {
        "id": int,
        "player_id": int,
        "season_id": int | None,
        "tour_id": int | None,
        "birdies": int | None,
        "bogey_free_rounds": int | None,
        "bogeys": int | None,
        "bounce_back_count": int | None,
        "consecutive_birdie_or_better_count": int | None,
        "cuts_made": int | None,
        "double_bogeys": int | None,
        "double_eagles": int | None,
        "driving_accuracy_pct": float | None,
        "driving_distance": float | None,
        "eagles": int | None,
        "events": int | None,
        "gir_pct": float | None,
        "holes_in_one": int | None,
        "pars": int | None,
        "putts_per_round": float | None,
        "rounds": int | None,
        "rounds_under_seventy": int | None,
        "scrambling_pct": float | None,
        "second_place": int | None,
        "sg_approach": float | None,
        "sg_around_green": float | None,
        "sg_off_tee": float | None,
        "sg_putting": float | None,
        "sg_total": float | None,
        "streaks_of_three_birdies_or_better": int | None,
        "third_place": int | None,
        "top_five": int | None,
        "top_ten": int | None,
        "top_twenty_five": int | None,
        "total_score": int | None,
        "total_strokes": int | None,
        "triple_bogeys": int | None,
        "wins": int | None,
        "worse_than_double_bogey": int | None,
        "worse_than_triple_bogey": int | None,
    },
)


class PGAPlayerSeasonStatsResource:
    _PATH = "/api/v1/pga/player_season_stats"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, season_id: int | None = None, player_id: int | None = None, tour_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[PGAPlayerSeasonStat]:
        body, quota = self._transport.request(self._PATH, {
            "season_id": season_id,
            "player_id": player_id,
            "tour_id": tour_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[PGAPlayerSeasonStat] = body["player_season_stats"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> PGAPlayerSeasonStat:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: PGAPlayerSeasonStat = body
        return row

    def iter(self, *, season_id: int | None = None, player_id: int | None = None, tour_id: int | None = None, limit: int | None = None) -> Iterator[PGAPlayerSeasonStat]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(season_id=season_id, player_id=player_id, tour_id=tour_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
