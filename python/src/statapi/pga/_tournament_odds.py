# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for pga.tournament_odds (public API).
# Source: schema/api/groups/public.yml + schema/model/golf/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

PGATournamentOdd = TypedDict(
    "PGATournamentOdd",
    {
        "id": int,
        "matchup_opponent_id": int | None,
        "player_id": int,
        "round_matchup_opponent_id": int | None,
        "three_ball_opponent1_id": int | None,
        "three_ball_opponent2_id": int | None,
        "tournament_id": int,
        "first_round_leader_odds": int | None,
        "is_alternate": bool | None,
        "is_available": bool | None,
        "make_cut_odds": int | None,
        "matchup_odds": int | None,
        "matchup_opponent_odds": int | None,
        "miss_cut_odds": int | None,
        "outright_odds": int | None,
        "round_matchup_odds": int | None,
        "round_matchup_opponent_odds": int | None,
        "sportsbook": str | None,
        "three_ball_odds": int | None,
        "top_five_odds": int | None,
        "top_ten_odds": int | None,
        "top_twenty_odds": int | None,
    },
)


class PGATournamentOddsResource:
    _PATH = "/api/v1/pga/tournament_odds"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, tournament_id: int | None = None, player_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[PGATournamentOdd]:
        body, quota = self._transport.request(self._PATH, {
            "tournament_id": tournament_id,
            "player_id": player_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[PGATournamentOdd] = body["tournament_odds"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> PGATournamentOdd:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: PGATournamentOdd = body
        return row

    def iter(self, *, tournament_id: int | None = None, player_id: int | None = None, limit: int | None = None) -> Iterator[PGATournamentOdd]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(tournament_id=tournament_id, player_id=player_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
