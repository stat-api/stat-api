# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nhl.standings (public API).
# Source: schema/api/groups/public.yml + schema/model/nhl/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NHLStanding = TypedDict(
    "NHLStanding",
    {
        "id": int,
        "season_id": int,
        "team_id": int,
        "away_losses": int,
        "away_overtime_losses": int,
        "away_wins": int,
        "clinched_conference": bool,
        "clinched_division": bool,
        "clinched_playoffs": bool,
        "clinched_presidents_trophy": bool,
        "conference": str,
        "conference_rank": int,
        "division": str,
        "division_rank": int,
        "games_played": int,
        "goal_differential": int,
        "goals_against": int,
        "goals_for": int,
        "home_losses": int,
        "home_overtime_losses": int,
        "home_wins": int,
        "last_10_losses": int,
        "last_10_overtime_losses": int,
        "last_10_wins": int,
        "losses": int,
        "overtime_losses": int,
        "pts": int,
        "pts_percentage": float,
        "rank": int,
        "regulation_wins": int,
        "season_type": str,
        "shootout_losses": int,
        "shootout_wins": int,
        "streak": str | None,
        "wild_card_rank": int | None,
        "wins": int,
    },
)


class NHLStandingsResource:
    _PATH = "/api/v1/nhl/standings"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, season_id: int | None = None, team_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NHLStanding]:
        body, quota = self._transport.request(self._PATH, {
            "season_id": season_id,
            "team_id": team_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NHLStanding] = body["standings"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NHLStanding:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NHLStanding = body
        return row

    def iter(self, *, season_id: int | None = None, team_id: int | None = None, limit: int | None = None) -> Iterator[NHLStanding]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(season_id=season_id, team_id=team_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
