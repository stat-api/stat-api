# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nhl.playoff_series (public API).
# Source: schema/api/groups/public.yml + schema/model/nhl/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NHLPlayoffSery = TypedDict(
    "NHLPlayoffSery",
    {
        "id": int,
        "away_team_id": int,
        "home_team_id": int,
        "season_id": int,
        "series_id": int | None,
        "winner_team_id": int | None,
        "away_team_seed": int,
        "away_team_wins": int,
        "conference": str | None,
        "games_played": int,
        "home_team_seed": int,
        "home_team_wins": int,
        "max_games": int,
        "playoff_round": str,
        "status": str,
    },
)


class NHLPlayoffSeriesResource:
    _PATH = "/api/v1/nhl/playoff_series"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, season_id: int | None = None, home_team_id: int | None = None, away_team_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NHLPlayoffSery]:
        body, quota = self._transport.request(self._PATH, {
            "season_id": season_id,
            "home_team_id": home_team_id,
            "away_team_id": away_team_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NHLPlayoffSery] = body["playoff_series"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NHLPlayoffSery:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NHLPlayoffSery = body
        return row

    def iter(self, *, season_id: int | None = None, home_team_id: int | None = None, away_team_id: int | None = None, limit: int | None = None) -> Iterator[NHLPlayoffSery]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(season_id=season_id, home_team_id=home_team_id, away_team_id=away_team_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
