# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nhl.season_team_stats (public API).
# Source: schema/api/groups/public.yml + schema/model/nhl/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NHLSeasonTeamStat = TypedDict(
    "NHLSeasonTeamStat",
    {
        "id": int,
        "season_id": int,
        "team_id": int,
        "blocked_shots": int,
        "faceoff_losses": int,
        "faceoff_percentage": float,
        "faceoff_wins": int,
        "games_played": int,
        "giveaways": int,
        "goal_differential": int,
        "goals_against": int,
        "goals_against_average": float,
        "goals_against_per_game": float,
        "goals_for": int,
        "goals_per_game": float,
        "hits": int,
        "losses": int,
        "overtime_losses": int,
        "penalty_kill_goals_against": int,
        "penalty_kill_percentage": float,
        "penalty_minutes": int,
        "power_play_goals": int,
        "power_play_opportunities": int,
        "power_play_percentage": float,
        "pts": int,
        "pts_percentage": float,
        "save_percentage": float,
        "season_type": str,
        "shooting_percentage": float,
        "short_handed_goals": int,
        "shots_against": int,
        "shots_for": int,
        "shutouts": int,
        "takeaways": int,
        "times_shorthanded": int,
        "wins": int,
    },
)


class NHLSeasonTeamStatsResource:
    _PATH = "/api/v1/nhl/season_team_stats"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, season_id: int | None = None, team_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NHLSeasonTeamStat]:
        body, quota = self._transport.request(self._PATH, {
            "season_id": season_id,
            "team_id": team_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NHLSeasonTeamStat] = body["season_team_stats"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NHLSeasonTeamStat:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NHLSeasonTeamStat = body
        return row

    def iter(self, *, season_id: int | None = None, team_id: int | None = None, limit: int | None = None) -> Iterator[NHLSeasonTeamStat]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(season_id=season_id, team_id=team_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
