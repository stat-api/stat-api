# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nhl.players (public API).
# Source: schema/api/groups/public.yml + schema/model/nhl/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NHLPlayer = TypedDict(
    "NHLPlayer",
    {
        "id": int,
        "draft_team_id": int | None,
        "league_id": str | None,
        "league_player_id": int,
        "team_id": int | None,
        "birth_city": str | None,
        "birth_country": str | None,
        "birth_date": str | None,
        "birth_state": str | None,
        "catches": str | None,
        "college": str | None,
        "draft_overall_pick": int | None,
        "draft_pick": int | None,
        "draft_round": int | None,
        "draft_year": int | None,
        "experience": int | None,
        "first_name": str,
        "full_name": str,
        "height": int | None,
        "injury_body_part": str | None,
        "injury_notes": str | None,
        "injury_start_date": str | None,
        "injury_status": str | None,
        "jersey": str | None,
        "last_name": str,
        "nationality": str | None,
        "photo_url": str | None,
        "position_category": str | None,
        "primary_position": str,
        "rookie_year": int | None,
        "roster_status": str | None,
        "shoots": str | None,
        "weight": int | None,
        "youth_league": str | None,
        "youth_team": str | None,
    },
)


class NHLPlayersResource:
    _PATH = "/api/v1/nhl/players"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, team_id: int | None = None, roster_status: str | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NHLPlayer]:
        body, quota = self._transport.request(self._PATH, {
            "team_id": team_id,
            "roster_status": roster_status,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NHLPlayer] = body["players"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NHLPlayer:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NHLPlayer = body
        return row

    def iter(self, *, team_id: int | None = None, roster_status: str | None = None, limit: int | None = None) -> Iterator[NHLPlayer]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(team_id=team_id, roster_status=roster_status, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
