# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nhl.games (public API).
# Source: schema/api/groups/public.yml + schema/model/nhl/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NHLGame = TypedDict(
    "NHLGame",
    {
        "id": int,
        "arena_id": int | None,
        "away_team_id": int,
        "home_team_id": int,
        "league_game_id": int | None,
        "league_id": str | None,
        "season_id": int,
        "attendance": int | None,
        "away_team_score": int | None,
        "away_team_score_overtime": int | None,
        "away_team_score_period1": int | None,
        "away_team_score_period2": int | None,
        "away_team_score_period3": int | None,
        "away_team_series_wins": int | None,
        "channel": str | None,
        "date_time": str,
        "date_time_utc": str | None,
        "day": int,
        "game_time": str,
        "home_team_score": int | None,
        "home_team_score_overtime": int | None,
        "home_team_score_period1": int | None,
        "home_team_score_period2": int | None,
        "home_team_score_period3": int | None,
        "home_team_series_wins": int | None,
        "is_overtime": bool,
        "is_shootout": bool,
        "last_play": str | None,
        "neutral_site": bool,
        "overtime_periods": int | None,
        "period": str | None,
        "playoff_round": str | None,
        "season_type": str,
        "series_game_number": int | None,
        "status": str,
        "time_remaining_minutes": int | None,
        "time_remaining_seconds": int | None,
    },
)


class NHLGamesResource:
    _PATH = "/api/v1/nhl/games"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, season_id: int | None = None, home_team_id: int | None = None, away_team_id: int | None = None, status: str | None = None, day: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NHLGame]:
        body, quota = self._transport.request(self._PATH, {
            "season_id": season_id,
            "home_team_id": home_team_id,
            "away_team_id": away_team_id,
            "status": status,
            "day": day,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NHLGame] = body["games"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NHLGame:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NHLGame = body
        return row

    def iter(self, *, season_id: int | None = None, home_team_id: int | None = None, away_team_id: int | None = None, status: str | None = None, day: int | None = None, limit: int | None = None) -> Iterator[NHLGame]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(season_id=season_id, home_team_id=home_team_id, away_team_id=away_team_id, status=status, day=day, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
