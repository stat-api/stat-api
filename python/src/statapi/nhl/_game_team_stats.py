# DO NOT EDIT — generated by generate-python-sdk-public.ts
#
# SDK resource for nhl.game_team_stats (public API).
# Source: schema/api/groups/public.yml + schema/model/nhl/*.yml

from __future__ import annotations

from collections.abc import Iterator
from typing import TypedDict

from ..page import Page
from ..transport import HttpTransport

NHLGameTeamStat = TypedDict(
    "NHLGameTeamStat",
    {
        "id": int,
        "game_id": int,
        "team_id": int,
        "assists": int,
        "blocked_shots": int,
        "faceoff_percentage": float,
        "faceoffs_lost": int,
        "faceoffs_won": int,
        "giveaways": int,
        "goals": int,
        "goals_against": int,
        "hits": int,
        "losses": int,
        "overtime_losses": int,
        "penalties": int,
        "penalty_kill_goals_against": int,
        "penalty_kill_percentage": float,
        "penalty_minutes": int,
        "power_play_attempts": int,
        "power_play_goals": int,
        "power_play_percentage": float,
        "save_percentage": float,
        "saves": int,
        "shooting_percentage": float,
        "short_handed_goals": int,
        "shots": int,
        "shots_on_goal": int,
        "takeaways": int,
        "times_shorthanded": int,
        "wins": int,
    },
)


class NHLGameTeamStatsResource:
    _PATH = "/api/v1/nhl/game_team_stats"

    def __init__(self, transport: HttpTransport) -> None:
        self._transport = transport

    def list(self, *, game_id: int | None = None, team_id: int | None = None, limit: int | None = None, from_id: int | None = None) -> Page[NHLGameTeamStat]:
        body, quota = self._transport.request(self._PATH, {
            "game_id": game_id,
            "team_id": team_id,
            "limit": limit,
            "from_id": from_id,
        })
        rows: list[NHLGameTeamStat] = body["game_team_stats"]
        return Page(
            rows=rows,
            limit=body.get("limit"),
            next_from_id=body.get("next_from_id"),
            quota=quota,
        )

    def get(self, id: int) -> NHLGameTeamStat:
        body, _ = self._transport.request(f"{self._PATH}/{id}")
        row: NHLGameTeamStat = body
        return row

    def iter(self, *, game_id: int | None = None, team_id: int | None = None, limit: int | None = None) -> Iterator[NHLGameTeamStat]:
        # Always walk with from_id (starting at 0): from_id forces the server's
        # ascending keyset order. Without it, DESC-default tables re-yield page
        # 1 and under-walk. Iteration is therefore always oldest-first.
        from_id: int | None = 0
        while True:
            page = self.list(game_id=game_id, team_id=team_id, limit=limit, from_id=from_id)
            for row in page.rows:
                yield row
            if page.next_from_id is None:
                return
            from_id = page.next_from_id
