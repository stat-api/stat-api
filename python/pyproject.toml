# DO NOT EDIT — generated by generate-python-sdk-public.ts
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "statapi"
version = "0.1.0"
description = "Official Python client for the stat-api sports data API"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = []
authors = [{ name = "stat-api" }]
keywords = ["sports", "stats", "api", "nba", "nfl", "mlb", "nhl", "golf"]
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Typing :: Typed",
]

[project.urls]
Homepage = "https://stat-api.com"
Documentation = "https://stat-api.com/docs"
Repository = "https://github.com/stat-api/stat-api/tree/main/python"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
statapi = ["py.typed"]
