// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for kalshi.public_trades (public API).
// Source: schema/api/groups/public.yml + schema/model/kalshi/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface KalshiPublicTrade {
  id: number;
  market_id: number;
  trade_id: string;
  count: number;
  created_time?: string | null;
  no_price?: number | null;
  no_price_dollars?: string | null;
  price?: number | null;
  taker_side?: string | null;
  ticker: string;
  yes_price?: number | null;
  yes_price_dollars?: string | null;
}

export interface KalshiPublicTradesListParams {
  market_id?: number;
  limit?: number;
  from_id?: number;
}

export interface KalshiPublicTradesListResponse {
  public_trades: KalshiPublicTrade[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class KalshiPublicTradesResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: KalshiPublicTradesListParams): Promise<KalshiPublicTradesListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<KalshiPublicTradesListResponse, 'quota'>>(this.opts, `/api/v1/kalshi/public_trades${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<KalshiPublicTrade> {
    return (await fetchJson<KalshiPublicTrade>(this.opts, `/api/v1/kalshi/public_trades/${id}`)).data;
  }
  async *iter(params?: KalshiPublicTradesListParams): AsyncGenerator<KalshiPublicTrade> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: KalshiPublicTradesListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as KalshiPublicTradesListParams);
      for (const row of page.public_trades) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
