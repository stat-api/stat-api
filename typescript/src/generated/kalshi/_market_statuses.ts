// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for kalshi.market_statuses (public API).
// Source: schema/api/groups/public.yml + schema/model/kalshi/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface KalshiMarketStatus {
  id: number;
  details?: Record<string, unknown> | null;
  end_ts: string;
  start_ts: string;
  status: string;
  ticker: string;
}

export interface KalshiMarketStatusesListParams {
  market_id?: number;
  limit?: number;
  from_id?: number;
}

export interface KalshiMarketStatusesListResponse {
  market_statuses: KalshiMarketStatus[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class KalshiMarketStatusesResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: KalshiMarketStatusesListParams): Promise<KalshiMarketStatusesListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<KalshiMarketStatusesListResponse, 'quota'>>(this.opts, `/api/v1/kalshi/market_statuses${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<KalshiMarketStatus> {
    return (await fetchJson<KalshiMarketStatus>(this.opts, `/api/v1/kalshi/market_statuses/${id}`)).data;
  }
  async *iter(params?: KalshiMarketStatusesListParams): AsyncGenerator<KalshiMarketStatus> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: KalshiMarketStatusesListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as KalshiMarketStatusesListParams);
      for (const row of page.market_statuses) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
