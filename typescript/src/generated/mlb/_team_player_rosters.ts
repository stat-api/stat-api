// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for mlb.team_player_rosters (public API).
// Source: schema/api/groups/public.yml + schema/model/mlb/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface MLBTeamPlayerRoster {
  id: number;
  player_id: number;
  season_id: number;
  team_id: number;
  day: number;
  position?: string | null;
  position_depth?: number | null;
  position_group?: string | null;
  roster_type: string;
}

export interface MLBTeamPlayerRostersListParams {
  team_id?: number;
  limit?: number;
  from_id?: number;
}

export interface MLBTeamPlayerRostersListResponse {
  team_player_rosters: MLBTeamPlayerRoster[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class MLBTeamPlayerRostersResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: MLBTeamPlayerRostersListParams): Promise<MLBTeamPlayerRostersListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<MLBTeamPlayerRostersListResponse, 'quota'>>(this.opts, `/api/v1/mlb/team_player_rosters${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<MLBTeamPlayerRoster> {
    return (await fetchJson<MLBTeamPlayerRoster>(this.opts, `/api/v1/mlb/team_player_rosters/${id}`)).data;
  }
  async *iter(params?: MLBTeamPlayerRostersListParams): AsyncGenerator<MLBTeamPlayerRoster> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: MLBTeamPlayerRostersListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as MLBTeamPlayerRostersListParams);
      for (const row of page.team_player_rosters) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
