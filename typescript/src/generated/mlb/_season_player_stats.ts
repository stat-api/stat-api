// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for mlb.season_player_stats (public API).
// Source: schema/api/groups/public.yml + schema/model/mlb/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface MLBSeasonPlayerStat {
  id: number;
  player_id: number;
  season_id: number;
  assists: number;
  at_bats: number;
  balls?: number | null;
  batters_faced: number;
  batting_average: number;
  blown_saves: number;
  caught_stealing: number;
  caught_stealing_by: number;
  complete_games: number;
  double_plays: number;
  doubles: number;
  earned_run_average: number;
  earned_runs: number;
  errors: number;
  fielding_games: number;
  fielding_games_started: number;
  fielding_percentage: number;
  fielding_position: string;
  games_pitched: number;
  games_played: number;
  games_started: number;
  games_started_pitching: number;
  grounded_into_double_play: number;
  hit_by_pitch: number;
  hits: number;
  hits_allowed: number;
  hits_per_nine: number;
  holds: number;
  home_runs: number;
  home_runs_allowed: number;
  home_runs_per_nine: number;
  innings_pitched: number;
  intentional_walks?: number | null;
  left_on_base?: number | null;
  losses: number;
  on_base_percentage: number;
  on_base_plus_slugging: number;
  passed_balls: number;
  pitches_thrown?: number | null;
  plate_appearances: number;
  putouts: number;
  quality_starts?: number | null;
  runs: number;
  runs_allowed: number;
  runs_batted_in: number;
  sacrifice_flies: number;
  sacrifice_hits: number;
  save_opportunities: number;
  saves: number;
  shutouts: number;
  singles?: number | null;
  slugging_percentage: number;
  stolen_bases: number;
  stolen_bases_allowed: number;
  strikeouts: number;
  strikeouts_per_nine: number;
  strikeouts_pitched: number;
  strikes?: number | null;
  total_bases?: number | null;
  total_chances: number;
  triples: number;
  walks: number;
  walks_allowed: number;
  walks_per_nine: number;
  whip: number;
  wins: number;
}

export interface MLBSeasonPlayerStatsListParams {
  season_id?: number;
  player_id?: number;
  limit?: number;
  from_id?: number;
}

export interface MLBSeasonPlayerStatsListResponse {
  season_player_stats: MLBSeasonPlayerStat[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class MLBSeasonPlayerStatsResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: MLBSeasonPlayerStatsListParams): Promise<MLBSeasonPlayerStatsListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<MLBSeasonPlayerStatsListResponse, 'quota'>>(this.opts, `/api/v1/mlb/season_player_stats${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<MLBSeasonPlayerStat> {
    return (await fetchJson<MLBSeasonPlayerStat>(this.opts, `/api/v1/mlb/season_player_stats/${id}`)).data;
  }
  async *iter(params?: MLBSeasonPlayerStatsListParams): AsyncGenerator<MLBSeasonPlayerStat> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: MLBSeasonPlayerStatsListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as MLBSeasonPlayerStatsListParams);
      for (const row of page.season_player_stats) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
