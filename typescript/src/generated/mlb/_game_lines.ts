// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for mlb.game_lines (public API).
// Source: schema/api/groups/public.yml + schema/model/mlb/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface MLBGameLine {
  id: number;
  game_id: number;
  operator_id: number;
  season_id: number;
  captured_at: string;
  implied_away_score?: number | null;
  implied_away_win_pct?: number | null;
  implied_home_score?: number | null;
  implied_home_win_pct?: number | null;
  is_opening: boolean;
  moneyline_away?: number | null;
  moneyline_home?: number | null;
  over_odds?: number | null;
  spread?: number | null;
  spread_away_odds?: number | null;
  spread_home_odds?: number | null;
  total?: number | null;
  under_odds?: number | null;
}

export interface MLBGameLinesListParams {
  game_id?: number;
  limit?: number;
  from_id?: number;
}

export interface MLBGameLinesListResponse {
  game_lines: MLBGameLine[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class MLBGameLinesResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: MLBGameLinesListParams): Promise<MLBGameLinesListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<MLBGameLinesListResponse, 'quota'>>(this.opts, `/api/v1/mlb/game_lines${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<MLBGameLine> {
    return (await fetchJson<MLBGameLine>(this.opts, `/api/v1/mlb/game_lines/${id}`)).data;
  }
  async *iter(params?: MLBGameLinesListParams): AsyncGenerator<MLBGameLine> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: MLBGameLinesListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as MLBGameLinesListParams);
      for (const row of page.game_lines) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
