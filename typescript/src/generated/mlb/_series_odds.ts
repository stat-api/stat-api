// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for mlb.series_odds (public API).
// Source: schema/api/groups/public.yml + schema/model/mlb/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface MLBSeriesOdd {
  id: number;
  away_team_id: number;
  home_team_id: number;
  operator_id: number;
  season_id: number;
  away_odds: number;
  captured_at: string;
  games_played?: number | null;
  home_odds: number;
  series_name: string;
  series_score?: string | null;
}

export interface MLBSeriesOddsListParams {
  season_id?: number;
  limit?: number;
  from_id?: number;
}

export interface MLBSeriesOddsListResponse {
  series_odds: MLBSeriesOdd[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class MLBSeriesOddsResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: MLBSeriesOddsListParams): Promise<MLBSeriesOddsListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<MLBSeriesOddsListResponse, 'quota'>>(this.opts, `/api/v1/mlb/series_odds${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<MLBSeriesOdd> {
    return (await fetchJson<MLBSeriesOdd>(this.opts, `/api/v1/mlb/series_odds/${id}`)).data;
  }
  async *iter(params?: MLBSeriesOddsListParams): AsyncGenerator<MLBSeriesOdd> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: MLBSeriesOddsListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as MLBSeriesOddsListParams);
      for (const row of page.series_odds) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
