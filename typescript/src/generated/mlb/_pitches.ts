// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for mlb.pitches (public API).
// Source: schema/api/groups/public.yml + schema/model/mlb/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface MLBPitch {
  id: number;
  at_bat_id: number;
  batter_id: number;
  game_id: number;
  pitcher_id: number;
  play_id: number;
  air_out: boolean;
  ball: boolean;
  barrel?: boolean | null;
  break_angle?: number | null;
  break_length?: number | null;
  break_y?: number | null;
  breaking_ball?: boolean | null;
  call: string;
  catchers_interference: boolean;
  chase?: boolean | null;
  double: boolean;
  effective_speed?: number | null;
  expected_batting_avg?: number | null;
  expected_woba?: number | null;
  extension?: number | null;
  fastball?: boolean | null;
  flare_burner?: boolean | null;
  fly_out: boolean;
  ground_into_double_play: boolean;
  ground_into_triple_play: boolean;
  ground_out: boolean;
  hard_hit?: boolean | null;
  hit_by_pitch: boolean;
  hit_coord_x?: number | null;
  hit_coord_y?: number | null;
  hit_distance?: number | null;
  hit_exit_velocity?: number | null;
  hit_launch_angle?: number | null;
  home_run: boolean;
  in_play: boolean;
  in_strike_zone?: boolean | null;
  intentional_walk: boolean;
  launch_speed_angle_zone?: number | null;
  line_out: boolean;
  offspeed?: boolean | null;
  pitch_number: number;
  pitch_speed?: number | null;
  pitch_type?: string | null;
  pitch_x?: number | null;
  pitch_y?: number | null;
  pitch_z?: number | null;
  pitch_zone?: number | null;
  plate_x?: number | null;
  plate_z?: number | null;
  pop_out: boolean;
  release_speed?: number | null;
  result: string;
  sacrifice_fly: boolean;
  sacrifice_hit: boolean;
  single: boolean;
  solid_contact?: boolean | null;
  spin_axis?: number | null;
  spin_direction?: number | null;
  spin_rate?: number | null;
  spray_angle?: number | null;
  statcast_quality_score?: number | null;
  strike: boolean;
  strike_zone_bottom?: number | null;
  strike_zone_top?: number | null;
  strikeout: boolean;
  sweet_spot?: boolean | null;
  topped?: boolean | null;
  tracking_confidence?: number | null;
  triple: boolean;
  under?: boolean | null;
  walk: boolean;
}

export interface MLBPitchesListParams {
  game_id?: number;
  limit?: number;
  from_id?: number;
}

export interface MLBPitchesListResponse {
  pitches: MLBPitch[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class MLBPitchesResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: MLBPitchesListParams): Promise<MLBPitchesListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<MLBPitchesListResponse, 'quota'>>(this.opts, `/api/v1/mlb/pitches${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<MLBPitch> {
    return (await fetchJson<MLBPitch>(this.opts, `/api/v1/mlb/pitches/${id}`)).data;
  }
  async *iter(params?: MLBPitchesListParams): AsyncGenerator<MLBPitch> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: MLBPitchesListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as MLBPitchesListParams);
      for (const row of page.pitches) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
