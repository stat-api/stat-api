// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for mlb.player_awards (public API).
// Source: schema/api/groups/public.yml + schema/model/mlb/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface MLBPlayerAward {
  id: number;
  player_id: number;
  season_id: number;
  team_id?: number | null;
  award_date?: string | null;
  award_name: string;
  award_type: string;
  is_winner: boolean;
}

export interface MLBPlayerAwardsListParams {
  player_id?: number;
  season_id?: number;
  limit?: number;
  from_id?: number;
}

export interface MLBPlayerAwardsListResponse {
  player_awards: MLBPlayerAward[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class MLBPlayerAwardsResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: MLBPlayerAwardsListParams): Promise<MLBPlayerAwardsListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<MLBPlayerAwardsListResponse, 'quota'>>(this.opts, `/api/v1/mlb/player_awards${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<MLBPlayerAward> {
    return (await fetchJson<MLBPlayerAward>(this.opts, `/api/v1/mlb/player_awards/${id}`)).data;
  }
  async *iter(params?: MLBPlayerAwardsListParams): AsyncGenerator<MLBPlayerAward> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: MLBPlayerAwardsListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as MLBPlayerAwardsListParams);
      for (const row of page.player_awards) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
