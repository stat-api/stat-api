// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for mlb.season_team_stats (public API).
// Source: schema/api/groups/public.yml + schema/model/mlb/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface MLBSeasonTeamStat {
  id: number;
  season_id: number;
  team_id: number;
  at_bats: number;
  batting_average: number;
  blown_saves: number;
  caught_stealing: number;
  complete_games: number;
  double_plays: number;
  doubles: number;
  earned_run_average: number;
  earned_runs: number;
  errors: number;
  fielding_percentage: number;
  games_played: number;
  hits: number;
  holds: number;
  home_runs: number;
  innings_pitched: number;
  losses: number;
  on_base_percentage: number;
  on_base_plus_slugging: number;
  runs_allowed: number;
  runs_batted_in: number;
  runs_scored: number;
  save_opportunities: number;
  saves: number;
  shutouts: number;
  slugging_percentage: number;
  stolen_bases: number;
  strikeouts: number;
  ties: number;
  triples: number;
  walks: number;
  whip: number;
  wins: number;
}

export interface MLBSeasonTeamStatsListParams {
  season_id?: number;
  team_id?: number;
  limit?: number;
  from_id?: number;
}

export interface MLBSeasonTeamStatsListResponse {
  season_team_stats: MLBSeasonTeamStat[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class MLBSeasonTeamStatsResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: MLBSeasonTeamStatsListParams): Promise<MLBSeasonTeamStatsListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<MLBSeasonTeamStatsListResponse, 'quota'>>(this.opts, `/api/v1/mlb/season_team_stats${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<MLBSeasonTeamStat> {
    return (await fetchJson<MLBSeasonTeamStat>(this.opts, `/api/v1/mlb/season_team_stats/${id}`)).data;
  }
  async *iter(params?: MLBSeasonTeamStatsListParams): AsyncGenerator<MLBSeasonTeamStat> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: MLBSeasonTeamStatsListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as MLBSeasonTeamStatsListParams);
      for (const row of page.season_team_stats) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
