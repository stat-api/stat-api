// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for nfl.depth_chart_entries (public API).
// Source: schema/api/groups/public.yml + schema/model/nfl/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface NFLDepthChartEntry {
  id: number;
  player_id: number;
  snapshot_id: number;
  depth_order: number;
  depth_position: string;
  formation?: string | null;
  jersey_number?: number | null;
  position: string;
}

export interface NFLDepthChartEntriesListParams {
  snapshot_id?: number;
  player_id?: number;
  position?: string;
  limit?: number;
  from_id?: number;
}

export interface NFLDepthChartEntriesListResponse {
  depth_chart_entries: NFLDepthChartEntry[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class NFLDepthChartEntriesResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: NFLDepthChartEntriesListParams): Promise<NFLDepthChartEntriesListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<NFLDepthChartEntriesListResponse, 'quota'>>(this.opts, `/api/v1/nfl/depth_chart_entries${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<NFLDepthChartEntry> {
    return (await fetchJson<NFLDepthChartEntry>(this.opts, `/api/v1/nfl/depth_chart_entries/${id}`)).data;
  }
  async *iter(params?: NFLDepthChartEntriesListParams): AsyncGenerator<NFLDepthChartEntry> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: NFLDepthChartEntriesListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as NFLDepthChartEntriesListParams);
      for (const row of page.depth_chart_entries) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
