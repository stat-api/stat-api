// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for nfl.depth_chart_snapshots (public API).
// Source: schema/api/groups/public.yml + schema/model/nfl/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface NFLDepthChartSnapshot {
  id: number;
  season_id: number;
  team_id: number;
  entry_count: number;
  is_baseline: boolean;
  snapshot_at: string;
  week: number;
}

export interface NFLDepthChartSnapshotsListParams {
  team_id?: number;
  season_id?: number;
  week?: number;
  limit?: number;
  from_id?: number;
}

export interface NFLDepthChartSnapshotsListResponse {
  depth_chart_snapshots: NFLDepthChartSnapshot[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class NFLDepthChartSnapshotsResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: NFLDepthChartSnapshotsListParams): Promise<NFLDepthChartSnapshotsListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<NFLDepthChartSnapshotsListResponse, 'quota'>>(this.opts, `/api/v1/nfl/depth_chart_snapshots${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<NFLDepthChartSnapshot> {
    return (await fetchJson<NFLDepthChartSnapshot>(this.opts, `/api/v1/nfl/depth_chart_snapshots/${id}`)).data;
  }
  async *iter(params?: NFLDepthChartSnapshotsListParams): AsyncGenerator<NFLDepthChartSnapshot> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: NFLDepthChartSnapshotsListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as NFLDepthChartSnapshotsListParams);
      for (const row of page.depth_chart_snapshots) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
