// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for nfl.players (public API).
// Source: schema/api/groups/public.yml + schema/model/nfl/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface NFLPlayer {
  id: number;
  gsis_id: string;
  league_player_id: string;
  team_id?: number | null;
  birth_city?: string | null;
  birth_country?: string | null;
  birth_date?: string | null;
  birth_state?: string | null;
  college?: string | null;
  draft_number?: string | null;
  draft_round?: string | null;
  draft_year?: string | null;
  experience?: number | null;
  first_name: string;
  full_name: string;
  full_position_list?: string | null;
  height?: number | null;
  is_team: boolean;
  jersey?: string | null;
  last_name: string;
  nfl_slug?: string | null;
  overall_pick?: number | null;
  position_group?: string | null;
  primary_position: string;
  profile_pic_url?: string | null;
  roster_status?: string | null;
  weight?: number | null;
}

export interface NFLPlayersListParams {
  team_id?: number;
  roster_status?: string;
  gsis_id?: string;
  primary_position?: string;
  limit?: number;
  from_id?: number;
}

export interface NFLPlayersListResponse {
  players: NFLPlayer[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class NFLPlayersResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: NFLPlayersListParams): Promise<NFLPlayersListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<NFLPlayersListResponse, 'quota'>>(this.opts, `/api/v1/nfl/players${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<NFLPlayer> {
    return (await fetchJson<NFLPlayer>(this.opts, `/api/v1/nfl/players/${id}`)).data;
  }
  async *iter(params?: NFLPlayersListParams): AsyncGenerator<NFLPlayer> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: NFLPlayersListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as NFLPlayersListParams);
      for (const row of page.players) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
