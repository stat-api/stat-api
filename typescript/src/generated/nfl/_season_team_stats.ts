// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for nfl.season_team_stats (public API).
// Source: schema/api/groups/public.yml + schema/model/nfl/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface NFLSeasonTeamStat {
  id: number;
  season_id: number;
  team_id: number;
  completions: number;
  defensive_interceptions: number;
  defensive_sacks: number;
  defensive_tds: number;
  extra_pts_attempted: number;
  extra_pts_made: number;
  field_goals_attempted: number;
  field_goals_made: number;
  first_downs: number;
  fourth_down_attempts: number;
  fourth_down_conversions: number;
  fumbles_recovered: number;
  games_played: number;
  losses: number;
  pass_attempts: number;
  passing_tds: number;
  passing_yds: number;
  pts_allowed: number;
  pts_scored: number;
  red_zone_attempts: number;
  red_zone_conversions: number;
  rushing_tds: number;
  rushing_yds: number;
  third_down_attempts: number;
  third_down_conversions: number;
  ties: number;
  total_yds: number;
  turnovers: number;
  wins: number;
}

export interface NFLSeasonTeamStatsListParams {
  team_id?: number;
  season_id?: number;
  limit?: number;
  from_id?: number;
}

export interface NFLSeasonTeamStatsListResponse {
  season_team_stats: NFLSeasonTeamStat[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class NFLSeasonTeamStatsResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: NFLSeasonTeamStatsListParams): Promise<NFLSeasonTeamStatsListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<NFLSeasonTeamStatsListResponse, 'quota'>>(this.opts, `/api/v1/nfl/season_team_stats${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<NFLSeasonTeamStat> {
    return (await fetchJson<NFLSeasonTeamStat>(this.opts, `/api/v1/nfl/season_team_stats/${id}`)).data;
  }
  async *iter(params?: NFLSeasonTeamStatsListParams): AsyncGenerator<NFLSeasonTeamStat> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: NFLSeasonTeamStatsListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as NFLSeasonTeamStatsListParams);
      for (const row of page.season_team_stats) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
