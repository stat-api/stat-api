// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for nfl.team_player_rosters (public API).
// Source: schema/api/groups/public.yml + schema/model/nfl/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface NFLTeamPlayerRoster {
  id: number;
  player_id: number;
  season_id: number;
  team_id: number;
  day: number;
  depth_order?: number | null;
  position?: string | null;
  position_group?: string | null;
}

export interface NFLTeamPlayerRostersListParams {
  team_id?: number;
  player_id?: number;
  season_id?: number;
  position?: string;
  limit?: number;
  from_id?: number;
}

export interface NFLTeamPlayerRostersListResponse {
  team_player_rosters: NFLTeamPlayerRoster[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class NFLTeamPlayerRostersResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: NFLTeamPlayerRostersListParams): Promise<NFLTeamPlayerRostersListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<NFLTeamPlayerRostersListResponse, 'quota'>>(this.opts, `/api/v1/nfl/team_player_rosters${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<NFLTeamPlayerRoster> {
    return (await fetchJson<NFLTeamPlayerRoster>(this.opts, `/api/v1/nfl/team_player_rosters/${id}`)).data;
  }
  async *iter(params?: NFLTeamPlayerRostersListParams): AsyncGenerator<NFLTeamPlayerRoster> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: NFLTeamPlayerRostersListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as NFLTeamPlayerRostersListParams);
      for (const row of page.team_player_rosters) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
