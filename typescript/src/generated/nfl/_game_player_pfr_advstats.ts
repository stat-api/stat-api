// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for nfl.game_player_pfr_advstats (public API).
// Source: schema/api/groups/public.yml + schema/model/nfl/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface NFLGamePlayerPfrAdvstat {
  id: number;
  game_id: number;
  opponent_team_id: number;
  player_id: number;
  team_id: number;
  carries?: number | null;
  def_adot?: number | null;
  def_air_yds_completed?: number | null;
  def_completion_pct?: number | null;
  def_completions_allowed?: number | null;
  def_ints?: number | null;
  def_missed_tackle_pct?: number | null;
  def_missed_tackles?: number | null;
  def_passer_rating_allowed?: number | null;
  def_pressures?: number | null;
  def_receiving_td_allowed?: number | null;
  def_sacks?: number | null;
  def_tackles_combined?: number | null;
  def_targets?: number | null;
  def_times_blitzed?: number | null;
  def_times_hitqb?: number | null;
  def_times_hurried?: number | null;
  def_yds_after_catch?: number | null;
  def_yds_allowed?: number | null;
  def_yds_allowed_per_cmp?: number | null;
  def_yds_allowed_per_tgt?: number | null;
  passing_bad_throw_pct?: number | null;
  passing_bad_throws?: number | null;
  passing_drop_pct?: number | null;
  passing_drops?: number | null;
  receiving_broken_tackles?: number | null;
  receiving_drop?: number | null;
  receiving_drop_pct?: number | null;
  receiving_int?: number | null;
  receiving_rat?: number | null;
  rushing_broken_tackles?: number | null;
  rushing_yds_after_contact?: number | null;
  rushing_yds_after_contact_avg?: number | null;
  rushing_yds_before_contact?: number | null;
  rushing_yds_before_contact_avg?: number | null;
  times_blitzed?: number | null;
  times_hit?: number | null;
  times_hurried?: number | null;
  times_pressured?: number | null;
  times_pressured_pct?: number | null;
  times_sacked?: number | null;
}

export interface NFLGamePlayerPfrAdvstatsListParams {
  game_id?: number;
  player_id?: number;
  team_id?: number;
  opponent_team_id?: number;
  limit?: number;
  from_id?: number;
}

export interface NFLGamePlayerPfrAdvstatsListResponse {
  game_player_pfr_advstats: NFLGamePlayerPfrAdvstat[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class NFLGamePlayerPfrAdvstatsResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: NFLGamePlayerPfrAdvstatsListParams): Promise<NFLGamePlayerPfrAdvstatsListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<NFLGamePlayerPfrAdvstatsListResponse, 'quota'>>(this.opts, `/api/v1/nfl/game_player_pfr_advstats${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<NFLGamePlayerPfrAdvstat> {
    return (await fetchJson<NFLGamePlayerPfrAdvstat>(this.opts, `/api/v1/nfl/game_player_pfr_advstats/${id}`)).data;
  }
  async *iter(params?: NFLGamePlayerPfrAdvstatsListParams): AsyncGenerator<NFLGamePlayerPfrAdvstat> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: NFLGamePlayerPfrAdvstatsListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as NFLGamePlayerPfrAdvstatsListParams);
      for (const row of page.game_player_pfr_advstats) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
