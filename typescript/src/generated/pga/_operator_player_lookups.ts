// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for pga.operator_player_lookups (public API).
// Source: schema/api/groups/public.yml + schema/model/golf/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface PGAOperatorPlayerLookup {
  id: number;
  operator_id: number;
  operator_player_id: string;
  player_id: number;
  player_name?: string | null;
}

export interface PGAOperatorPlayerLookupsListParams {
  operator_id?: number;
  player_id?: number;
  limit?: number;
  from_id?: number;
}

export interface PGAOperatorPlayerLookupsListResponse {
  operator_player_lookups: PGAOperatorPlayerLookup[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class PGAOperatorPlayerLookupsResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: PGAOperatorPlayerLookupsListParams): Promise<PGAOperatorPlayerLookupsListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<PGAOperatorPlayerLookupsListResponse, 'quota'>>(this.opts, `/api/v1/pga/operator_player_lookups${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<PGAOperatorPlayerLookup> {
    return (await fetchJson<PGAOperatorPlayerLookup>(this.opts, `/api/v1/pga/operator_player_lookups/${id}`)).data;
  }
  async *iter(params?: PGAOperatorPlayerLookupsListParams): AsyncGenerator<PGAOperatorPlayerLookup> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: PGAOperatorPlayerLookupsListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as PGAOperatorPlayerLookupsListParams);
      for (const row of page.operator_player_lookups) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
