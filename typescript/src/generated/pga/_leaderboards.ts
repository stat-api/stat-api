// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for pga.leaderboards (public API).
// Source: schema/api/groups/public.yml + schema/model/golf/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface PGALeaderboard {
  id: number;
  player_id: number;
  player_tournament_id: number;
  tournament_id: number;
  earnings?: number | null;
  fedex_pts?: number | null;
  is_alternate?: boolean | null;
  is_withdrawn?: boolean | null;
  made_cut?: number | null;
  made_cut_did_not_finish?: boolean | null;
  rank?: number | null;
  sg_approach?: number | null;
  sg_around_green?: number | null;
  sg_off_tee?: number | null;
  sg_putting?: number | null;
  sg_tee_to_green?: number | null;
  sg_total?: number | null;
  tee_time?: string | null;
  total_score?: number | null;
  total_strokes?: number | null;
  total_through?: number | null;
  tournament_status?: string | null;
  win?: number | null;
}

export interface PGALeaderboardsListParams {
  tournament_id?: number;
  player_id?: number;
  limit?: number;
  from_id?: number;
}

export interface PGALeaderboardsListResponse {
  leaderboards: PGALeaderboard[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class PGALeaderboardsResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: PGALeaderboardsListParams): Promise<PGALeaderboardsListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<PGALeaderboardsListResponse, 'quota'>>(this.opts, `/api/v1/pga/leaderboards${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<PGALeaderboard> {
    return (await fetchJson<PGALeaderboard>(this.opts, `/api/v1/pga/leaderboards/${id}`)).data;
  }
  async *iter(params?: PGALeaderboardsListParams): AsyncGenerator<PGALeaderboard> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: PGALeaderboardsListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as PGALeaderboardsListParams);
      for (const row of page.leaderboards) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
