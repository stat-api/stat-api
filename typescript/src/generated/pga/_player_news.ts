// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for pga.player_news (public API).
// Source: schema/api/groups/public.yml + schema/model/golf/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface PGAPlayerNew {
  id: number;
  player_id?: number | null;
  tournament_id?: number | null;
  ai_processed: boolean;
  author?: string | null;
  categories?: string | null;
  content?: string | null;
  news_time: string;
  original_source?: string | null;
  original_source_url?: string | null;
  priority: number;
  situational_impact?: Record<string, unknown> | null;
  source?: string | null;
  terms_of_use?: string | null;
  title: string;
  updated_time?: string | null;
  url?: string | null;
}

export interface PGAPlayerNewsListParams {
  player_id?: number;
  tournament_id?: number;
  news_time?: string;
  limit?: number;
  from_id?: number;
}

export interface PGAPlayerNewsListResponse {
  player_news: PGAPlayerNew[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class PGAPlayerNewsResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: PGAPlayerNewsListParams): Promise<PGAPlayerNewsListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<PGAPlayerNewsListResponse, 'quota'>>(this.opts, `/api/v1/pga/player_news${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<PGAPlayerNew> {
    return (await fetchJson<PGAPlayerNew>(this.opts, `/api/v1/pga/player_news/${id}`)).data;
  }
  async *iter(params?: PGAPlayerNewsListParams): AsyncGenerator<PGAPlayerNew> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: PGAPlayerNewsListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as PGAPlayerNewsListParams);
      for (const row of page.player_news) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
