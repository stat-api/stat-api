// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for pga.tournament_odds (public API).
// Source: schema/api/groups/public.yml + schema/model/golf/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface PGATournamentOdd {
  id: number;
  matchup_opponent_id?: number | null;
  player_id: number;
  round_matchup_opponent_id?: number | null;
  three_ball_opponent1_id?: number | null;
  three_ball_opponent2_id?: number | null;
  tournament_id: number;
  first_round_leader_odds?: number | null;
  is_alternate?: boolean | null;
  is_available?: boolean | null;
  make_cut_odds?: number | null;
  matchup_odds?: number | null;
  matchup_opponent_odds?: number | null;
  miss_cut_odds?: number | null;
  outright_odds?: number | null;
  round_matchup_odds?: number | null;
  round_matchup_opponent_odds?: number | null;
  sportsbook?: string | null;
  three_ball_odds?: number | null;
  top_five_odds?: number | null;
  top_ten_odds?: number | null;
  top_twenty_odds?: number | null;
}

export interface PGATournamentOddsListParams {
  tournament_id?: number;
  player_id?: number;
  limit?: number;
  from_id?: number;
}

export interface PGATournamentOddsListResponse {
  tournament_odds: PGATournamentOdd[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class PGATournamentOddsResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: PGATournamentOddsListParams): Promise<PGATournamentOddsListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<PGATournamentOddsListResponse, 'quota'>>(this.opts, `/api/v1/pga/tournament_odds${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<PGATournamentOdd> {
    return (await fetchJson<PGATournamentOdd>(this.opts, `/api/v1/pga/tournament_odds/${id}`)).data;
  }
  async *iter(params?: PGATournamentOddsListParams): AsyncGenerator<PGATournamentOdd> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: PGATournamentOddsListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as PGATournamentOddsListParams);
      for (const row of page.tournament_odds) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
