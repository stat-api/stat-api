// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for polymarket.public_trades (public API).
// Source: schema/api/groups/public.yml + schema/model/polymarket/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface PolymarketPublicTrade {
  id: number;
  market_id: number;
  token_id: string;
  trade_id: string;
  maker_address?: string | null;
  price_bps: number;
  side: string;
  size: number;
  taker_address?: string | null;
  traded_at: string;
}

export interface PolymarketPublicTradesListParams {
  market_id?: number;
  token_id?: string;
  limit?: number;
  from_id?: number;
}

export interface PolymarketPublicTradesListResponse {
  public_trades: PolymarketPublicTrade[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class PolymarketPublicTradesResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: PolymarketPublicTradesListParams): Promise<PolymarketPublicTradesListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<PolymarketPublicTradesListResponse, 'quota'>>(this.opts, `/api/v1/polymarket/public_trades${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<PolymarketPublicTrade> {
    return (await fetchJson<PolymarketPublicTrade>(this.opts, `/api/v1/polymarket/public_trades/${id}`)).data;
  }
  async *iter(params?: PolymarketPublicTradesListParams): AsyncGenerator<PolymarketPublicTrade> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: PolymarketPublicTradesListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as PolymarketPublicTradesListParams);
      for (const row of page.public_trades) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
