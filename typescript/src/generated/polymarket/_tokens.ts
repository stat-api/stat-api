// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for polymarket.tokens (public API).
// Source: schema/api/groups/public.yml + schema/model/polymarket/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface PolymarketToken {
  id: number;
  event_id?: number | null;
  market_id: number;
  token_id: string;
  final_price_bps?: number | null;
  outcome: string;
  outcome_index: number;
  price_bps?: number | null;
  winner?: boolean | null;
}

export interface PolymarketTokensListParams {
  market_id?: number;
  event_id?: number;
  limit?: number;
  from_id?: number;
}

export interface PolymarketTokensListResponse {
  tokens: PolymarketToken[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class PolymarketTokensResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: PolymarketTokensListParams): Promise<PolymarketTokensListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<PolymarketTokensListResponse, 'quota'>>(this.opts, `/api/v1/polymarket/tokens${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<PolymarketToken> {
    return (await fetchJson<PolymarketToken>(this.opts, `/api/v1/polymarket/tokens/${id}`)).data;
  }
  async *iter(params?: PolymarketTokensListParams): AsyncGenerator<PolymarketToken> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: PolymarketTokensListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as PolymarketTokensListParams);
      for (const row of page.tokens) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
