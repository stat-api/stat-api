// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for polymarket.market_snapshots (public API).
// Source: schema/api/groups/public.yml + schema/model/polymarket/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface PolymarketMarketSnapshot {
  id: number;
  condition_id: string;
  market_id: number;
  best_ask_bps?: number | null;
  best_bid_bps?: number | null;
  captured_at: string;
  last_trade_price_bps?: number | null;
  liquidity?: number | null;
  volume?: number | null;
}

export interface PolymarketMarketSnapshotsListParams {
  market_id?: number;
  limit?: number;
  from_id?: number;
}

export interface PolymarketMarketSnapshotsListResponse {
  market_snapshots: PolymarketMarketSnapshot[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class PolymarketMarketSnapshotsResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: PolymarketMarketSnapshotsListParams): Promise<PolymarketMarketSnapshotsListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<PolymarketMarketSnapshotsListResponse, 'quota'>>(this.opts, `/api/v1/polymarket/market_snapshots${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<PolymarketMarketSnapshot> {
    return (await fetchJson<PolymarketMarketSnapshot>(this.opts, `/api/v1/polymarket/market_snapshots/${id}`)).data;
  }
  async *iter(params?: PolymarketMarketSnapshotsListParams): AsyncGenerator<PolymarketMarketSnapshot> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: PolymarketMarketSnapshotsListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as PolymarketMarketSnapshotsListParams);
      for (const row of page.market_snapshots) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
