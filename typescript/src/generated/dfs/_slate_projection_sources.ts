// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for dfs.slate_projection_sources (public API).
// Source: schema/api/groups/public.yml + schema/model/dfs/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface DFSSlateProjectionSource {
  id: number;
  slate_setting_id: number;
  description?: string | null;
  display_order: number;
  enabled: boolean;
  name: string;
  source_type: number;
}

export interface DFSSlateProjectionSourcesListParams {
  limit?: number;
  from_id?: number;
}

export interface DFSSlateProjectionSourcesListResponse {
  slate_projection_sources: DFSSlateProjectionSource[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class DFSSlateProjectionSourcesResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: DFSSlateProjectionSourcesListParams): Promise<DFSSlateProjectionSourcesListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<DFSSlateProjectionSourcesListResponse, 'quota'>>(this.opts, `/api/v1/dfs/slate_projection_sources${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<DFSSlateProjectionSource> {
    return (await fetchJson<DFSSlateProjectionSource>(this.opts, `/api/v1/dfs/slate_projection_sources/${id}`)).data;
  }
  async *iter(params?: DFSSlateProjectionSourcesListParams): AsyncGenerator<DFSSlateProjectionSource> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: DFSSlateProjectionSourcesListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as DFSSlateProjectionSourcesListParams);
      for (const row of page.slate_projection_sources) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
