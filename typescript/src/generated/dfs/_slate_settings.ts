// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for dfs.slate_settings (public API).
// Source: schema/api/groups/public.yml + schema/model/dfs/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface DFSSlateSetting {
  id: number;
  league_id: number;
  operator_id: number;
  contest_type: number;
  game_style?: string | null;
  game_type: number;
  name: string;
  settings: Record<string, unknown>;
}

export interface DFSSlateSettingsListParams {
  limit?: number;
  from_id?: number;
}

export interface DFSSlateSettingsListResponse {
  slate_settings: DFSSlateSetting[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class DFSSlateSettingsResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: DFSSlateSettingsListParams): Promise<DFSSlateSettingsListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<DFSSlateSettingsListResponse, 'quota'>>(this.opts, `/api/v1/dfs/slate_settings${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<DFSSlateSetting> {
    return (await fetchJson<DFSSlateSetting>(this.opts, `/api/v1/dfs/slate_settings/${id}`)).data;
  }
  async *iter(params?: DFSSlateSettingsListParams): AsyncGenerator<DFSSlateSetting> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: DFSSlateSettingsListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as DFSSlateSettingsListParams);
      for (const row of page.slate_settings) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
