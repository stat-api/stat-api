// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for dfs.contest_user_lineups (public API).
// Source: schema/api/groups/public.yml + schema/model/dfs/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface DFSContestUserLineup {
  id: number;
  contest_id: number;
  contest_user_id: number;
  external_id: number;
  points?: number | null;
  rank?: number | null;
  winnings?: number | null;
}

export interface DFSContestUserLineupsListParams {
  contest_id?: number;
  contest_user_id?: number;
  limit?: number;
  from_id?: number;
}

export interface DFSContestUserLineupsListResponse {
  contest_user_lineups: DFSContestUserLineup[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class DFSContestUserLineupsResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: DFSContestUserLineupsListParams): Promise<DFSContestUserLineupsListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<DFSContestUserLineupsListResponse, 'quota'>>(this.opts, `/api/v1/dfs/contest_user_lineups${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<DFSContestUserLineup> {
    return (await fetchJson<DFSContestUserLineup>(this.opts, `/api/v1/dfs/contest_user_lineups/${id}`)).data;
  }
  async *iter(params?: DFSContestUserLineupsListParams): AsyncGenerator<DFSContestUserLineup> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: DFSContestUserLineupsListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as DFSContestUserLineupsListParams);
      for (const row of page.contest_user_lineups) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
