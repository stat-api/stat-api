// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for nhl.teams (public API).
// Source: schema/api/groups/public.yml + schema/model/nhl/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface NHLTeam {
  id: number;
  arena_id?: number | null;
  league_team_id?: number | null;
  abbreviation: string;
  active: boolean;
  city: string;
  conference: string;
  division: string;
  full_name: string;
  head_coach?: string | null;
  logo_url?: string | null;
  name: string;
  primary_color?: string | null;
  quaternary_color?: string | null;
  secondary_color?: string | null;
  tertiary_color?: string | null;
  wikipedia_logo_url?: string | null;
  wikipedia_wordmark_url?: string | null;
}

export interface NHLTeamsListParams {
  active?: boolean;
  limit?: number;
  from_id?: number;
}

export interface NHLTeamsListResponse {
  teams: NHLTeam[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class NHLTeamsResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: NHLTeamsListParams): Promise<NHLTeamsListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<NHLTeamsListResponse, 'quota'>>(this.opts, `/api/v1/nhl/teams${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<NHLTeam> {
    return (await fetchJson<NHLTeam>(this.opts, `/api/v1/nhl/teams/${id}`)).data;
  }
  async *iter(params?: NHLTeamsListParams): AsyncGenerator<NHLTeam> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: NHLTeamsListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as NHLTeamsListParams);
      for (const row of page.teams) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
