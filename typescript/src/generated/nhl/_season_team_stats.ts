// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for nhl.season_team_stats (public API).
// Source: schema/api/groups/public.yml + schema/model/nhl/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface NHLSeasonTeamStat {
  id: number;
  season_id: number;
  team_id: number;
  blocked_shots: number;
  faceoff_losses: number;
  faceoff_percentage: number;
  faceoff_wins: number;
  games_played: number;
  giveaways: number;
  goal_differential: number;
  goals_against: number;
  goals_against_average: number;
  goals_against_per_game: number;
  goals_for: number;
  goals_per_game: number;
  hits: number;
  losses: number;
  overtime_losses: number;
  penalty_kill_goals_against: number;
  penalty_kill_percentage: number;
  penalty_minutes: number;
  power_play_goals: number;
  power_play_opportunities: number;
  power_play_percentage: number;
  pts: number;
  pts_percentage: number;
  save_percentage: number;
  season_type: string;
  shooting_percentage: number;
  short_handed_goals: number;
  shots_against: number;
  shots_for: number;
  shutouts: number;
  takeaways: number;
  times_shorthanded: number;
  wins: number;
}

export interface NHLSeasonTeamStatsListParams {
  season_id?: number;
  team_id?: number;
  limit?: number;
  from_id?: number;
}

export interface NHLSeasonTeamStatsListResponse {
  season_team_stats: NHLSeasonTeamStat[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class NHLSeasonTeamStatsResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: NHLSeasonTeamStatsListParams): Promise<NHLSeasonTeamStatsListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<NHLSeasonTeamStatsListResponse, 'quota'>>(this.opts, `/api/v1/nhl/season_team_stats${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<NHLSeasonTeamStat> {
    return (await fetchJson<NHLSeasonTeamStat>(this.opts, `/api/v1/nhl/season_team_stats/${id}`)).data;
  }
  async *iter(params?: NHLSeasonTeamStatsListParams): AsyncGenerator<NHLSeasonTeamStat> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: NHLSeasonTeamStatsListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as NHLSeasonTeamStatsListParams);
      for (const row of page.season_team_stats) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
