// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for nhl.draft_picks (public API).
// Source: schema/api/groups/public.yml + schema/model/nhl/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface NHLDraftPick {
  id: number;
  draft_id?: number | null;
  original_team_id?: number | null;
  player_id?: number | null;
  team_id: number;
  overall_pick: number;
  pick_in_round: number;
  round: number;
  year: number;
}

export interface NHLDraftPicksListParams {
  year?: number;
  team_id?: number;
  player_id?: number;
  limit?: number;
  from_id?: number;
}

export interface NHLDraftPicksListResponse {
  draft_picks: NHLDraftPick[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class NHLDraftPicksResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: NHLDraftPicksListParams): Promise<NHLDraftPicksListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<NHLDraftPicksListResponse, 'quota'>>(this.opts, `/api/v1/nhl/draft_picks${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<NHLDraftPick> {
    return (await fetchJson<NHLDraftPick>(this.opts, `/api/v1/nhl/draft_picks/${id}`)).data;
  }
  async *iter(params?: NHLDraftPicksListParams): AsyncGenerator<NHLDraftPick> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: NHLDraftPicksListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as NHLDraftPicksListParams);
      for (const row of page.draft_picks) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
