// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for nhl.arenas (public API).
// Source: schema/api/groups/public.yml + schema/model/nhl/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface NHLArena {
  id: number;
  league_arena_id?: number | null;
  address?: string | null;
  capacity?: number | null;
  city: string;
  closed_date?: string | null;
  country?: string | null;
  description?: string | null;
  elevation?: number | null;
  image_url?: string | null;
  latitude?: number | null;
  longitude?: number | null;
  name: string;
  opened_date?: string | null;
  roof_type?: string | null;
  state?: string | null;
  surface: string;
  team_name?: string | null;
  timezone?: string | null;
}

export interface NHLArenasListParams {
  limit?: number;
  from_id?: number;
}

export interface NHLArenasListResponse {
  arenas: NHLArena[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class NHLArenasResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: NHLArenasListParams): Promise<NHLArenasListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<NHLArenasListResponse, 'quota'>>(this.opts, `/api/v1/nhl/arenas${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<NHLArena> {
    return (await fetchJson<NHLArena>(this.opts, `/api/v1/nhl/arenas/${id}`)).data;
  }
  async *iter(params?: NHLArenasListParams): AsyncGenerator<NHLArena> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: NHLArenasListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as NHLArenasListParams);
      for (const row of page.arenas) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
