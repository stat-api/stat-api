// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for nba.game_team_lineup_stints (public API).
// Source: schema/api/groups/public.yml + schema/model/nba/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface NBAGameTeamLineupStint {
  id: number;
  game_id: number;
  player1_id: number;
  player2_id: number;
  player3_id: number;
  player4_id: number;
  player5_id: number;
  team_id: number;
  assist_pct: number;
  defensive_rating: number;
  defensive_rebound_pct: number;
  end_clock: number;
  end_event_seq: number;
  net_rating: number;
  offensive_rating: number;
  offensive_rebound_pct: number;
  pace: number;
  period: number;
  player_stats: Record<string, unknown>;
  possessions: number;
  pts_allowed: number;
  pts_scored: number;
  start_clock: number;
  start_event_seq: number;
  turnover_pct: number;
}

export interface NBAGameTeamLineupStintsListParams {
  game_id?: number;
  team_id?: number;
  limit?: number;
  from_id?: number;
}

export interface NBAGameTeamLineupStintsListResponse {
  game_team_lineup_stints: NBAGameTeamLineupStint[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class NBAGameTeamLineupStintsResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: NBAGameTeamLineupStintsListParams): Promise<NBAGameTeamLineupStintsListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<NBAGameTeamLineupStintsListResponse, 'quota'>>(this.opts, `/api/v1/nba/game_team_lineup_stints${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<NBAGameTeamLineupStint> {
    return (await fetchJson<NBAGameTeamLineupStint>(this.opts, `/api/v1/nba/game_team_lineup_stints/${id}`)).data;
  }
  async *iter(params?: NBAGameTeamLineupStintsListParams): AsyncGenerator<NBAGameTeamLineupStint> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: NBAGameTeamLineupStintsListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as NBAGameTeamLineupStintsListParams);
      for (const row of page.game_team_lineup_stints) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
