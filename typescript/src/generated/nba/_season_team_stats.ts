// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for nba.season_team_stats (public API).
// Source: schema/api/groups/public.yml + schema/model/nba/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface NBASeasonTeamStat {
  id: number;
  season_id: number;
  team_id: number;
  games_played: number;
  losses: number;
  pts_allowed: number;
  pts_scored: number;
  ties: number;
  wins: number;
}

export interface NBASeasonTeamStatsListParams {
  season_id?: number;
  team_id?: number;
  limit?: number;
  from_id?: number;
}

export interface NBASeasonTeamStatsListResponse {
  season_team_stats: NBASeasonTeamStat[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class NBASeasonTeamStatsResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: NBASeasonTeamStatsListParams): Promise<NBASeasonTeamStatsListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<NBASeasonTeamStatsListResponse, 'quota'>>(this.opts, `/api/v1/nba/season_team_stats${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<NBASeasonTeamStat> {
    return (await fetchJson<NBASeasonTeamStat>(this.opts, `/api/v1/nba/season_team_stats/${id}`)).data;
  }
  async *iter(params?: NBASeasonTeamStatsListParams): AsyncGenerator<NBASeasonTeamStat> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: NBASeasonTeamStatsListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as NBASeasonTeamStatsListParams);
      for (const row of page.season_team_stats) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
