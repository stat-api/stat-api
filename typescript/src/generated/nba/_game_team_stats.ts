// DO NOT EDIT — generated by generate-ts-sdk-public.ts
//
// SDK resource for nba.game_team_stats (public API).
// Source: schema/api/groups/public.yml + schema/model/nba/*.yml
import { fetchJson, toQueryString, type Quota, type ResolvedStatApiOptions } from '../../core/client.js';

export interface NBAGameTeamStat {
  id: number;
  game_id: number;
  team_id: number;
  assist_percentage: number;
  assist_ratio: number;
  assists: number;
  assists_to_turnovers: number;
  bench_pts: number;
  blocks: number;
  conference_rank: number;
  defensive_rating: number;
  defensive_rebound_percentage: number;
  defensive_rebounds: number;
  effective_field_goal_percentage: number;
  estimated_defensive_rating: number;
  estimated_net_rating: number;
  estimated_offensive_rating: number;
  estimated_pace: number;
  fast_break_pts: number;
  field_goal_percentage: number;
  field_goals_attempted: number;
  field_goals_made: number;
  fouls: number;
  free_throw_attempt_rate: number;
  free_throw_percentage: number;
  free_throws_attempted: number;
  free_throws_made: number;
  loss: number;
  losses: number;
  minutes: number;
  net_rating: number;
  offensive_rating: number;
  offensive_rebound_percentage: number;
  offensive_rebounds: number;
  pace: number;
  pace_per40: number;
  pct_ast_2pm: number;
  pct_ast_3pm: number;
  pct_ast_fgm: number;
  pct_fga_2pt: number;
  pct_fga_3pt: number;
  pct_pts_2pt: number;
  pct_pts_2pt_mr: number;
  pct_pts_3pt: number;
  pct_pts_fb: number;
  pct_pts_ft: number;
  pct_pts_off_tov: number;
  pct_pts_paint: number;
  pct_uast_2pm: number;
  pct_uast_3pm: number;
  pct_uast_fgm: number;
  player_impact_estimate: number;
  possessions: number;
  pts: number;
  pts_in_paint: number;
  pts_off_turnovers: number;
  rebound_percentage: number;
  rebounds: number;
  second_chance_pts: number;
  steals: number;
  steals_to_turnovers: number;
  three_point_percentage: number;
  three_pointers_attempted: number;
  three_pointers_made: number;
  timeouts_remaining: number;
  true_shooting_percentage: number;
  turnover_percentage: number;
  turnovers: number;
  win: number;
  wins: number;
}

export interface NBAGameTeamStatsListParams {
  game_id?: number;
  team_id?: number;
  limit?: number;
  from_id?: number;
}

export interface NBAGameTeamStatsListResponse {
  game_team_stats: NBAGameTeamStat[];
  limit: number;
  next_from_id: number | null;
  /** Parsed X-Quota-* headers for this request (null if absent). */
  quota: Quota | null;
}

export class NBAGameTeamStatsResource {
  constructor(private readonly opts: ResolvedStatApiOptions) {}

  async list(params?: NBAGameTeamStatsListParams): Promise<NBAGameTeamStatsListResponse> {
    const qs = toQueryString(params as Record<string, unknown> | undefined);
    const r = await fetchJson<Omit<NBAGameTeamStatsListResponse, 'quota'>>(this.opts, `/api/v1/nba/game_team_stats${qs}`);
    return { ...r.data, quota: r.quota };
  }

  async get(id: number): Promise<NBAGameTeamStat> {
    return (await fetchJson<NBAGameTeamStat>(this.opts, `/api/v1/nba/game_team_stats/${id}`)).data;
  }
  async *iter(params?: NBAGameTeamStatsListParams): AsyncGenerator<NBAGameTeamStat> {
    // Always walk with from_id (starting at 0): from_id forces the server's
    // ascending keyset order. Without it, DESC-default tables return page 1
    // newest-first and next_from_id (the page MINIMUM) re-yields those same
    // rows ascending — duplicates + silent under-walk. Iteration order is
    // therefore always oldest-first.
    let fromId = params?.from_id ?? 0;
    for (;;) {
      // Merge the caller's filters with the walking cursor. Cast because a
      // required filter (if any) is enforced by the server (400 →
      // ValidationError), not by the spread's widened type.
      const page: NBAGameTeamStatsListResponse = await this.list({ ...(params ?? {}), from_id: fromId } as NBAGameTeamStatsListParams);
      for (const row of page.game_team_stats) yield row;
      if (page.next_from_id === null) return;
      fromId = page.next_from_id;
    }
  }
}
